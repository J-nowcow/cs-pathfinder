/**
 * 예시 루트 노드.
 *
 * 매일 배치(계획 3)가 생성할 콘텐츠의 형태를 손으로 쓴 것이다.
 * 생성 프롬프트의 기준선이자 확장 엔진 수동 검증용 시드다.
 *
 * 해설 규칙: 결론 먼저, 짧은 문장, 3~5문단, 맥락 중립적.
 * 꼬리질문 규칙: 5개, 서로 다른 방향, 한 문장.
 */
export type ExampleNode = {
  identityScope: string
  category: string
  question: string
  body: string
  suggestions: string[]
}

export const EXAMPLE_NODES: ExampleNode[] = [
  {
    identityScope: 'postgres',
    category: '데이터베이스',
    question: 'DB 커넥션을 매번 새로 맺는 비용이 큰 이유는?',
    body: [
      '커넥션 생성은 객체 하나를 만드는 일이 아니다. 네트워크 연결부터 세션 준비까지 여러 단계를 매번 다시 밟는다.',
      '구체적으로는 TCP 3-way handshake, 인증, 권한 확인, 세션 초기화, 그리고 DB 내부의 커넥션 자원 할당이 일어난다. 각 단계는 왕복 지연을 포함하므로 밀리초 단위 비용이 쌓인다.',
      '요청마다 맺고 닫으면 이 비용이 응답 시간에 그대로 실린다. 동시 요청이 몰리면 문제가 커진다. DB가 질의 처리 대신 커넥션 생성과 해제에 CPU와 메모리를 쓰기 때문이다.',
      'connection pool은 두 가지를 한다. 미리 만들어 둔 커넥션을 재사용해 생성 비용을 없애고, 동시에 DB로 나가는 커넥션 수에 상한을 둬 DB를 보호한다. 뒤쪽이 더 중요하다.',
      '면접에서는 pool 크기를 어떻게 정하느냐로 한 단계 더 들어온다. 크게 잡으면 DB 자원을 과점유하고 작게 잡으면 애플리케이션 쪽 대기가 늘어난다는 양면을 함께 말할 수 있어야 한다.',
    ].join('\n\n'),
    suggestions: [
      'TCP 3-way handshake는 어떤 과정인가?',
      'connection pool size는 어떤 기준으로 정하는가?',
      'pool이 너무 크거나 작으면 각각 어떤 문제가 생기는가?',
      'connection leak은 어떻게 감지하고 방어하는가?',
      'WAS가 여러 대일 때 전체 DB 커넥션 수는 어떻게 계산하는가?',
    ],
  },
  {
    identityScope: 'generic',
    category: '아키텍처 · 분산시스템',
    question: '분산 트랜잭션에서 Saga 패턴이 필요한 이유는?',
    body: [
      '서비스마다 DB가 분리되면 하나의 트랜잭션으로 묶을 수 없기 때문이다. 롤백을 DB에 맡길 수 없으니 애플리케이션이 직접 되돌려야 한다.',
      '2PC는 이 문제를 조정자로 푼다. 다만 모든 참여자가 커밋 준비를 마칠 때까지 자원을 잠그므로 가용성이 떨어진다. 조정자가 죽으면 참여자가 잠긴 채로 남는 위험도 있다.',
      'Saga는 긴 트랜잭션을 로컬 트랜잭션의 연쇄로 바꾼다. 각 단계는 자기 DB에서 즉시 커밋하고, 뒤 단계가 실패하면 앞 단계를 되돌리는 보상 트랜잭션을 실행한다. 잠금이 없으니 가용성이 높다.',
      '대가는 격리성 포기다. 중간 상태가 외부에 보인다. 결제는 됐는데 배송 생성이 실패한 순간이 잠깐 존재한다. 그래서 보상이 반드시 성공해야 하고, 보상 자체가 멱등해야 한다.',
      '실무에서 갈리는 지점은 오케스트레이션이냐 코레오그래피냐다. 중앙 조정자를 둘지 이벤트로 흘릴지는 단계 수와 디버깅 난이도로 판단한다.',
    ].join('\n\n'),
    suggestions: [
      '보상 트랜잭션이 실패하면 어떻게 처리하는가?',
      '2PC와 Saga는 어떤 기준으로 선택하는가?',
      '오케스트레이션과 코레오그래피의 트레이드오프는 무엇인가?',
      '중간 상태가 외부에 노출되는 문제는 어떻게 완화하는가?',
      '메시지 재전송 시 멱등성은 어떻게 보장하는가?',
    ],
  },
  {
    identityScope: 'tcp',
    category: '네트워크',
    question: 'TCP 연결을 끊을 때 TIME_WAIT 상태가 필요한 이유는?',
    body: [
      '마지막 ACK가 유실될 수 있기 때문이다. 먼저 닫는 쪽이 잠시 남아 있어야 상대의 재전송을 받아줄 수 있다.',
      '4-way handshake의 마지막에서 능동 종료 측은 상대의 FIN에 ACK를 보낸다. 이 ACK가 유실되면 상대는 FIN을 재전송한다. 이때 소켓이 이미 사라졌다면 응답할 주체가 없어 상대는 연결을 정상 종료하지 못한다.',
      '두 번째 이유는 이전 연결의 지연 패킷이다. 같은 포트 조합으로 새 연결이 곧바로 열리면 뒤늦게 도착한 옛 패킷이 새 연결의 데이터로 섞일 수 있다. TIME_WAIT은 그 시간을 벌어준다.',
      '대기 시간은 통상 2MSL이다. 문제는 트래픽이 많은 서버에서 TIME_WAIT 소켓이 쌓여 포트가 고갈되는 것이다. 다만 이는 대개 능동 종료를 서버가 하고 있다는 신호다.',
      '면접에서는 해결책을 물어온다. `SO_REUSEADDR`나 커널 파라미터를 만지기 전에 keep-alive로 연결을 재사용하는 쪽이 먼저다. 원인이 연결을 너무 자주 맺고 끊는 데 있기 때문이다.',
    ].join('\n\n'),
    suggestions: [
      '4-way handshake는 왜 3단계로 줄일 수 없는가?',
      'MSL은 무엇이고 2MSL로 정한 근거는 무엇인가?',
      'TIME_WAIT이 쌓여 포트가 고갈되면 어떻게 진단하는가?',
      'keep-alive는 이 문제를 어떻게 줄이는가?',
      'CLOSE_WAIT이 쌓이는 것은 무엇이 다른 문제인가?',
    ],
  },
  {
    identityScope: 'os',
    category: '운영체제',
    question: '컨텍스트 스위칭 비용은 구체적으로 어디서 발생하는가?',
    body: [
      '레지스터 교체 자체보다 캐시가 무너지는 비용이 크다. 눈에 보이는 저장·복원은 일부일 뿐이다.',
      '직접 비용은 명확하다. 실행 중이던 스레드의 레지스터와 프로그램 카운터를 PCB에 저장하고 다음 스레드의 것을 복원한다. 커널 모드 전환도 함께 일어난다. 이건 수 마이크로초 수준이다.',
      '간접 비용이 본체다. CPU 캐시와 TLB에 쌓아둔 내용이 새 스레드에게는 쓸모없다. 다시 실행될 때 캐시 미스가 연달아 나고 메모리에서 다시 읽어와야 한다. 프로세스 간 전환이면 주소 공간까지 바뀌어 TLB를 통째로 비운다.',
      '그래서 스레드를 늘린다고 처리량이 비례해 늘지 않는다. 코어 수를 넘어서면 실행이 아니라 전환에 시간을 쓰기 시작한다. 커넥션 풀 크기를 코어 수 기준으로 잡는 근거가 여기 있다.',
      'CPU 바운드 작업은 코어 수에 맞추고 I/O 바운드는 더 늘려도 되는 이유도 같다. 대기 중인 스레드는 CPU를 점유하지 않기 때문이다.',
    ].join('\n\n'),
    suggestions: [
      'TLB는 무엇이고 왜 프로세스 전환 시 비워야 하는가?',
      '스레드 전환과 프로세스 전환의 비용 차이는 어디서 오는가?',
      'CPU 바운드와 I/O 바운드는 스레드 수를 어떻게 다르게 잡는가?',
      '컨텍스트 스위칭이 과도한지 어떻게 측정하는가?',
      '코루틴은 이 비용을 어떻게 줄이는가?',
    ],
  },
  {
    identityScope: 'postgres',
    category: '데이터베이스',
    question: '인덱스를 만들었는데 실행 계획에서 타지 않는 이유는?',
    body: [
      '인덱스가 있어도 옵티마이저가 쓸 수 없거나 쓰지 않는 편이 낫다고 판단한 것이다. 두 경우는 원인이 다르다.',
      '쓸 수 없는 경우가 먼저다. 인덱스 컬럼에 함수나 연산을 적용하면 인덱스는 무력해진다. 저장된 값과 비교 대상이 달라지기 때문이다. 타입이 암묵적으로 변환되는 경우도 같다. 복합 인덱스에서 선행 컬럼을 조건에 넣지 않은 경우도 여기 속한다.',
      '쓰지 않기로 한 경우는 통계 문제다. 조회 대상이 전체의 상당 비율이면 인덱스로 한 건씩 찾아가는 것보다 풀 스캔이 빠르다. 옵티마이저는 카디널리티 추정으로 이를 판단하는데, 통계가 오래되면 추정이 틀린다.',
      '진단 순서가 있다. 실행 계획을 먼저 보고 인덱스가 후보에 올랐는지 확인한다. 후보에도 없으면 쓸 수 없는 경우이고, 후보에 있는데 선택되지 않았으면 통계나 비용 추정 문제다.',
      '면접에서는 함수 적용 사례를 자주 묻는다. 날짜 컬럼에 포맷 함수를 씌운 조건이 대표적이다. 조건을 범위 비교로 바꾸면 인덱스가 다시 살아난다.',
    ].join('\n\n'),
    suggestions: [
      '복합 인덱스에서 선행 컬럼 규칙은 왜 성립하는가?',
      '카디널리티가 낮은 컬럼에 인덱스가 비효율적인 이유는 무엇인가?',
      '함수 기반 인덱스는 언제 쓰는가?',
      '통계 정보는 언제 갱신되고 어떻게 강제하는가?',
      '커버링 인덱스는 무엇을 절약하는가?',
    ],
  },
  {
    identityScope: 'react',
    category: '프론트엔드',
    question: 'useEffect 의존성 배열을 잘못 넣으면 어떤 문제가 생기는가?',
    body: [
      '너무 적게 넣으면 오래된 값을 붙잡고, 너무 많이 넣으면 불필요하게 다시 실행된다. 방향이 반대인 두 문제다.',
      '의존성을 빠뜨리면 클로저가 첫 렌더의 값을 계속 참조한다. 이펙트 안에서 최신 상태를 읽는 줄 알았는데 초기값이 나오는 상황이 여기서 생긴다. 콘솔에는 아무 오류도 없어 발견이 늦다.',
      '반대로 매 렌더마다 새로 만들어지는 객체나 함수를 의존성에 넣으면 참조가 계속 바뀐다. 이펙트가 매번 실행되고, 그 안에서 상태를 갱신하면 무한 루프가 된다.',
      '정리 함수를 빠뜨리는 것도 같은 범주다. 구독이나 타이머를 걸어두고 정리하지 않으면 언마운트된 컴포넌트가 계속 살아 있다. 이펙트가 자주 실행될수록 누수가 빨리 쌓인다.',
      '판단 기준은 단순하다. 이펙트 안에서 쓰는 모든 외부 값을 넣고, 그래서 너무 자주 실행되면 값 자체를 안정화한다. 의존성을 빼서 해결하는 것은 증상만 가리는 것이다.',
    ].join('\n\n'),
    suggestions: [
      '클로저가 오래된 값을 잡는 현상은 왜 일어나는가?',
      'useCallback과 useMemo는 어떤 기준으로 쓰는가?',
      '정리 함수는 정확히 언제 호출되는가?',
      '의존성에서 함수를 제거하려면 어떤 방법이 있는가?',
      'useEffect 대신 이벤트 핸들러에서 처리해야 하는 경우는 언제인가?',
    ],
  },
  {
    identityScope: 'security',
    category: '인프라 · 보안',
    question: 'JWT를 세션 대신 쓸 때 무엇을 잃는가?',
    body: [
      '즉시 무효화 능력을 잃는다. 서버가 상태를 갖지 않는다는 장점이 그대로 단점이 된다.',
      '세션은 서버가 저장소를 들고 있으므로 지우면 그 순간 끝난다. JWT는 서명만 유효하면 통과하므로 발급된 토큰을 회수할 방법이 없다. 로그아웃, 권한 변경, 탈취 대응이 전부 만료 시각까지 지연된다.',
      '통상적인 대응은 수명을 짧게 가져가고 리프레시 토큰을 붙이는 것이다. 다만 리프레시 토큰은 결국 서버가 상태를 들고 관리해야 하므로, 무상태라는 원래 이점이 상당 부분 사라진다.',
      '저장 위치도 문제다. 로컬 스토리지는 XSS에 그대로 노출되고, 쿠키는 CSRF를 따로 막아야 한다. 어디에 둬도 공짜가 아니다.',
      '판단 기준은 규모다. 서버가 여러 대이고 세션 공유 비용이 실제 부담일 때 JWT가 값을 한다. 단일 서비스에서 관성으로 고르면 잃는 쪽이 크다.',
    ].join('\n\n'),
    suggestions: [
      '리프레시 토큰을 쓰면 무상태라는 이점이 얼마나 남는가?',
      '토큰 탈취를 즉시 차단하려면 어떤 구조가 필요한가?',
      'JWT를 어디에 저장해야 XSS와 CSRF를 함께 줄이는가?',
      '서명 알고리즘 선택이 보안에 어떤 영향을 주는가?',
      '세션 저장소를 공유하는 방식에는 어떤 선택지가 있는가?',
    ],
  },
  {
    identityScope: 'java',
    category: '언어 · 런타임',
    question: 'equals를 재정의할 때 hashCode도 함께 재정의해야 하는 이유는?',
    body: [
      '해시 기반 컬렉션이 두 메서드를 함께 쓰기 때문이다. 하나만 바꾸면 컬렉션이 같은 객체를 다른 객체로 취급한다.',
      'HashMap은 먼저 hashCode로 버킷을 찾고 그 안에서 equals로 비교한다. equals만 재정의하면 논리적으로 같은 두 객체가 서로 다른 버킷으로 흩어진다. equals 비교까지 가지도 못하므로 조회가 실패한다.',
      '규약은 단방향이다. equals가 true면 hashCode는 반드시 같아야 한다. 반대는 성립하지 않는다. hashCode가 같아도 equals는 false일 수 있고 이게 해시 충돌이다.',
      '실무에서 자주 터지는 자리는 JPA 엔티티다. 식별자가 영속화 시점에 채워지므로 id 기반 hashCode는 저장 전후로 값이 바뀐다. Set에 넣어둔 엔티티를 저장하면 다시 찾지 못하게 된다.',
      '가변 필드를 hashCode에 쓰는 것도 같은 함정이다. 컬렉션에 넣은 뒤 필드를 바꾸면 그 객체는 사실상 사라진다.',
    ].join('\n\n'),
    suggestions: [
      'HashMap은 해시 충돌을 어떻게 처리하는가?',
      'JPA 엔티티의 equals와 hashCode는 무엇을 기준으로 구현해야 하는가?',
      '가변 필드를 hashCode에 포함하면 어떤 문제가 생기는가?',
      'hashCode가 항상 같은 값을 반환하면 어떤 일이 벌어지는가?',
      'record와 Lombok의 자동 생성 구현은 어떤 기준을 쓰는가?',
    ],
  },
]
