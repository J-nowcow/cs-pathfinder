# 고치는 법

**가장 반기는 기여는 틀린 해설을 찾아 주시는 거예요.**

해설 초안은 AI가 써요. 표본을 떠 대조해 보니 스무 편 중 열다섯 편에 사실
오류가 있었어요([잰 기록](code/docs/audit/2026-08-08-factcheck-strict.md)).
전부 훑으며 고치는 중이지만 사람 눈이 훨씬 빨라요.

---

## 틀린 걸 봤을 때

### 이슈만 남겨도 충분해요

[새 이슈](https://github.com/J-nowcow/cs-pathfinder/issues/new)에 이렇게만 적어
주셔도 돼요.

```
질문 번호: #157
틀린 문장: "최근 Java의 HashMap은 일정 길이 이상이면 트리로 변환해 이를 해결한다."
왜: 버킷 엔트리 수만이 아니라 테이블 용량이 64 이상이어야 트리로 바꿔요.
    용량이 작으면 먼저 리사이즈해요.
```

번호는 주소에 있어요 — `https://cs-pathfinder.vercel.app/q/157`의 `157`이에요.

### 직접 고치실 거면

**해설 본문은 `cs/explanations/`가 아니라 `code/data/`에 있어요.**
`cs/explanations/`는 떠 놓은 사본이라 거기서 고치면 다음 덤프에 덮여요.

본문은 네 파일 중 하나에 있어요.

| 파일 | 무엇 |
|---|---|
| `code/data/example-nodes.ts` | 사람이 손으로 쓴 30편. 생성 규칙의 기준선이라 시험이 이걸 상대로 걸려 있어요 |
| `code/data/authored-nodes.ts` | 사람이 손으로 쓴 16편 |
| `code/data/generated-nodes.ts` | AI가 배치로 쓴 219편 |
| `code/data/on-demand-nodes.ts` | 사용자가 물어보다 만들어진 26편 |

어느 파일에 있는지 모르시면 문장으로 찾으시면 돼요.

```bash
cd code
grep -rl "고치려는 문장" data/
```

---

## 고칠 때 지켜야 하는 것

검사기가 자동으로 막는 규칙이 있어요. 어기면 시험이 실패해요.

- **한 문단은 150자 아래로.** 폰에서 읽히지 않아요
- **꼬리질문은 35자 아래로, 평어체로.** 단추라서 넘치면 잘려요
- **첫 문장이 곧 답.** 배경이나 정의로 시작하지 않아요
- **도식은 답 바로 뒤에.** 문단을 나누면 도식이 뒤로 밀리니 같이 옮겨 주세요

문단을 나누다 도식이 밀렸으면 이 도구를 쓰시면 돼요.

```bash
cd code
node_modules/.bin/tsx scripts/reorder-blocks.ts "질문 문장"          # 지금 차례 보기
node_modules/.bin/tsx scripts/reorder-blocks.ts "질문 문장" 0 3 1 2  # 차례 바꾸기
```

---

## 보내기 전에

세 가지가 전부 통과해야 해요. **하나라도 실패하면 안 보내는 게 맞아요.**

```bash
cd code
npm run typecheck
npx vitest run
npm run build
```

말뭉치가 규칙을 지키는지만 빠르게 보고 싶으시면 이걸로도 돼요.

```bash
cd code
node_modules/.bin/tsx scripts/find-below-bar.ts --list
```

`합계 0/291 걸림`이 나와야 정상이에요.

---

## 새 질문을 더하고 싶으시면

`code/data/authored-nodes.ts`에 같은 모양으로 붙이시면 돼요. 다만 **이미 있는
질문인지 먼저 봐 주세요.**

```bash
cd code
grep -c "비슷한 키워드" data/*.ts
```

같은 걸 묻는 질문이 이미 있으면 서비스가 그 자리로 보내요. 파일에 둘이 있으면
목록에 같은 질문이 두 번 떠요.

---

## 처음이면 이런 것부터

작고 끝이 분명한 것들이에요. `good first issue` 라벨이 붙은 이슈도 이 종류고요.

| 하는 일 | 고치는 곳 | 크기 |
|---|---|---|
| 해설의 오타·조건 뺀 단정 하나 고치기 | `code/data/`의 해당 본문 | 몇 줄 |
| 용어 사전에 용어 하나 더하기 | `code/data/glossary.ts` | 두 줄 — 단, 본문 5편 이상에 나오는 용어만 |
| 질문에 빠진 태그 달기 | `code/data/node-tags.ts` | 한 줄 — 태그는 `tags.ts`의 16개 안에서만 |
| 난이도 판정 이의 제기 | 이슈로 — 기준은 `code/data/levels.ts` | 이슈 하나 |

셋 다 시험이 지켜요 — 통제 어휘 밖의 태그, 데이터 파일에 없는 질문 키는
`npx vitest run`이 잡아 줘요. 로컬에서 돌려 보고 보내시면 돼요.

---

## 코드 쪽 기여

`code/docs/engineering.md`에 구조를 적어 뒀어요. 고치실 때 두 가지만 지켜 주세요.

- **되돌림을 잡는 시험을 같이 주세요.** 고친 줄을 지웠을 때 실제로 실패하는지
  확인해 주시면 더 좋아요. 아무것도 안 세는 시험이 통과하는 일이 실제로 있었어요
- **왜 그렇게 했는지를 주석에 남겨 주세요.** 이 저장소는 "무엇을 하는가"보다
  "왜 이렇게 됐는가"를 적어요

---

MIT 라이선스예요. 보내 주신 기여도 같은 라이선스로 들어가요.
