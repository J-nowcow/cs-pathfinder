# 24시간 자율 개선 — 목표와 백로그

2026-08-08 시작. 사용자가 하루 자리를 비우는 동안 스스로 굴린다. **매 사이클이 이 파일을 먼저 읽고, 하나를 끝내고, 여기에 표시를 남긴다.** 세션은 매번 새로 뜨므로 이 파일이 유일한 기억이다.

## 24시간 뒤에 있어야 할 것

사용자가 말한 그대로 옮긴다.

1. 개쩌는 도식 — 그림으로 설명되는 해설
2. 가독성
3. 문제 수가 짱짱 많다
4. 진짜 사람들이 궁금해할 CS 지식
5. 매일 볼 수 있다
6. 추천을 받을 수 있다
7. 회원가입해서 각자 공부한 것이 누적된다
8. 그 누적이 잔디로 보인다
9. 가능하면 카카오톡 봇이 매일 질문을 쏜다
10. 이 전부가 오픈소스로 관리되어 GitHub 스타를 모은다

## 판단 기준

하나다. **처음 본 사람이 "이거 정확하고 읽기 좋다"고 느끼는가.**

숫자를 늘리는 것이 목적이 아니다. 틀린 해설 400편보다 맞는 해설 250편이 낫다. 새 질문을 만들 때마다 사실 검증을 같이 건다.

## 이미 있는 것 — 다시 만들지 마라

- 검색 노출: `sitemap.xml` 261개 · `robots.txt` · `manifest.webmanifest` 전부 200 (2026-08-08 실측)
- 질문 번호: `/q/3`과 UUID 둘 다 받는다
- 매일 발행: `publish:daily`, 큐가 2026-08-13까지 차 있다
- 지도·경로 칩·미니맵·공유 트리·OG 이미지
- 도식 렌더러: `flow | state | tree | memory | timeline | stack | table`
- 사실 검증 파이프라인: `docs/audit/` 아래 전수 검사 결과와 교정 적용기(`scripts/apply-fixes.ts`)

전제로 쓰기 전에 **코드로 확인한다.** 지난 리서치가 낸 제안 5개 중 2개가 이미 있는 기능이었다.

---

## 백로그

위에서부터 판다. 한 사이클에 하나. 끝내면 `[x]`로 바꾸고 그 아래에 **무엇을 쟀는지 한 줄** 남긴다.

### ⚡ 우선순위 변경 (2026-08-08 15:40, 사용자 지적)

사용자가 물었다 — **"왜 아직도 그림 안 그렸어?"** 재 보니 맞았다.

```
279편 · 진짜 그림 57편(20.4%) · 표만 167편(59.9%)
```

지금까지 한 것은 **있는 도식을 제대로 그리게** 만든 것이지 **없는 도식을 늘린** 것이 아니다. 아침에 그림이 0편이었으니 0→57은 사실이지만, 독자가 보는 화면의 60%는 그대로다.

**그래서 인증(C)보다 표 167편 가르기를 앞으로 당긴다.**

- [x] **T1. 표 167편을 가른다 (끝).** 분류자 셋(Codex · 서브에이전트 둘)이 각각 따로 읽는 중. **둘 이상 일치한 것만** 바꾼다
  - 원문: `docs/audit/_table-only.md` (`npm run dump:tables`). 반으로 가른 `-a`·`-b`도 있다
  - **먼저 잰 것** — 열 수: 3열 **127편(76.5%)** · 2열 28 · 4열 9 · 6열 2. 첫 칸 이름: **`기준` 117편(70.5%)** · `구분` 9 · 그 밖 소수
  - **3열 127편은 대부분 정당한 비교표로 보인다.** `기준 | A | B`에 행이 비교 축인 모양이다. 표적은 **2열 28편** 쪽일 가능성이 높다
  - **`기준`을 고치는 것은 폰에서 값이 없다.** 좁은 화면에서는 `thead`가 통째로 숨고(`globals.css`) 첫 칸 값이 카드 제목이 된다. 데스크톱 전용 개선이고, 대체할 말은 편마다 달라 기계적으로 못 바꾼다
  - **표를 다 없애는 것이 아니다.** 앞선 손대조는 표 127개 중 65~76%가 정당한 비교표라고 봤다. 표적은 **순서·계층·관계인데 표로 눕힌 것** 30~45편
  - 확신이 없으면 `표유지`. 멀쩡한 비교표를 그림으로 만들면 손해다
- [x] **T2. 판정 끝. 바꾼 것은 2편뿐이다. 그리고 그것이 답이다.**
  - Codex가 25편을 바꾸자고 했는데 원문을 대조하니 **대부분 바꾸면 안 되는 것**이었다
  - **9편은 열을 버린다.** 3·4열 표를 두 칸 도식으로 옮기면 열 하나가 통째로 사라진다. `#30` 격리 수준은 `수준 | 막아주는 것 | 대가`인데 stack으로 가면 `대가`가 없어진다
  - **14편은 제목 붙은 목록이다.** CAP의 P·C·A는 서로 동렬인데 `tree`로 옮기며 없던 뿌리를 만든다. **목록은 도식이 아니다** — 어휘 문서가 조건 집합을 보류로 둔 이유가 그것이다
  - 넣은 둘은 **본문이 이미 그 구조를 말하고 있었다.** `#216` JVM 영역(공유/개별 아래 실제 영역), `#233` HTTPS(본문이 "위에 얹은 구조"라는데 표는 층을 한 칸에 뭉쳤다)
  - **분류자 셋이 다 돌아온 뒤 넷으로 늘었다.** A는 84편 중 83편, B는 83편 중 81편이 표유지였다 — Codex(85%)보다 더 보수적이다
  - **내가 하나 틀렸다.** `#30`은 Codex·A 둘이 일치했는데 내가 "열을 버린다"고 거부했었다. A가 그 손실을 알면서도 "누적 사다리"로 갈랐고 본문이 "위로 올라갈수록"이라 쓴다 → 넣었다
  - **`#233`은 내 규칙을 어겼다.** 1표(Codex)뿐인데 내 판단을 더해 넣었다. B는 "질문이 무엇이 다른가라 대조가 답"이라고 반대했다. 갈린 판정으로 기록하고 되돌리는 법을 스크립트 주석에 적었다
  - **`#203`은 표가 사실을 뒤집고 있었다** — `JVM 내부 | JRE`. JRE가 JVM을 담는다. 도식 판정과 무관하게 고쳐야 하는 것이었다
  - **표만 167→162 · flow 70→71 · stack 34→36 · tree 1→2**
- [x] **T3. 결론이 바뀌었다 — 표를 바꿔서는 그림이 안 는다**
  - 167편 중 **정말 구조인 것은 4편(2.4%)**이었다. 표유지 비율은 Codex 85% · A 98.8% · B 97.6%
  - **표는 대부분 정당하다.** 두 방식을 견주는 글에서 표가 맞는 형태다. 억지로 도식으로 옮기면 열을 잃거나 없던 위계를 만든다
  - **그림을 늘리려면 새로 써야 한다.** 있는 것을 옮기는 길은 여기서 끝났다 → **B2(빈 분야 새 질문)로 간다.** 처음부터 도식을 달고 쓴다

### A. 도식을 본선에 올린다 (목표 1·2)

시안 다섯이 `/zz-proto`에 있다. 설계 노트는 `docs/design/proto-*.md`.

- [x] A1. 판정 → `docs/design/2026-08-08-verdict.md`. Codex 교차검증 + 논증 둘 + 전수 측정
  - **결론이 양쪽 논증 어느 쪽도 아니다.** `blocks.ts:18`이 `flow`를 이미 "행위자 사이의 순서"로 정의하므로 `:::seq`는 같은 뜻의 문법을 둘 만드는 일이다. 그런데 저장된 flow 73개는 **왕복 31(42.5%) · 선형 사슬 22(30.1%) · 그 밖 20**으로 한 모양이 아니다 → **모양을 보고 렌더러를 고른다.** 문법도 본문도 안 건드리고 68/73(93%)이 새 그림을 받는다
  - Codex가 크래시 둘·접근성 셋을 파일·줄로 잡았다. 판정문에 목록
- [x] **A2-a. `flow` → `state` 재태깅.** 도구 `npm run retag` 만듦(기본 미리보기, `--apply`로만 씀, 백업 남김)
  - **결과: 1편.** flow 74→73, state 1→2. `data/generated-nodes.ts`와 운영 DB 양쪽
  - **어휘 문서의 "state 14건"은 재태깅으로 안 된다.** 후보 28건을 열어 보니 진짜 상태 기계로 **쓰인** 것은 서킷 브레이커 하나뿐이었다. 나머지는 마디가 상태가 아니라 **행위자**다(`시스템 -> Scene`, `클라이언트 -> 프록시`). 어휘 문서는 "이 질문의 주제가 상태 기계"로 분류한 것이지 "본문이 그렇게 쓰였다"가 아니다
  - **그래서 나머지 13건은 본문 재작성이 필요하다.** 이름만 바꾸면 상태 도식 자리에 행위자가 들어앉는다 — 지금보다 나쁘다
- [x] **A2-b(1차). `stack` 3편을 제자리로.** 도구 `npm run find:stack`·`npm run fix:stack`·`npm run show`
  - **stack 64→61 · memory 0→2 · 표 136→137.** 주소 공간 둘(#166·#176)과 고장난 표 하나(#179)
  - **주소 공간은 순서를 뒤집어야 맞았다.** `MemoryDiagram`은 위가 높은 주소이고 빈 공간 점선은 `아래로` 바로 밑에 `위로`가 올 때만 생긴다. 저장된 `코드/데이터/힙/스택`은 정확히 거꾸로여서 이름만 바꾸면 코드가 높은 주소에 앉았다
  - **그림이 본문과 어긋나 있었다.** 본문은 "스택은 위에서 아래로, 힙은 아래에서 위로 자란다"인데 그림은 코드를 맨 위에 놓고 있었다. 이제 맞는다. 390px·낭독기 확인 완료
  - #179는 머리줄·구분줄까지는 표인데 아래 네 줄에 파이프가 빠져 `2xx는 성공적인 처리`가 **설명 없는 층 이름**으로 나가고 있었다
  - **`표만 있는 편`이 136→137로 올랐다. 나빠진 것이 아니다** — 고장난 층이 제대로 된 표가 됐다. 계획이 경고한 그 함정이다
  - #166과 #176은 **거의 같은 질문**이다. B6 중복 정리 대상
- [x] **A2-b(2차). 비교표 13편을 표로.** 도구 `npm run dump:stack`·`npm run stack:table`
  - **stack 61→48 · 표 137→150.** 61편 전부를 Codex와 내가 **각각 따로** 판정해 **둘이 일치한 것만** 손댔다
  - Codex 판정 전체: **표 31 · 계층 11 · tree 10 · flow 6 · memory 1 · timeline 1 · 보류 1**
  - 손으로 앞뒤를 적지 않았다. 머리 이름만 주고 변환은 기계가 한다 — 13편을 옮겨 적으면 반드시 하나는 틀린다
  - `#199`는 첫 줄 `저장소 | 데이터`가 이미 머리줄이었다. 그대로 옮기면 머리가 두 번 들어간다. `FIRST_LINE_IS_HEAD`로 처리
- [x] **A2-b(3차). 세 번째 판정으로 14편 더.** 세 분류자(나 · Codex · 서브에이전트)가 각각 따로 판정
  - **stack 48→34 · 표 150→164.** 두 독립 분류자가 일치한 것만
  - **`#41`은 도구가 스스로 걸렀다.** 내용에 `netstat -antp | grep TIME_WAIT`이 있어 파이프가 칸 구분자로 오인된다. "칸이 둘이 아니면 손대지 않는다"는 규칙이 값을 했다
  - 구분줄 거르기를 더했다. 안 그러면 `#248`처럼 울타리 안에 표가 든 것이 `| --- | --- |` 데이터 행을 하나 만든다
  - **표가 136→164로 늘었다. 목표와 반대로 보이지만 아니다** — 비교를 층으로 그리면 *없는 위계*를 만든다(`트라이`가 `해시 맵`보다 위라는 말이 된다). 틀린 도식을 남겨 두는 것보다 맞는 표가 낫다. 표를 줄이는 일은 A2-c(본문 재작성)와 B2(새 질문)에서 한다
- [x] **A2-b(4차) 판정 종료.** 세 분류자가 `계층`으로 본 것은 **그대로 두는 것이 결론**이다 — 판정이 끝난 것이지 미룬 것이 아니다. 남은 34편 중 내용을 고쳐야 하는 것은 A2-c로 넘긴다
  - **어긋난 자리**: `#19`(나 계층 / Codex flow) · `#24`(보류 / 표) · `#32`(보류 / flow) · `#41`(flow / 표) · `#60`(계층 / tree) · `#72`(계층 / 표) · `#85`(표 / timeline) · `#167`·`#171`·`#172`(표·memory / tree) · `#178`(memory / flow) · `#184`(timeline / flow)
  - Codex가 `tree`로 본 10편: `#60 #77 #92 #108 #167 #171 #172 #246 #269 #270`. **`parseTree`는 들여쓰기를 요구하므로 이름만 바꿔선 안 된다** — 구조를 넣어야 한다
  - Codex가 `계층`으로 본 11편은 **그대로 두는 것**이다: `#20 #39 #88 #141 #142 #154 #165 #169 #192 #198 #272`
  - `#192`는 한 줄에 칸이 셋이다(`커널 모드 | 무제한 권한 | 하드웨어 제어`). 설명 칸에 `|`가 그대로 새어 나온다. 따로 봐야 한다
  - `#267`은 첫 행 이름이 `보안 수준`으로 어긋나 있다. 행을 `FTP | 평문 전송. 매우 위험`으로 **고쳐 쓴 뒤** 표로 보내야 한다
  - `#269`는 참조 사슬이 LaTeX 화살표로 한 칸에 뭉쳐 있다. 노드로 펴는 재작성 필요
  - **셋이 갈린 자리**: `#32`(보류/flow/표) · `#72`(계층/표/계층) · `#85`(표/timeline/표) · `#167`·`#172`(표/tree/표) · `#221`·`#246`(표/flow·tree/표) · `#178`(memory/flow/계층) · `#184`(timeline/flow/보류) · `#60`(계층/tree/계층 — `MAX_TREE_DEPTH=3`에 눌려 4단이 뭉개진다)
  - **가는 곳이 예상과 다르다.** 논증자가 57개(정적 표본)를 손으로 분류한 값 — **표 ~18 · memory ~6 · tree ~5 · timeline ~2 · 조건집합 1(보류) · 진짜 계층 ~9**. 가장 큰 덩어리가 `memory`가 아니라 **표**다. 2항 비교를 stack으로 쓴 것들이다
  - **`CallStack` 후보는 1~2개, `Encapsulation` 후보는 사실상 0개.** layers 시안의 근거가 코퍼스에 거의 없다는 뜻이다. A4를 다시 볼 것
  - 위 분류는 **논증자의 손분류이고 내가 대조하지 않았다.** 한 편씩 열어 판정한다. **일괄 치환 금지**
  - stack 안에 마크다운 표 구분선(`--- | ---`)이 든 것이 최소 3건 있다(생성분 1673·2570 등). 확실한 표행이다
  - `npm run retag`는 `stack`→`memory`/`tree`/`timeline`을 받는다. **표로 보내는 것은 울타리를 지우는 일이라 도구가 아직 못 한다** — 그 경로를 먼저 만들어야 한다
- [x] **A2-c(1차). timeline 세 편.** `npm run fix:timeline`. **timeline 0→3**
  - 셋 다 같은 결함이었다 — **두 쪽을 견주는 질문인데 도식이 한쪽만 그린다**
    - `#210` 블로킹 대 논블로킹 — 두 경우를 한 라벨에 슬래시로 욱여넣었다
    - `#211` 경쟁 상태 — **스레드가 하나뿐이었다.** 하나로는 경쟁이 성립하지 않는다. 게다가 그린 것이 문제가 아니라 해법(락)이었다
    - `#220` 인터럽트 대 폴링 — **인터럽트만 그렸다**
  - 울타리만 바꾸지 않았다. 도식이 말을 바꾸면 그것을 가리키는 문장도 같이 바꾼다
  - **빈 칸을 낭독기가 못 읽는 것을 고쳤다.** 빈 `<td>`는 아무 말도 안 하고 지나간다. `아무것도 하지 않는다`를 글자로 남겼다. 시험 3 + 되돌림 확인
- [x] **A2-c(나머지). 다섯 중 둘만 진짜였다.** `timeline 8 → 10 · flow 90 → 89`
  - 한 편씩 열었다. **시간 축이 있는 것은 `#74`와 `#244`뿐**이었다. Codex 교차검증 5/5 일치
    - `#74` GIL — "한 번에 하나만 돈다"가 답인데 표가 그걸 못 보여준다. 스레드 셋이 번갈아 도는 것을 시간 축에 놓았다. CPU/IO 표는 그대로 뒀다
    - `#244` ANR — 도식이 어긋나 있었다. `블로킹 작업 -> 메인 스레드`처럼 **주체가 아닌 것을 주체로** 놓았다. 진짜 답은 "메인 스레드가 몇 초 동안 큐를 못 비운다"라는 시간이다
  - **안 바꾼 셋과 이유** — `#68`(시간 축 없는 2항 비교) · `#196`(두 축의 네 조합이라 timeline은 한 축만 보여준다) · `#197`(계층 차이는 있지만 질문이 견주기)
  - **후보 목록은 추측이었다.** 다섯 중 셋이 아니었다. 다음에도 목록을 그대로 믿지 말고 한 편씩 열 것
- [x] **state 13편은 재작성 대상이 아니다. 항목을 닫는다**
  - 주제로 후보를 뽑아 **다섯 편을 열었고 다섯 다 아니었다**
    - `부모 작업이 취소되면` — 세 주체가 주고받는다. flow가 맞다
    - `CPU 스케줄러의 단계별 역할` — 셋을 견준다. 표가 맞다
    - `데몬 프로세스는 왜 세션 리더로` — 두 실행 형태를 견준다. 표가 맞다
    - `TIME_WAIT 상태가 필요한 이유` — 능동/수동 종료가 주고받는다. flow가 맞다
    - `상태 변경은 어디까지 다시 실행되는가` — 상태·Compose·컴포저블 세 주체다. flow가 맞다
  - 이 문서가 이미 적어 뒀다(`:86`) — "어휘 문서는 **이 질문의 주제가** 상태 기계로 분류한 것이지 **본문이 그렇게 쓰였다**가 아니다". 그 말이 맞았다
  - **state를 늘린 것은 재태깅이 아니라 새로 쓰기였다. 1 → 7편.** 늘리려면 그 길뿐이다

### ⚠️ 본문을 고칠 때 반드시 읽을 것

**정적 파일이 진짜 출처다.** `bootstrap.ts`가 `data/*.ts`에서 `qnode.body`를 덮어쓴다. **DB만 고치면 조용히 되돌아간다** — 배포를 안 해도, 살아 있는 사이트에 요청 하나만 들어오면 그때 복구된다.

실제로 당했다. 34편을 고쳤는데 넷이 원래대로 돌아가 있었다. 그 넷은 `generated-nodes.ts`가 아니라 `example-nodes.ts`에 살던 것이었다.

본문이 사는 곳이 **넷**이다 — `generated-nodes.ts`(대부분) · `example-nodes.ts`(손으로 쓴 30개) · `authored-nodes.ts` · `pending-nodes.ts`. 게다가 **담는 방식이 다르다.**

- `generated-nodes.ts` — 한 줄짜리 문자열에 `\n`을 박아 넣는다
- `example-nodes.ts` — `[ '줄', '줄' ].join('\n')` 줄 배열이다

`scripts/lib/patch-data.ts`가 둘 다 다루고, 못 찾으면 **큰 소리로 알린다**. 새 도구를 만들 때 반드시 이것을 거쳐라. 고친 뒤에는 `docs/audit/_bodies-before-*.json`의 원본과 DB를 대조해 되돌아갔는지 확인한다.

- [x] **A3(1차). 오가는 flow를 기둥과 화살표로.** `flow-shape.ts` + `SequenceDiagram`
  - **flow 블록 72개 중 30개가 새 그림을 받는다.** 나머지 42개는 지금 그대로 — 파이프라인에 기둥을 세우면 마디마다 하나씩 서서 화살표가 글자보다 짧아진다
  - **문법도 저장된 본문도 한 글자 안 고쳤다.** 파싱된 걸음에서 왕복인지 읽어 그림만 고른다
  - 좌표를 한 줄도 계산하지 않는다. 격자 두 층(`repeat(N,1fr)` / `0.5fr repeat(N-1,1fr) 0.5fr`)이 겹쳐 **주체 i의 기둥 = 격자선 i+2** 하나로 끝난다
  - **Codex가 지적한 자동 추론을 뺐다.** 재시도·실패를 구조에서 짐작하면 관계없는 걸음을 오탐한다. `flow` 문법에 그걸 적을 자리도 없다
  - 시험 8 + 렌더 2. **되돌림 양쪽 확인** — 판별을 끄면 3개, 항상 켜면 2개가 깨진다
  - 390px 확인: 4기둥 DNS 조회에서 가로 넘침 0, 낭독기가 `1. 브라우저에서 리졸버로: …`를 읽는다
- [x] **A3(2차). 선형 사슬을 상자와 이음줄로.** `ChainDiagram`
  - **flow 72개 → 기둥 30 · 사슬 22 · 그대로 20.** 이제 **52개(72%)**가 새 그림을 받는다
  - **이름 합치기 걱정은 여기선 없었다.** 사슬은 마디가 저마다 한 번씩만 나오므로 합치는 것이 맞다. 위험했던 건 왕복에 그 규칙을 쓰는 것이었고 왕복은 앞에서 갈라낸다
  - **낭독기 조사 오류를 브라우저에서 잡았다.** `그다음은 주문다` → `주문이다`. 눈으로는 안 보이고 소리로 들으면 걸린다. `needsI`를 만들고 시험을 걸었다
  - 시험 15. 되돌림 양방향(사슬 끄면 3개, 이어짐 검사 빼면 1개 실패). 390px 넘침 0
- [x] **A4를 닫는다. 만들지 않는다.** `stack 40 → 38 · memory 4 → 5 · flow 89 → 90`
  - **`MemoryDiagram`이 이미 `MemoryMap`이 하려던 것을 한다.** 390px에서 확인했다 — `높은 주소`/`낮은 주소` 축 문장이 **눈에 보이는 글**로 붙고, `아래로` 바로 밑에 `위로`가 오면 그 사이에 **빈 공간 점선**이 그려진다. 시안이 내세운 두 가지가 그것이다
  - 검토문 `:122`의 "빈 공간 데이터가 저장분에 없다"는 memory가 0편이던 때의 말이다. **지금 memory 4편 전부가 방향값을 쓴다(100%)**
  - 후보 15편 중 **7편을 열어 판정** — 진짜 후보 **1편**, 잘못 담긴 것 1편, 나머지 5편은 표·tree·stack이 맞다
  - Codex 교차검증: 닫는 것에 동의. 판정 하나를 **반박**했고 받아들였다 — JVM GC의 Young/Old는 논리적 세대라 G1에서는 고정된 물리 층이 아니다. 그 편에 한 문장을 덧붙였다
  - **새 코드 0줄로 두 편만 옮겼다.** 스택 오버플로 → `memory`(가운데 빈 자리가 답이다) · 가상 메모리 → `flow`(시안 문서 자신이 "층이 아니라 가리킴"이라 적었다)
  - **다시 열 조건**(Codex 제안) — *기존 `memory`·`stack`으로 방향·빈 공간·경계를 정확히 못 그리는 독립 사례가 **3편 이상** 쌓일 때.* 그전에는 `layers`를 만들지 마라. 지금 그 수는 1이다
  - `Encapsulation`은 후보 0개다. 되살릴 이유가 없다
- [ ] A5. **`ratio`는 폐기했다. 되살리지 마라 — 세 번 쟀고 세 번 다 0에 가깝다**
  - 어휘 문서가 코퍼스 249개 손대조로 이미 닫았다(`:170`,`:191`)
  - 논증자가 219편에서 실측 단위를 센 값 6건(2.7%)
  - 논증자가 **마크다운 표 131개를 전수 분류**한 값 — 실측 단위(ns/ms/MB/배) 포함 **0개**, Big-O 11개(이미 기각된 부류), 기타 숫자 8개, **순수 정성적 112개(85.5%)**
  - 표 136편이 큰 덩어리인 건 맞지만 **거기 든 것이 수치가 아니다.** ratio를 만들 이유는 "앞으로 그렇게 쓰게 하고 싶다"뿐이고 그건 콘텐츠 근거가 아니다
- [x] **A6. 울타리를 열고도 그림이 안 나온 자리를 잡는다.** `삼켜진 울타리 1 → 0` · `stack 41 → 40` · `표 162 → 163`
  - **전제가 반쯤 틀렸다.** 모르는 이름은 코퍼스에 **0건**이었다. 진짜 구멍은 다른 데 있었다
  - `도식삼킴`은 **도식이 하나도 없을 때만** 운다. 표 하나가 함께 있으면 옆에서 울타리가 통째로 사라져도 지나간다
  - 실제로 한 편이 그랬다. `:::stack` 안에 표 구분선(`--- | ---`)이 들어 있어 파서가 표로 읽고 울타리를 버렸다. **317편 검사에서 0건으로 나왔다** — 그림 하나가 없어진 채로
  - 규칙 둘. **울타리삼킴**(연 개수 대 그려진 개수) · **모르는울타리**(`:::sequence` 같은 없는 종류). 이름이 틀린 것을 "문법이 틀렸다"로 읽으면 엉뚱한 데를 고치므로 갈랐다
  - `contentIssues`는 배치도 운영도 부른다. 매일 올라오는 질문과 사용자가 파는 질문에 그대로 걸린다
  - 걸린 한 편을 고쳤다. 같은 본문에 깨진 문장이 셋 더 있어 함께 고쳤다
  - 시험 6. **두 규칙을 각각 지워 보고 깨지는 것을 확인했다**
  - **표가 162 → 163으로 는 것은 나빠진 것이 아니다.** 고장난 층이 제대로 된 표가 됐다(`:93`이 경고한 그 함정)
- [x] **A7. 재는 도구가 화면이 안 쓰는 기준으로 재고 있었다.** `새 그림을 받는 flow 69/90 (77%)` — 처음 잰 값 52/72(72%)
  - `measure:flow-shape`가 **자기 어림짐작(`shapeOf`)으로 재고 있었다.** 화면이 쓰는 `flowShape()`를 안 부른다. 이 도구가 판별기보다 **먼저** 만들어졌고 사본이 그대로 남았다
  - 지금은 두 값이 같다(42/27/21). **그래서 더 위험하다** — 같아 보이니 아무도 안 본다. `MAX_LANES`를 바꾸면 화면은 달라지는데 도구는 옛 숫자를 계속 찍는다
  - `measure:corpus`가 이 값을 함께 찍는다. 따로 돌려야 하면 아무도 안 본다. **`flow` 개수는 늘어도 새 그림을 받는 수는 그대로일 수 있다**
  - 시험 3. `MAX_LANES`를 4→2로 좁혀 보고 깨지는 것을 확인했다. 개수가 아니라 **비율**로 건다 — flow를 다른 종류로 옮기는 작업이 앞으로도 있다(A2·A4에서 실제로 했다)
- [x] **A8. 시안 제거.** `/zz-proto`(6쪽)·`/zz-svg`·`diagram/proto/`(5개)·`DiagramSvg.tsx`
  - **프로덕션에 공개 주소로 나가 있던 것**이다. 참조하는 곳은 0곳이었다
  - 살아 있는 것은 이미 옮겨졌다 — `SeqProto`→`SequenceDiagram`, `FlowProto`→`ChainDiagram`
  - 되찾는 커밋 해시를 판정문에 적었다(`17093fe`). 판정문이 짚은 줄 번호는 그 커밋 기준이라는 것도

### M. 매칭 후보 (사용자 요청으로 추가)

- [x] **M1. 후보를 구조에서 의미로 넓혔다.** `후보 0개 309(96.3%) → 86(26.8%)` · 평균 `0.1 → 1.8`
  - **게이트는 병목이 아니었다.** 튜닝 124/124 · 홀드아웃 60/60인데 **321개 중 309개가 후보 0개**였다. `qedge`가 12행이라 보여 줄 것이 없었다. 정확한데 일할 기회가 없었다
  - 네 층으로 넓혔다 — `qedge` 자식·형제 → `semantic_relation`(330행이 있는데 안 보고 있었다) → 부모 임베딩 코사인 top-k
  - 단계별로 쟀다. **309 → 110(관계) → 86(벡터).** 관계가 대부분을 메웠고 벡터가 24개를 더 줄였다
  - **런타임에 임베딩 모델을 안 부른다.** `collectCandidates`가 사용자 입력이 아니라 **부모 노드**로 후보를 뽑아서 비교 대상이 전부 미리 계산된 값이다. 그래서 로컬 ollama(`bge-m3`·1024차원)로 밤에 만들 수 있다 — 한도도 위탁 처리자도 지연도 없다
    - 대가는 정확도다. 사용자가 **실제로 무엇을 물었는지**가 아니라 "지금 보고 있는 질문 근처"를 가져온다. 입력으로 찾고 싶어지면 런타임에 같은 모델을 불러야 하고 그때는 로컬이 성립하지 않는다
  - **컬럼을 안 옮겼다.** `('{1,0,0,0}'::real[])::vector(4)`가 되는 것을 PGlite에서 실증했다. 321행에 컬럼 전환·HNSW는 순차 스캔보다 느리고 되돌리기만 어렵다. `0005`의 "값은 그대로 살아 있다"가 참이라 나중에 옮겨도 백필은 불필요하다
  - **문턱 0.7은 실측이다.** 51,360쌍 분포가 중앙값 `0.430` · 90분위 `0.517` · 99분위 `0.611`. **처음에 0.5로 잡았다가 고쳤다** — 중앙값이 0.430이라 0.5는 상위 10%를 통과시킨다. `bge-m3`의 한국어 유사도가 높은 쪽에 몰려 있어 **다른 모델의 감각으로 잡으면 틀린다**
  - `shortlist`도 벡터로 바꿨다. 낱말 방식은 `score === 0`인 쌍을 뺐는데, **같은 분야면 카테고리 가산점 1점이 붙어 0이 안 된다.** 실제로 빠지던 것은 "낱말도 안 겹치고 **분야도 다른** 쌍"이고, 하필 그것이 이 층을 만든 이유였다. **분야를 넘으려고 둔 장치가 분야를 넘는 쌍을 막고 있었다**
    - 낱말 방식은 안 지웠다. 새 노드는 밤 배치 전까지 벡터가 없다
    - **관계는 아직 다시 안 만들었다.** 코드만 바꿨고 `semantic_relation`은 330행 그대로다. 재생성은 밤 배치의 일이다
  - **계획 두 가지가 오진이었다** — ① 매칭 성공 시 `bindAlias`가 게이트 호출을 아낀다고 썼는데 `lookupByHash`가 게이트 **뒤**에 있어 안 아낀다 ② `linkEquivalent`를 죽은 코드라 했는데 **두 노드**를 잇는 표라 매칭 경로(노드 1개)에 자리가 없다. B6에서 쓸 물건이다
  - 시험 20개. 각 층을 지우면 깨진다. votes 문턱을 1로 낮춰 **실제로 깨지는 것을 확인**했다
  - **이 지표는 대리다.** 진짜 보고 싶은 것은 실사용 매칭률인데 아직 홍보 전이라 확장 이벤트가 없다. 트래픽이 생기면 갈아탄다
- [x] **M1-b. 임베딩을 Gemini로 옮겼다.** `후보 0개 83(25.9%)` · 게이트 회귀 `precision 123/123 · recall 123/124 · 오병합 0`
  - **M1의 로컬 ollama 결정을 debate(D3-lite)가 뒤집었다.** 신규 노드를 채우는 경로가 전부 내 맥에 매여 있었다 — 맥이 꺼지면 밀리고 fork한 기여자는 배치를 스스로 지어야 한다. 가중 스코어 6.5 vs 8.3
  - **결정타는 "워크플로 없음"이 오판이었던 것.** `publish-daily.yml`이 매일 돌고 있었다. `ls` 빈 출력을 "없음"으로 읽은 것 — 이미 있는 스케줄러를 두고 launchd(TCC 함정 포함)를 새로 지을 뻔했다
  - 경로 셋: 응답 뒤 `after()` 백필(fail-open) · 매일 GHA 스윕 `/api/embed-sweep`(21:43 UTC, 부분 실패도 502→빨간불) · 전량 `scripts/embed.ts`. **fail-open이 성립하는 것은 스윕이 그물이라서다**
  - **문턱을 다시 쟀다. 이월했으면 두 번째로 틀렸다.** bge에서 정한 0.7이 gemini 분포(중앙값 0.722·90분위 0.765·99분위 0.816)에서 **중앙값 아래**다. 매칭 0.85 · 관계 0.76으로 재설정. 모델을 바꾸면 문턱은 항상 다시 잰다
  - 전환 창 방어 — `vectorNeighbors`가 차원 불일치도 fail-open으로 잡는다. 코드(768)와 DB(1024)가 어긋나는 몇 분간 벡터 후보만 빠지고 확장은 산다
  - 429 실측 — `batchEmbedContents`는 **안에 든 항목이 각각 요청으로 센다.** 50개 두 덩이를 몇 초에 보내면 분당 한도. 덩이 사이 20초 + 백오프 45초
  - 스윕 끝단 검증 — 수동 트리거로 GHA→인증→라우트→DB 확인, `{"status":"ok","scanned":0}` (321편 전부 차 있으니 0이 정답)
  - **한계를 남긴다** — `measure:match`는 고정 후보 세트라 **후보 구성 변화의 회귀를 못 잡는다.** 지금 회귀는 게이트 경로만 확인한 것. 실제 DB 후보로 재는 도구는 아직 없다. recall 1건 놓침(run 2)은 한도 소진 폴백 중의 기록된 흔들림 범위이고 방향이 안전한 실패(새 노드)다
- [x] **M2(관계). Claude 직접 판정으로 60개 추가.** `관계 330 → 390` · `관계 0 노드 117 → 64` · Gemini 0회
  - 벡터 이웃 쌍 221개를 서브에이전트 둘이 2-pass(판정→자기반박)로 심사. **none 172(78%)** — 벡터가 가깝다고 관계가 아니다. "애매하면 none" 기본값이 이 비율을 만들었고 그게 맞다
  - 충돌 해소 기록 — 201-271은 구체적인 쪽(prerequisite), 321-17은 방향 근거 불일치로 shares 강등. **틀린 화살표는 없는 화살표보다 나쁘다**
  - 원본은 `data/relations.ts`. 배포 시 `ensureSeeded`가 DB에 얹는 것을 실측 확인
  - dup 2건 발견(#20/#30·#71/#256) → 등가로. `qnode_equivalence 18 → 20행`
  - **남은 것**: ~~문턱 완화 라운드~~ → **했고 소득 0** (12h 루프 21). 0.70~0.78 쌍 14개 전부 none — 문장 틀 오탐. **고립 66노드는 주제가 실제로 외딴 것**이라 벡터로 못 잇고, 콘텐츠가 늘어야 관계가 생긴다. 승격 파이프라인은 대상이 생기면
- [ ] M2-b. 승격 파이프라인 — `pending` 노드가 생기기 시작하면(홍보 후). 검증→승격→덤프. 처음엔 수동 트리거
- [x] **M3. 계측 수정 완료.** `0013` `matched_via` 추가(gate·hash·suggestion·lease·ancestor) + 미기록 경로 셋 배선
  - 운영 실측이 전제를 확인했다 — accepted 33건 중 matched 5건인데 **그 5건이 무엇인지 로그로 알 수 없었다**
  - "전체 확장의 대부분"(suggestion_resolved)이 이벤트를 안 남겨서, 스펙 §5의 임베딩 활성 조건("같은 개념 재생성 비율")도 잴 수 없었다. 이제 잰다
  - 시험 5. 각 경로의 recordEvent를 지우면 깨진다. 조상 시험은 게이트 화이트리스트 때문에 순환 모양이어야 했다
- [x] **M4. 용어 사전 완료.** 25용어(빈도 실측 ≥5편) · `/glossary` · 본문 첫 등장 링크 · 시험 10
  - 함정 넷 각각 차단 — 도배(본문 단위 seen) · 도식 오염(문단 토큰만, 구조적) · 부분 문자열(경계 검사 + **합성어에 막힌 용어는 다음 홑 등장에서 회복** — 첫 구현이 틀렸고 시험이 잡았다) · 조사(정확 열거)
  - `불변`은 빈도가 됐지만 뺐다 — 일반 형용사와 겹쳐 오탐. 짧고 흔한 낱말은 빈도가 높아도 뺀다
  - 390px 실측: `/glossary` 넘침 0px · `/q/4` 링크 4개(스레드·커널·TLB·프로세스) 각 1회
  - code·bold 토큰은 안 건드린다 — 마크업 중첩의 복잡성이 첫판에 값을 못 한다

### B. 문제 수와 품질 (목표 3·4)

- [x] B1. 기준선 → `docs/audit/2026-08-08-corpus-baseline.md`. `npm run measure:corpus` 추가
  - 277편 중 **표만 있는 편 136(49.1%)** · flow 74 · stack 64 · state 1 · tree 1 · memory 0 · timeline 0 · 통짜 글 1. 쿠버네티스·도커·테스트·빌드·모니터링·Paxos·로드밸런싱·CI/CD 각 0편
- [x] **B2(1차). 빈 분야 넷을 도식과 함께 새로 썼다.** `data/authored-nodes.ts`
  - **279→283편.** flow 71→74 · stack 36→37. 표만 162편 그대로(새 것은 표가 아니다)
  - 넷 다 **도식이 답인 질문**으로 골랐다 — 쿠버네티스 조정 고리(기둥), 도커 이미지 층(층), CI/CD 결과물 승격(사슬), 로드밸런서 헬스체크(기둥)
  - **내 빈 분야 측정이 부분적으로 틀렸다.** `테스트`로 찾아서 이미 있던 `단위 시험과 통합 시험`(#276)을 못 잡았다. 한글 낱말이 다르면 키워드 검색이 놓친다
  - `authored-nodes.ts`에 이미 5편이 있었고 DB에도 #272~276으로 들어가 있었다. **"없다"를 코드로 확인한 값**
- [x] **B2(2차). 넷 더. 전부 기둥과 화살표다**
  - **283→288편**(일일 발행 1편 포함) · flow 74→78. 표만 162편 그대로
  - 제목에 한 번도 안 나오던 말을 세어 골랐다 — `OAuth`·`CORS`·`재처리`·`CDN`이 전부 0건이었다
  - `#279` 인가 코드 흐름(3주체) · `#280` 사전 요청(2주체) · `#281` 실패 메시지 재처리(3주체) · `#282` 엣지 캐시(3주체)
  - **번호가 작게 나온다.** 앞 사이클의 시퀀스 고침이 실제로 들었다 — 278~282
- [x] **B8. 재검 17건을 고쳤다 + 제목 바꾸기 경로.** `남은 오류 17 → 0` · `행 317 그대로` · 말뭉치 규칙 0/317
  - **한 번으로는 부족했다.** 이미 훑고 고친 40편을 같은 잣대로 다시 내니 17편에 오류가 남아 있었다. `docs/audit/2026-08-08-factcheck-strict.md`가 답을 안 남긴 물음의 답이다
  - 고치기 전에 17건이 지금도 있는지 셌다. **17/17 전부 있었다**
  - 본문 12건. **`#12`는 본문 끝에 `Redlock 알고리즘)st l//`가 그대로 화면에 나가고 있었다**
  - `#22`는 **내가 앞 사이클에 쓴 문장**이었다. 고친 사람이 다음 오류를 만든다
  - **제목 5건은 그냥 못 바꾼다.** 시드가 `normalized_question`으로 행을 찾아서, 제목만 고치면 못 찾고 새 행을 만든다. 291행이 317행이 됐던 그 결함이다
    - `data/renames.ts`에 옛 제목을 적고 시드가 그 행을 찾게 했다. `on conflict do update`가 제목도 갱신한다
    - 다섯 다 **원래 번호를 지킨 채** 바뀌었다(#29 #44 #212 #213 #230). 317편 그대로
    - 옛 제목 alias는 안 지웠다 — 옛 문장으로 물어봐도 같은 자리로 가야 한다
  - 시험 5. 옛 제목 찾기와 제목 갱신을 **각각 지워 보고** 깨지는 것을 확인했다
  - **남은 205편은 아직 재검을 안 했다.** 40편에서 17편이면 비슷한 비율이 남아 있다고 봐야 한다. 다음에 이어서 할 것
  - `#213`("해시 충돌이 생기면 어떤 방법으로 푸는가?")은 비슷한 질문이 셋 더 있다. **B6 중복 정리 대상**

- [x] **B2(3차). 목록의 절반은 이미 다뤄져 있었다.** `317 → 321편` · `timeline 10 → 11 · tree 4 → 5`
  - 12개 낱말이 **제목에 0건인 것은 맞았다.** 그런데 낱말이 없다고 주제가 없는 것은 아니었다
    - 백프레셔 → "보내는 쪽이 받는 쪽보다 빠르면 무엇이 터지는가?" · CQRS → "읽기와 쓰기 모델을 나누면" · gRPC → "내부 API에 바이너리 RPC가 유리한 때는?" · SSE → "실시간 갱신에 연결을 계속 열어야 하는가?" · Paxos → Raft가 있어 겹친다
    - GraphQL·CSP는 견주기라 **"도식이 답인 것만"에 안 맞는다**
  - 진짜 빈 곳은 넷. 넷 다 도식이 답이다 — 샤딩(flow) · 레이트 리밋(timeline) · RBAC(tree) · 웹훅(flow)
  - **Codex 판정 4/4 틀림.** 여섯 곳을 고쳤다
    - "나머지 연산이면 거의 모든 키가 옮겨 간다" — 둘로 늘리면 절반쯤이다
    - "`429`와 함께 언제 다시 오라고 알려 준다" — `Retry-After`는 표준이 강제하지 않는다
    - "누구냐가 아니라 무엇이냐에 달렸다" — 요청자와 글 주인의 **관계**다
    - 웹훅 재시도와 고유 식별자 — 둘 다 상대 정책에 달렸다
  - **낱말 세기로 빈 곳을 찾으면 절반이 오탐이다.** 다음에는 주제로 먼저 확인할 것
- [x] **번호 새는 것을 막았다.** `0011_qnode_number_stop_leak.sql`
  - **행 283개에 29,480개를 태우고 있었다.** 시퀀스가 29763까지 갔다
  - 원인은 부팅 시드의 `insert ... on conflict (id) do update`다. `number`를 안 적으면 **컬럼 기본값이 충돌 검사보다 먼저 평가된다.** 이미 있는 행이어도 `nextval`이 돌고 시퀀스는 트랜잭션을 안 타므로 되돌지도 않는다 — **부팅 한 번에 283개씩**
  - 기본값을 떼고 세 삽입 자리가 각자 번호를 붙이게 했다. 부팅 시드는 `created`일 때만
  - **시퀀스 29763 → 277.** 이미 나간 번호(19836·25425·28728~28731)는 안 건드렸다 — 옛 링크가 엉뚱한 질문으로 간다
  - **내 시험이 두 번 헛돌았다.** ① `total`이 0인데 0끼리 견줘 통과 ② `ensureSeeded`가 약속을 캐싱하는데 `resetSeedCache()`를 안 불러 새 DB에 아무것도 안 심겼다. 둘 다 고친 뒤에야 되돌림이 잡혔다
  - **마이그레이션에도 버그가 있었다.** 빈 표에서 `setval(seq, 1, true)`가 되어 첫 번호가 2부터 나갔다. 기존 시험 셋이 그래서 깨졌고 `is_called`를 행 유무로 갈라 고쳤다
- [x] **B3. 면접 후기 조사 → B2 3차로 반영.** 병렬 리서치
  - 조사가 열한 개를 짚었는데 **여덟은 이미 있었다.** URL 입력 전 과정(#225) · JWT 대 세션(#7) · TLS 핸드셰이크(#148·#149) · 프록시 자기호출(#46) · 배포 방식(#59) · Compose 재구성(#95) · ARC(#63·#100) · 컨테이너 대 VM(#272)
  - **키워드로 세면 오탐이 난다.** `프록시`가 스프링 AOP를 잡았다. 제목을 직접 훑어 갈랐다
  - 실제로 0건이던 것 — `스케일`·`부하`·`트래픽`, 그리고 리버스 프록시
  - **조사의 한계도 기록한다.** 한 출처(liamkwo 시리즈) 의존이 크고, 회사별 정식 기출은 대부분 비공개다. LLM·옵저버빌리티는 트렌드 기사만 있고 "실제로 물어봤다"는 1차 근거가 없어 조사자가 스스로 뺐다
- [x] **B2(3차). 셋 더.** 288→291편 · flow 78→79 · stack 38→39 · **state 2→3**
  - `#283` 오토스케일링(상태 전이 — state가 드디어 셋) · `#284` 리버스 프록시(기둥 3주체) · `#285` 이미지와 컨테이너(층 4단)
- [x] **B4를 닫는다. 부족분은 이미 메워졌다.** `프레임워크 22 → 30 · 언어 · 런타임 16 → 37`
  - 이 항목이 지목한 두 분야가 **다른 작업으로 이미 늘었다.** 채울 것이 남아 있지 않다
  - 지금 짧은 곳은 다른 데다 — `아키텍처 · 분산시스템 23` · `자료구조 · 알고리즘 28`. 가장 많은 `네트워크 41`과 벌어져 있다
  - **다만 그것을 메우는 것이 목표는 아니다.** 이 문서의 판단 기준이 그렇게 적혀 있다(`:24`) — "숫자를 늘리는 것이 목적이 아니다. 틀린 해설 400편보다 맞는 해설 250편이 낫다"
  - 분야를 고르게 맞추고 싶어지면 **그때 짧은 분야를 다시 재라.** 이 항목의 22·16은 이미 옛 숫자다
- [x] **B9. 245편 전부 재검했다.** `남은 오류 59건 → 0` · 오류율은 배치마다 **7%에서 43%까지** 갈렸다
  - B8이 "한 번으로는 부족하다"를 보였고 나머지를 마저 훑었다

    | 회차 | 편수 | 남아 있던 오류 |
    |---|---|---|
    | B8 (0·9) | 40 | 17 (43%) |
    | 1차 (3·6·11) | 60 | 17 (28%) |
    | 2차 (1·2·5) | 60 | **4 (7%)** |
    | 3차 (4·7·8·10·12) | 85 | 21 (25%) |
    | **합계** | **245** | **59** |

  - **한 숫자로 "말뭉치 오류율"을 말하면 안 된다.** 배치마다 6배 차이가 난다
  - 굵직한 것 — `#192` 시스템 콜을 컨텍스트 스위칭이라 부름 · `#204` 표가 `Exception` 대 `RuntimeException`으로 갈라 놓음(후자도 전자의 하위다) · `#175` 대칭키를 "비밀키 방식"이라 부르며 비대칭과 섞음 · `#191`·`#248` thread-safe를 상호 배제로만 설명
  - **셋은 내가 앞 사이클에 쓴 문장이었다.** `#246`(스택 오버플로를 "힙에 부딪혀서"로 암시 — A4에서 도식을 옮기며 만든 오류) · `#233`(TLS 키 합의를 (EC)DHE로 단정) · `#22`(B8에서). **고친 사람이 다음 오류를 만든다**
  - 검증 못 하는 것은 안 썼다 — CPython 세대 수가 특정 판에서 바뀌었다는 지적은 확인할 방법이 없어 "판마다 바뀌어 왔다"로만 적었다
  - **이것이 끝이 아니다.** 두 번째 훑기에서 59건이 나왔으니 세 번째에서도 나올 것이다. 다만 배치 2차가 7%인 것을 보면 **수렴하기는 한다**
- [x] **B5(재분류). 미분류 수용소 해체.** `인프라·보안 31 → 18` (운영체제 +7 · 언어 +2 · 자료구조 +2 · 아키텍처 +2)
  - **"19개 중 9개"는 낡은 숫자였다** — 다시 재니 31개 중 13개. B2·B4에 이어 세 번째로 확인된 패턴: 이 백로그의 숫자는 적힌 날의 숫자다
  - **upsert가 분야를 안 갱신하던 결함을 함께 고쳤다.** 파일에서 분야를 고쳐도 기존 행이 그대로였다 — 시험 추가, 갱신 절 지우면 깨지는 것 확인
  - 교체 스크립트 첫 시도가 13건 전부 실패 — `category`가 `question` **앞**에 온다. "못 찾으면 exit 1"이 이걸 잡았다. 조용히 건너뛰었으면 "고쳤는데 안 바뀌는" 상태였다
  - 남긴 18편은 전부 실제 보안·인프라다(#334 Git revert는 도구=인프라 유지 판정)
- [x] **B5-b(1차). 시크릿 관리·HSTS 2편 게시** (#343·#344, 인프라·보안 18→20). Codex 지적 9건 전부 반영(조건부 정확화 — B7의 주 오류 유형). XSS·CSRF는 이미 있었다(낱말 검색 함정 재확인). 남은 후보: 세션 하이재킹·최소 권한·입력 검증
- [x] **B6. 중복을 등가로 기록했다. 지우지 않았다.** `qnode_equivalence 0 → 18행`
  - **잉여 31 목록은 사라져 있었다** — 숫자만 남고 명세가 없었다. 임베딩 0.90+ 쌍 34개를 다시 판정: **중복 17군집(잉여 18) · 관련 15쌍+**. audit의 31과 차이는 0.90 아래 의미 중복(인프라·보안 재등재 9개 = B5 몫)
  - `0014` `canonical_id` 추가 — 남길 쪽 판정 근거(관계 수·판 경로)는 변하므로 **판정 시점의 결정을 저장**한다. 목록이 사라졌던 실수를 표 차원에서 막는다
  - `decided_by`에 `claude` 추가 — 모델 판정을 'human'으로 적으면 검수 기록이 거짓
  - 중복 아님 8건도 `scripts/mark-duplicates.ts` 주석에 남겼다 — 같은 쌍 재판정 방지. 이들은 M2에서 관계로
  - **화면은 아직 안 접는다.** 목록·지도에서 잉여를 접는 것은 별도 결정(정본 표시 UI 포함) — M5로 큐에 추가
- [x] **M5. 등가 접기 라이브.** 잉여 20노드가 목록·지도에서 접힘(322→298 표시 — 차이 검산 완료, 4는 미발행 데일리). 주소는 산다(시험 고정). 필터 SQL은 equivalence.ts 한 곳 — 목록·지도가 갈리는 것을 막는다. active 내리면 되돌아오는 것도 시험
- [x] **M6(데이터). 태그 — 통제 어휘 16개, 174문항 태깅.** `qnode.tags 173/322` (사용자 요청)
  - 원본 `data/node-tags.ts`(질문 키 매핑 한 곳, renames 패턴) · 어휘 `data/tags.ts` — **여기 없는 태그는 시험이 막는다**
  - 판정 Claude 둘 분할, 질문당 0~3개, 애매하면 안 담. 분야와 겹치는 태그는 정보 0이라 금지. 상위: 메모리 29 · 프로토콜 26 · 동시성 24
  - `0015` tags text[] + 시드 조인. 갱신 시험 포함(B5의 분야 미갱신 구멍을 태그에서 선제 차단)
  - #342는 파일로 안 추려진 온디맨드 노드라 제외 — **시험이 잡았다**
  - 빌드 1회 실패에서 배운 것: **prerender가 Neon에 시드를 돈다.** 새 컬럼 마이그레이션은 빌드보다 먼저
- [x] **M6-b. /questions 태그 필터.** 주소 기반(?tag=) · 서버 컴포넌트 그대로(JS 0 증가) · 390px 넘침 0 · 동시성 필터 24개 실측 일치
  - 통제 어휘 밖 값은 조용히 무시 — 주소는 크롤러가 온갖 것을 들고 온다
  - ~~상세 페이지 칩 잔여~~ → **완료** (12h 루프 19). CachedNode→API→PublicNode 전체 배선, /q/4에서 심화·동시성 칩 실측, 390px 넘침 0
- [x] **B7(1차). 오늘 손으로 쓴 11편 사실 검증 — 결과가 나빴다**
  - Codex 판정: **틀림 7 · 부정확 2 · 맞음 2.** 내가 쓰고 배포한 것의 **64%가 부정확**했다
  - 전부 고쳐 다시 배포했다. flow 79→80 · **state 3→2**(아래 참조)
  - **내가 틀린 방식은 하나였다 — 조건부인 것을 법칙처럼 단정했다**
    - 쿠버네티스: "컨트롤러가 만든다" → 상위 컨트롤러가 있을 때만. 독립 파드는 안 살아난다. 컨테이너만 죽으면 kubelet이 재시작한다
    - 로드밸런서: "한 번 실패로 빼지 않는다" → 제품과 설정에 달렸다. 그리고 빼도 **이미 열린 연결은 이어진다**
    - OAuth: "앱 비밀키가 있어야 토큰이 된다" → public client는 비밀키가 없다. PKCE로 묶는다
    - 실패 큐: 재시도·DLQ가 저절로 되는 것처럼 썼다 → **설정하거나 구현하는 정책**이다
    - CDN: "원본까지 가는 것은 사본이 없을 때뿐" → 재검증·우회·캐시 불가도 원본에 간다
    - CORS: 단순 요청과 사전 요청을 안 갈랐다. **사전 요청이 거절되면 진짜 요청은 아예 안 간다**
  - **수치 하나는 근거가 없었다.** "대륙 간 왕복 100밀리초"를 출처 없이 썼다. 조건 없이 빼고 정성적으로 바꿨다
  - **상태 도식 하나를 포기했다.** 오토스케일링을 `state`로 그렸는데 실제 오토스케일러는 상태 기계가 아니라 제어 고리다. 정확도를 택하고 `flow`로 바꿨다 — `state` 편수가 줄었지만 맞는 그림이 맞다
- [x] **B7(2차) — B9로 흡수 종결.** 분야별 표본 20편 재검이 목적이었는데 B9가 모델 작성 245편 **전부**를 4회차로 재검해 표본을 포함했다. 따로 돌릴 것이 남지 않았다
- [ ] **단정 표현을 잡는 검사기는 만들지 마라. 재 봤고 안 된다**
  - `항상`·`반드시`·`모든 ~는`·`무조건`·`뿐이다`·`저절로`가 든 편이 **49/291(16.8%)**
  - 열어 보니 **대부분 정당한 단정**이었다. `equals가 true면 hashCode는 반드시 같아야 한다`는 실제 규약이고, `모든 참여자가 커밋 준비를 마칠 때까지`는 2PC 그대로다. `#49`는 오히려 "무조건 조인으로 바꿀 필요는 없다"는 **완화**인데 낱말만 보면 걸린다
  - **내 오류는 낱말이 아니라 예외를 몰라서 생겼다.** 쿠버네티스의 독립 파드, OAuth의 public client, 로드밸런서의 제품별 차이 — 전부 도메인 지식이지 어법이 아니다. 어휘 검사기로는 못 잡는다
  - `stale-facts.ts`처럼 **특정 낡은 주장**을 집어내는 방식은 여전히 유효하다. 일반화 오류와는 다른 문제다

### C. 회원가입·누적·잔디 (목표 6·7·8)

`docs/plan/2026-08-07-auth-design.md`가 5단계로 쪼개 놨다. **이전에 "손대지 마라"였으나 사용자가 이번에 직접 요청해 해제됐다.**

- [x] **C1. 결정 셋 확정.** 설계안이 이미 근거를 갖고 내렸고, **오늘 실측으로 그 근거가 지금도 참인지 확인했다**
  - `next-auth@beta` = **5.0.0-beta.32**, peerDeps `next: ^14||^15||^16` · `react: ^18.2||^19`. `@auth/pg-adapter@1.11.3` peer는 `pg: ^8`
  - 이 저장소는 **next 16.2.6 · react 19.2.4 · pg ^8.22.0** → 새로 들어오는 런타임 의존이 사실상 없다. `--legacy-peer-deps` 불필요
  - ① Auth.js v5 + `@auth/pg-adapter` + Neon, 구글부터, **세션은 JWT** ② 방침을 로그인 버튼보다 먼저 ③ **익명 지원 유지**(가입은 순수하게 더하기)
  - **Codex가 넷을 뒤집었다 → `docs/design/2026-08-08-auth-verdict.md`**
  - **① v5 beta 보류.** `next-auth@4.24.15`(stable)도 `next: ^16`·`react: ^19`를 선언하고 **beta.32와 같은 날(2026-07-20)** 나왔다. 호환성은 v5를 고를 이유가 못 된다. `5.0.0-beta.0`은 2023-10-24, **1,019일째 beta**
  - **내가 틀린 방식**: v5의 peer만 재고 v4의 peer를 안 쟀다. 사실은 맞았고 **논증이 틀렸다**
  - **② JWT → DB 세션.** JWT는 DB를 안 보므로 탈퇴해도 다른 기기 쿠키가 만료까지 산다. 설계안이 스스로 요구한 "즉시 하드 삭제"와 모순
  - **③ 익명 localStorage 공유는 계정 간 유출이다.** 키가 전 사용자 공통이라 Alice 로그아웃 → Bob 로그인이면 Alice 여정이 Bob 계정으로 병합된다. 키를 가른다
  - **④ FK를 둔다.** "나중에 생긴 `users`를 참조 못 한다"는 성립하지 않는다. `ALTER TABLE`로 걸면 된다
- [x] **C1-b. spike 끝 → `docs/design/2026-08-08-auth-spike.md`. 답은 `better-auth@1.6.26`**
  - 임시 작업공간에 우리 런타임(next 16.2.6·react 19.2.4·pg ^8.22.0)을 맞추고 **실제로 설치해 생성자를 호출**했다
  - **v4는 후보에서 빠진다. `@next-auth/pg-adapter`가 존재하지 않는다** — 공식 어댑터는 Prisma·TypeORM뿐이다. Codex가 "v4 어댑터를 우선하라"고 했지만 있는지 확인하지 않은 말이었다
  - better-auth: **stable** · `pg.Pool` 직접 받음(실증) · **`session` 테이블이 기본**(Codex가 요구한 즉시 차단이 저절로 풀림) · **`kakao`·`naver`가 1급 provider**(커스텀 불필요) · 테이블이 **단수**라 저장소 관례와 같음 · +17 패키지
  - v5 beta는 `5.0.0-beta.0`이 2023-10-24로 **1,019일째 beta**
  - **못 잰 것**: 실제 로그인 왕복(OAuth 클라이언트 없음) · 마이그레이션 SQL 추출 · 카카오 실동작 · 부하
- [x] **C1-d. 항목별 결정 완료** → docs/design/2026-08-10-auth-data-decision.md. 받는 것 2(email·세션)·안 받는 것 6(name·image·토큰3·IP/UA). /privacy 반영은 C3 배포와 같은 커밋(원칙: 코드에서 확인한 것만 적는다)
  - `user.image` (설계안은 "저장 안 한다"고 했다) · `account.accessToken`·`refreshToken` · **`session.ipAddress`·`userAgent`**
  - 안 쓰는 것은 안 받는 것이 지키기 쉽다. 끄는 것과 적는 것을 갈라 정한다
- [x] **C1-c. 공용 Pool 노출.** `getPool()` — 만든 것을 내주기만 하고 **새로 만들지 않는다.** PGlite면 `null`
  - **첫 시험이 아무것도 안 잡았다.** 되돌림을 걸어 보니 풀 얹는 줄을 지워도, 되돌릴 때 놓는 줄을 지워도 통과했다. PGlite에서는 풀이 어차피 `null`이라 관찰이 안 된다. 표식을 직접 얹고 그 다음 계약을 거는 방식으로 다시 썼다
  - **못 거는 것을 시험 파일에 적어 뒀다** — `createPostgres`가 풀을 얹는 행위 자체는 실제 Postgres 경로에서만 돌아 여기서 못 건다
  - 곁가지로 확인한 것: `postgresUrl()`이 `NODE_ENV==='test' && USE_REAL_DB!=='1'`이면 무조건 `null`을 준다. **시험이 운영 DB를 건드릴 길은 막혀 있다**(`truncateAll`이 있는 코드베이스라 확인이 필요했다)
- [x] **C2(방침). `/privacy` 올림.** 바닥글에 걸었다
  - **계정이 생기기 전에 이미 필요했다.** 지금도 IP를 저장하고(`quota/key.ts`, 하루 한도) 식별 쿠키를 심는다(`vote/identity.ts`, `cspf_vid`·1년·httpOnly). 제2조는 "업무를 목적으로" 운용하는 개인도 처리자로 본다
  - **코드에서 확인한 것만 적었다.** 있어 보이려고 항목을 늘리지 않았다. 수집 항목이 바뀌면 이 페이지도 같이 바꿔야 한다
  - 위탁: Vercel(실행) · Neon(보관) · Google Gemini(해설 생성 — 질문 글자만, IP·쿠키는 안 보냄)
  - 링크가 빠지는 회귀를 잡는 시험 1개(되돌림 확인). 390px 넘침 0
- [x] **C2. 이용약관 /terms 라이브.** 7조항 — 핵심은 2조(AI가 쓰고 틀릴 수 있다, 오류 0 약속 안 함, 답변 책임은 답한 사람). 준거법·분쟁 조항은 감당 못 할 약속이라 뺌. 바닥글 링크
- [ ] **C3 앞에 사람이 해야 하는 것** — Google Cloud Console에서 OAuth 클라이언트를 만들어 `AUTH_GOOGLE_ID`·`AUTH_GOOGLE_SECRET`을 줘야 한다. **동의 화면을 "테스트 중"으로 두면 refresh token이 7일에 만료된다.** Production으로 전환해야 하고 `email`·`profile`·`openid`는 non-sensitive라 검수 없이 된다
- [ ] C3. 로그인만 (구글). 스키마는 추가만 한다
- [ ] C4. 여정 서버 저장·병합. **여기가 제일 위험하다** — OAuth는 전체 페이지 이동이라 `ReadingView`가 재마운트되고 1개짜리 여정이 서버로 올라간다. `ReadingView.journey.test.tsx`가 잡던 그 버그와 같은 모양이다. 시험을 먼저 쓴다
- [x] **C5. `/me` 공부 기록.** 로그인 없이 붙였다 — 무엇을 팠는지는 여정이 이미 알고 있었다
  - 이어서 판 날·판 질문 수·연 횟수. 같은 질문을 다시 열어도 그날 한 번만 센다
- [x] **C6. 잔디.** 26주. React 요소, 390px 확인, 낭독기가 읽을 한 문장을 함께 둔다
  - 여정에는 시각이 없어(`journey/types.ts`) 날짜만 따로 적는다. 여정 형식을 바꾸면 저장된 기록이 통째로 버려진다
- [x] **C7. 익명 보장 실측 완료.** 공개 화면·API 11개 무인증 200(유일한 401은 CRON 내부 API — 정상) · 확장(suggestion 해소) 무인증 200·쿼터 0 · 이 호출이 M3 계측의 첫 운영 발동(matched_via=suggestion 확인). 실측 목록을 C3의 익명 회귀 가드 기준선으로 auth-data-decision.md에 박음

### D. 추천 (목표 6)

- [x] D1. **M1이 해결했다.** 근거는 임베딩 코사인 + `semantic_relation`. 이 항목의 "277편 전부 NULL"은 옛 숫자다
- [x] D2. **M1-b가 해결했다.** 321/321 채움(gemini-embedding-001·768차원) + 신규 노드는 `after()` 백필 + 매일 GHA 스윕
- [ ] D3. "이거 봤으면 이것도" — 익명도 되게. localStorage 여정 기반. 임베딩·관계가 준비됐으니 화면만 남았다
- [ ] D4. 로그인 사용자에겐 안 본 것·복습할 것 우선 (C3 뒤)

### E. 오픈소스 정비 (목표 10)

- [x] **E1. README 현행화.** "그림이 없다"는 전제가 낡아 있었다 — 199줄에 `<img>` 7장이 이미 걸려 있었다(`![`만 센 측정의 오판 직전). 낡은 곳만 갱신: 수치 322 · 화면 여섯 · "태그와 난이도" 섹션 신설 · 용어 링크 안내 · 정확도 섹션 완료형(245편 59건·회차별 7~43%) · 스크린샷 4장 갱신
- [x] **E2. 질문 하나에 파일 하나.** 사용자가 직접 요청한 것이다
  - **분야당 한 파일(29KB·30편) → 질문당 한 파일 258개.** 평균 1.4KB, 가장 큰 것 2.1KB
  - `cs/explanations/<분야>/<번호>-<슬러그>.md` + 분야마다 `README.md` 목차 + 최상위 목차
  - 번호를 세 자리로 채운다. 안 채우면 파일 목록이 `1, 10, 100, 2`로 선다
  - 파일 번호 = 서비스 주소 번호(`/q/31`). 파일·링크·서비스가 한 번호로 꿰인다
  - 상대 링크 깨진 것 0건(전수 검사). 분야 목차가 한 단계 깊어져 `../../` → `../../../`
  - 왜 나눴나 — GitHub 코드 검색은 파일 단위라 30편이 한 파일이면 어디서 걸렸는지 안 보이고, 이슈에서 특정 질문을 가리킬 주소가 없고, 오타 하나에 29KB PR을 열어야 한다
- [x] **E3. 이슈 템플릿 3종 + "처음이면 이런 것부터".** 사실오류·제안·버그, blank 허용
  - 사실 오류 템플릿 첫 줄이 "직접 고치지 않으셔도 돼요" — 신고 문턱이 목적
  - CONTRIBUTING에 작고 끝이 분명한 기여 4종 표(오타·용어·태그·난이도 이의) + 시험이 지키는 범위
  - **good first issue 실제 등록은 사용자 몫** — 자기 저장소 이슈가 "외부 게시"는 아니나 라벨 체계·첫 공개 이슈의 톤은 소유자가 정하는 것이 맞다
  - 본문은 `cs/explanations/`가 아니라 `code/data/`에 있다는 것부터 적었다. 거기서 고치면 다음 덤프에 덮인다
- [x] **E4. audit README.** 핵심 숫자 시계열 + 반증 기록 절 + 방법 뼈대 4줄. 작업 파일은 재현용이라 안 치운다고 명시
- [x] **E5. 라이선스 일관 확인.** LICENSE 실재(MIT)·README·CONTRIBUTING·terms 일관. 구멍 1 — package.json license 필드 없음 → MIT 명시. README 인용 문단 보강(출처 권장 + 오류 고지 연결)

### F. 카카오톡 (목표 9)

- [x] **F1. 카카오 조사 끝.** 병렬 리서치
  - **길이 있다.** 카카오 로그인 `talk_message` 권한의 **"나에게 보내기"는 검수 없이·사업자등록 없이·무료**다. 서버가 각 가입자의 토큰으로 매일 부르면 카톡이 간다
  - **막히는 것**: "친구에게 보내기"는 비즈 앱 + 검수. 채널 개설은 사업자등록증 또는 전자증명서 + 본인인증(사용자만 가능). 알림톡 8원/건, 친구톡 22원/건. 오픈빌더 신규 챗봇은 **공식 에이전시를 통해서만** — 개인 직접 신청 불가
  - **미확인**: 자동화(cron)에서 refresh token 만료 주기. 공식 문서에 수치가 없다. **이게 이 경로의 급소다** — 만료되면 매일 발송이 조용히 멈춘다
  - **먼저 할 것은 카톡이 아니다.** "나에게 보내기"는 **로그인이 있어야** 성립한다(C 사슬 뒤). 그 전에 값이 나오는 것은 웹푸시(무료·PWA manifest 이미 있음·iOS 16.4+는 홈화면 추가 필요)와 RSS(무료·막을 것 없음)다
- [ ] F1-b. refresh token 만료 주기를 실측하거나 카카오에 문의. 확인 못 하면 이 경로는 "매일 보내다 조용히 멈추는" 위험을 안는다
- [ ] F2. 할 수 있는 데까지 만든다. 발송 파이프라인은 플래그 뒤에 두고 **실제 발송은 하지 않는다**
- [x] **F3. 알림 채널 비교 완료** → docs/design/2026-08-10-notify-channels.md. 순서가 답: RSS(이미 삶) → 웹푸시(수요 숫자 먼저 — 신청 버튼이 구현보다 앞) → 이메일(로그인 뒤 — 수집 최소화) → 카톡(F1-b 뒤). 웹푸시를 지금 안 만드는 근거 기록

---

## 사이클 규칙

1. **이 파일을 먼저 읽는다.** 맨 위 미완료 항목을 고른다. 앞 항목이 막혀 있으면 건너뛰되 왜 막혔는지 적는다
2. **한 사이클에 하나.** 반쯤 한 것을 남기지 않는다
3. **재는 것부터.** 고치기 전에 지금 값을 재고, 고친 뒤 다시 잰다. 안 쟀으면 좋아졌다고 쓰지 않는다
4. **되돌림을 잡는 시험.** 코드를 고쳤으면 시험이 있어야 하고, **지웠을 때 실패하는지 확인한다**
5. 끝내면 이 파일에 `[x]`와 잰 숫자 한 줄

## 검증 게이트

파일로 받고 종료 코드를 직접 본다. `| tail`은 파이프 종료 코드가 tail의 것이라 **실패를 감춘다.**

```
npm run typecheck > /tmp/tc.log 2>&1; echo "tc=$?"
npx vitest run    > /tmp/vt.log 2>&1; echo "vt=$?"
npm run build     > /tmp/bd.log 2>&1; echo "bd=$?"
```

**하나라도 0이 아니면 푸시하지 않는다.**

## 하지 마라

- **외부에 글 올리기** — 트위터·HN·레딧·디스코드·블로그 전부. 홍보는 사용자가 한다
- **카카오톡 실제 발송·채널 개설·사업자 정보 입력**
- 결제·구독·도메인 구입
- 운영 DB의 파괴적 변경 — `drop`·`truncate`·조건 없는 `delete`. 스키마는 **추가만**
- 파일·브랜치 삭제 (백로그가 지목한 `/zz-proto`·`/zz-svg` 제거는 예외)
- `main` 히스토리 고쳐쓰기 (`push --force`)
- Gemini 실호출을 한 사이클 10건 넘게. Codex와 직접 쓰기는 이 제한을 안 받는다

## 조심할 것

- **"없다"는 보고는 코드로 확인하기 전까지 전제로 쓰지 않는다**
- **문자열 유사도로 중복 규모를 판단하지 않는다.** 6.2배 틀린 적이 있다
- **검사기를 만들면 프롬프트에도 넣는다.** 안 그러면 아무도 안 부른다
- **표 비율은 지표가 아니다.** 2층 stack이 표로 옮겨가면 비율이 오히려 오른다. 지표는 오형 건수다
- **`block`을 남발하면 내용을 빼서 통과하는 답이 이긴다.** 지적 수가 줄기만 하면 재시도가 채택되기 때문이다
- **`rtk`가 파이프 출력을 자른다.** curl 결과는 파일로 받아서 읽는다
- React는 `#{n}`을 `#<!-- -->3`으로 낸다. 순진한 grep은 못 찾는다
- 할당량 키가 IP인데(`quota/key.ts`) 투표 키는 국내 캐리어 NAT 때문에 IP를 일부러 안 쓴다(`vote/identity.ts`). `QUOTA_ANON_DAILY`를 정상값으로 되돌리면 NAT 뒤 여러 명이 한 사람 몫을 나눈다

## 알람

사용자가 Remote Control을 켜뒀다. **사람이 정해야 하는 것이 생기면 알람을 보내되, 답을 기다리지 않고 계속 판다.** 스스로 정하고 근거를 남긴 뒤, 사용자가 뒤집을 수 있게 되돌리는 법을 적는다.

## 12시간 루프 (2026-08-09 19:35 → 08-10 07:35 KST)

사용자 지시: "할일 정리 → 리팩토링 → 12시간 자율 루프. **반드시 사람이 해야 하는 것이 있을 때만** 푸시로 부른다."

**큐 (사이클당 한 항목, 위에서부터):**

| # | 항목 | 왜 이 순서인가 |
|---|---|---|
| 1 | M3 계측 수정 | 작고, 이후 모든 매칭률 측정의 기반. 로그가 틀리면 다음 측정이 다 틀린다 |
| 2 | B6 중복 정리 | 임베딩 0.93+ 클러스터가 실측으로 드러났다(해시 충돌 4편 등). `linkEquivalent`의 첫 실사용 |
| 3 | M4 용어 사전 | **사용자 직접 요청.** 함정 넷을 계획에 적어 뒀다 |
| 4 | M2 관계 재생성 | `shortlist` 벡터 후보로 `semantic_relation` 확장. **판정은 루프 안의 Claude가 직접** — Gemini 쿼터 0 |
| 5 | B5 인프라·보안 재분류 | 시작 전에 "19개 중 9개" 전제를 다시 잰다 (B2·B4가 그랬듯 낡았을 것) |
| 5-b | M6 태그 (사용자 요청 2026-08-09 22시) | **통제 어휘**만 — `data/tags.ts`에 열거하고 그 안에서만. 자유 태그는 `동시성`/`concurrency` 분열을 만든다. 원본은 `data/` 파일, 화면은 칩. B5의 "재등재 9개"가 단일 분류의 한계 — 태그가 그 답 |
| 5-c | ~~M7 난이도~~ **완료** (데이터+화면 — 필터 라이브) |
| 6 | E1 README | 스타 목표 직결. 169줄에 그림 0 |
| 7 | E3 이슈 템플릿·good first issue | |
| 8 | E4 audit 정리 | 정확성 근거가 이 레포의 차별점 |
| 9 | C1-d 저장 항목·`/privacy` | 로그인 전 정책. 사람 없이 가능 |
| 10 | C2 약관 | 같음 |
| 11 | C7 익명 보장 확인 | |
| 12 | B7(2차) 또는 F3 | 시간 남으면 |

**사람이 필요한 것 (푸시 대상):**

- C3 OAuth — Google Cloud Console에서 클라이언트 만들어 `AUTH_GOOGLE_ID`·`AUTH_GOOGLE_SECRET`. **9·10이 끝나는 시점에 한 번 푸시**하고 기다리지 않고 다음 항목 진행
- 그 외에는 부르지 않는다. 판단이 갈리면 계획 문서에 근거를 적고 스스로 정한다

**종료:** 07:35 KST 이후 첫 사이클은 작업 대신 종료 보고를 쓰고 cron을 지운다.

---

## 기록

| 사이클 | 한 것 | 잰 것 |
|---|---|---|
| 71 | **노하우 메모리·총괄 사전집계** (12h 루프 25) | vault 메모리 1건(PGlite vector·429·prerender 시드·문장틀 오탐) · 현황: 노드 326·관계 397·등가 20·임베딩 322/326(4개는 06:43 스윕 대기) |
| 70 | **문서 정확성 스윕** (12h 루프 24) | README 46→50 · engineering 매칭 절 3층 갱신 · 해결된 한계(중복 표시) 정리 — 실측 대조로 발견 |
| 69 | **관계 3라운드** (12h 루프 23) | 옛 번호 4편에 7쌍 채택(문형 오탐 반박 통과분만) · #343~346 임베딩 공백 발견 — 06:43 스윕 첫 실전 검증 대상으로 보존 |
| 68 | **B5-b 2차** (12h 루프 22) | 세션 하이재킹(심화)·최소 권한(기초) · Codex 9건(RFC·OWASP 근거) 반영 — bearer 한정·피해=권한 등식 완화·고정공격 방어=재발급+폐기 · 인프라·보안 20→22 |
| 67 | **관계 2라운드 — 음성 실측** (12h 루프 21) | 문턱 완화(0.70~0.78) 쌍 14개 전부 none — 문장 틀 오탐 다수 · **고립 66노드는 벡터로 못 잇는다, 콘텐츠가 늘어야 관계가 생긴다** · 후보 0개 26.8%→**19.1%**(324노드 기준, 새 노드·관계 60 누적 효과) |
| 66 | **B5-b 보안 2편** (12h 루프 20) | 시크릿 관리·HSTS(#343·344) · Codex 9건 반영 · 번호·태그·난이도 시드 확인 · 인프라·보안 18→20 |
| 65 | **상세 칩** (12h 루프 19) | 태그·난이도 칩을 상세 화면에 · 데이터 흐름 전체 배선(초기·확장·API 이동 셋 다) · /q/4 실측 · 390px 넘침 0 |
| 64 | **M5 등가 접기** (12h 루프 18) | 잉여 20 접힘(목록 322→298 검산 일치) · 주소 생존 시험 · 가역(active) 시험 · 필터 SQL 한 곳 |
| 63 | **E5 라이선스** (12h 루프 17) | LICENSE·문서 4곳 일관 실측 · package.json license 필드 구멍 수리 · README 인용 문단 보강 |
| 62 | **M7-b 난이도 필터** (12h 루프 16) | /questions 난이도 줄 · 태그와 AND 조합(동시성·심화 15개 실측) · rubric이 칩 title로 · 390px 넘침 0 |
| 61 | **F3 알림 채널 + B7(2차) 정리** (12h 루프 15) | 4채널 비교·순서 결정(RSS→웹푸시→이메일→카톡) · 웹푸시는 수요 숫자 먼저 · B7(2차)는 B9 흡수 종결 |
| 60 | **C7 익명 실측** (12h 루프 14) | 화면·API 11개 무인증 200 · 익명 확장 200/쿼터0 · M3 계측 첫 운영 발동 확인 · C3 회귀 가드 기준선 기록 |
| 59 | **C2 이용약관** (12h 루프 13) | /terms 7조항 · 지킬 수 있는 것만 · audit 링크로 근거 · 바닥글 추가 · **OAuth 키 요청 푸시 발송** |
| 58 | **C1-d 저장 항목 결정** (12h 루프 12) | 받음 2 · 안 받음 6 · C3 체크리스트(끄기 실측·항상-null 시험) · /privacy 문안 준비 |
| 57 | **E4 audit README** (12h 루프 11) | 읽는 순서·핵심 숫자 시계열·반증 기록 절·방법 뼈대 — 차별점을 폴더 얼굴로 |
| 56 | **E3 이슈 템플릿** (12h 루프 10) | 템플릿 3종(사실오류·제안·버그) · blank 허용 · CONTRIBUTING 첫 기여 표 4종 · GFI 등록은 사용자 몫(근거 기록) |
| 55 | **E1 README 현행화** (12h 루프 9) | "그림 0" 전제 반증(`<img>` 7장 존재) · 태그·난이도·용어사전 반영 · 정확도 섹션 완료형 · 스크린샷 4장 갱신 |
| 54 | **M7 난이도 데이터** (12h 루프 8) | rubric 3단 명문화 · 321문항 판정(기초164·심화144·깊이14) · 0016 level · 분포 시험(급 소멸 감지) · 화면은 M7-b |
| 53 | **M6-b 태그 필터** (12h 루프 7) | /questions ?tag= 필터 · 태그 줄 17칩 · 390px 넘침 0 · 동시성 24개 실측 일치 · listRoots tags 시험 2 |
| 52 | **M6 태그 데이터** (12h 루프 6) | 통제 어휘 16 · **174문항 태깅(173/322 반영)** · `0015 tags[]` · 질문 키 매핑 한 곳 · 시험 6 · 화면은 M6-b |
| 51 | **B5 재분류** (12h 루프 5) | **인프라·보안 31→18** · 13편 원 분야로 · upsert 분야 미갱신 결함 수리(시험 포함) · "19중 9" 전제는 낡았었다(31중 13) |
| 50 | **M2 관계 재생성** (12h 루프 4) | 221쌍 Claude 2-pass 판정(Gemini 0회) → **관계 330→390 · 관계 0 노드 117→64** · none 78% · dup 2건 → 등가 20행 |
| 49 | **M4 용어 사전** (12h 루프 3) | 25용어(실측 ≥5편) · 본문 첫 등장 링크 · 함정 넷 차단 · 시험 10 · 390px 넘침 0 · `/q/4` 링크 4개 실측 |
| 48 | **B6 중복 등가 기록** (12h 루프 2) | 0.90+ 쌍 34개 판정 → 중복 17군집·잉여 18 · `qnode_equivalence` **0→18행** · `0014 canonical_id` · 사라졌던 잉여 목록 재생성 |
| 47 | **M3 계측 수정** (12h 루프 1) | `matched_via` 5종 배선 · 미기록 경로 3개 복구 · 시험 5 · 운영 33건 중 matched 5건이 "무엇인지 알 수 없던" 상태 종료 |
| 46 | **리팩토링** (4관점 리뷰) | 적용 8(cosine 통일·isMissingTable 재사용·votes 공유상수·classifyFailure 재사용·safeAfter·후보질의 병렬·스윕 배치UPDATE 200→4왕복·시험헬퍼) · 보류 4(근거 기록) |
| 45 | **임베딩을 Gemini로** (M1-b) | 후보 0개 **83(25.9%)** · 문턱 0.85/0.76(재실측 — bge 값 이월 시 중앙값 아래) · 게이트 회귀 precision 123/123 · GHA 스윕 끝단 확인 |
| 44 | **매칭 후보를 구조에서 의미로** (M1) | **후보 0개 309(96.3%) → 86(26.8%)** · 평균 0.1 → 1.8 · `npm run measure:candidates` |
| — | 계획 수립 | 백로그 30항목 |
| 0 | B1 기준선 | 277편 · 표만 136(49.1%) · flow 74 · stack 64 · state 1 · tree 1 · memory 0 · timeline 0 |
| 0 | A1 판정 (Codex + 논증 둘) | flow 73개 = 왕복 31 · 선형 22 · 그 밖 20. **ratio 폐기**(A등급 0건), 미마이그레이션 71건이 1순위로 올라옴 |
| 1 | A2-a state 재태깅 + `retag` 도구 | flow 74→73 · state 1→2. **재태깅으로 되는 건 14가 아니라 1편** — 후보 28건 중 마디가 실제 상태인 것이 하나뿐 |
| 2 | A2-b 1차 (주소공간 2 + 고장난 표 1) | stack 64→61 · **memory 0→2** · 표 136→137. 그림이 본문과 어긋나던 것(코드가 높은 주소)까지 잡힘 |
| 3 | A2-b 2차 (비교표 13편) | stack 61→48 · 표 137→150. Codex와 내가 따로 판정해 **일치한 13편만**. Codex 전체 판정: 표 31·계층 11·tree 10·flow 6 |
| 3 | A2-b 3차 (비교표 14편 더) | stack 48→34 · 표 150→164. 세 번째 분류자 합류. `#41`은 본문의 셸 파이프 때문에 도구가 자동 제외 |
| 4 | A3 1차 (오가는 flow → 기둥) | flow 72개 중 **30개가 기둥과 화살표로**. 문법·본문 무수정. 시험 10, 되돌림 양방향 확인 |
| 5 | A3 2차 (선형 사슬 → 상자와 이음줄) | flow 72개 → 기둥 30 · 사슬 22 · 그대로 20. **72%가 새 그림.** 낭독기 조사 오류(`주문다`) 브라우저에서 발견·수정 |
| 6 | A2-c 1차 (timeline 3편) + 정적 파일 동기화 결함 | **timeline 0→3.** DB만 고친 본문 4편이 `bootstrap`에 되돌려진 것을 발견·복구. `patch-data.ts`로 네 파일·두 형식 모두 처리 |
| 7 | E2 질문당 파일 하나 (사용자 요청) | 분야당 29KB 한 파일 → **질문당 파일 258개**(평균 1.4KB). 분야 목차 10개 + 최상위 목차. 깨진 상대 링크 0 |
| 8 | C1 결정 + C2 개인정보처리방침 | `/privacy` 배포. **Codex가 결정 넷을 뒤집었다** — stable v4도 Next16/React19 지원(같은 날 배포), JWT는 즉시 탈퇴와 모순, 공통 localStorage는 계정 간 유출 |
| 9 | C1-c 공용 Pool + F1 카카오 조사 | `getPool()` 추가(시험 6). **첫 시험이 되돌림을 못 잡아 다시 씀.** 카카오는 "나에게 보내기"만 무료·무검수, 나머지는 사업자등록·검수·유료 |
| 10 | C1-b 인증 런타임 spike | 셋을 실제 설치해 재 봄. **`better-auth@1.6.26` 채택.** v4는 raw pg 어댑터가 **없어서** 탈락 — 앞선 두 판정이 다 틀렸다 |
| 43 | **B9 완료 — 245편 전수 재검** | 4회차로 나눠 훑어 **59건**을 고쳤다. 오류율이 배치마다 **7%~43%**로 갈려 한 숫자로 말할 수 없다. 셋은 내가 앞 사이클에 쓴 문장이었다 |
| 42 | **B9(1차) 재검 60편** | 배치 4·7·12에서 **17편(28%)**. B8 표본 43%보다 낮지만 여전히 크다. 깨진 문장 넷이 화면에 그대로 나가고 있었다. `남은 오류 17→0` · 아직 145편이 재검 전 |
| 41 | **B4 닫음** | 지목한 부족분이 이미 메워졌다 — `프레임워크 22→30 · 언어 16→37`. 지금 짧은 곳은 아키텍처 23인데 이 문서 판단 기준이 "숫자를 늘리는 것이 목적이 아니다"라 균형을 목표로 두지 않았다 |
| 40 | **B2(3차) 빈 주제 넷** | 12개 낱말이 제목엔 0건이었지만 **절반은 이미 주제가 다뤄져** 있었다(백프레셔·CQRS·gRPC·SSE·Paxos). 진짜 빈 곳 넷을 썼고 **Codex 4/4 틀림**을 고쳤다. `317→321편 · timeline 10→11 · tree 4→5` |
| 39 | **B8 재검 17건** | 이미 고친 40편에 오류가 **17편 남아 있었다**(한 번으로 부족). 화면에 `st l//`가 그대로 나가고 있었다. 제목 5건은 그냥 못 바꿔 `renames.ts` 경로를 만들었다 — 다섯 다 번호를 지킨 채 바뀌고 **행 317 그대로** |
| 38 | **A7 — 재는 도구가 화면과 달랐다** | `measure:flow-shape`가 자기 어림짐작으로 재고 있었다. 판별기보다 먼저 만들어진 사본이다. 값은 같지만 그래서 더 위험하다. `measure:corpus`에 합치고 비율 시험을 걸었다. **새 그림을 받는 flow 69/90(77%)**, 처음 52/72(72%) |
| 37 | **A4 닫음** | `MemoryDiagram`이 이미 축 문장과 빈 공간을 그린다(390px 확인). 후보 15편 중 7편을 열어 **진짜 후보 1편**. Codex 동의, 판정 하나는 반박받아 수정. 새 코드 0줄로 두 편만 옮겼다. `stack 40→38 · memory 4→5 · flow 89→90` |
| 36 | **A6 검사기** | `도식삼킴`이 **도식이 하나도 없을 때만** 울어서, 표 옆에서 울타리가 사라진 편을 317편 검사가 **0건**으로 넘기고 있었다. 규칙 둘을 넣고 걸린 한 편을 고쳤다. `삼켜진 울타리 1→0 · stack 41→40 · 표 162→163` |
| 35 | **A2-c 닫음** | timeline 후보 다섯 중 **둘만 진짜**(`#74`·`#244`). Codex 5/5 일치. state 13편은 표본 다섯을 열어 보니 **다섯 다 재작성 대상이 아니다** — 항목을 닫았다. `timeline 8→10 · flow 90→89` |
| 34 | 질문 2편 더 + 대문에 RSS 안내 | 복합 인덱스 칸 순서·React 얕은 비교. 315→317편(공개 312). Codex 2/2 틀림 — skip scan과 얕은 비교의 층위를 빠뜨렸다 |
| 33 | 도식 우선 질문 5차 3편 | HTTP 캐시·git revert/reset·페이지 캐시. 312→315편. Codex **3/3 틀림**. 분야 이름을 목록 밖 값으로 적었는데 아무것도 안 잡아 시험을 새로 걸었다 |
| 32 | 표 비중은 고칠 결함이 아니다 | 세 번 재고 내린 결론. 견주는 질문 72편 중 60편이 표만인데 그 자리는 표가 맞다. 남은 102편도 순서를 표로 담은 것은 2편뿐. 근거를 `docs/audit/2026-08-08-table-share.md`에 남겨 네 번째 시도를 막았다 |
| 31 | 도식 우선 질문 4차 3편 | 격리 수준·비밀번호 해시·단일 스레드. 309→312편. Codex 2/3 틀림 — **SQL 표준의 기본은 SERIALIZABLE**인데 READ COMMITTED로 적었다 |
| 30 | 표가 기본값이 되지 않게 프롬프트 수정 | 표만 있는 편 162 중 **견주는 질문이 아닌 것이 102편**. "비교형이 많아서"라는 내 첫 진단은 틀렸다(비교형 72편). 두 프롬프트에 규칙을 넣고 표식 시험으로 지켰다 |
| 29 | 도식 우선 질문 3차 6편 | ARP·NAT·더티 체킹·리플로우·분기 예측·타임아웃 예산. 303→309편. Codex 판정 **5/6 틀림**. 그중 하나는 내 논리 자체가 틀렸다 — 단계마다 독립 5초면 뒤가 "시작하자마자 끊기"지 않고 시간이 더해진다 |
| 28 | 도식 우선 질문 2차 6편 | MESI·우선순위 역전·파일 디스크립터·N+1·경로 MTU·백프레셔. 297→303편. Codex 판정 **3/6 틀림**(1차 5/6보다 나아졌다) |
| 27 | 도식 우선 질문 1차 6편 | state·timeline·memory·tree가 각각 2~3편뿐이라 그 형태가 본체인 주제로 썼다. 291→297편. **내가 쓴 6편 중 5편이 틀렸다** — 넣기 전 Codex 검증이 없었으면 그대로 나갔다 |
| 26 | RSS 구독 · CONTRIBUTING · README 개편 | 매일 올라오는데 돌아올 이유가 기억뿐이었다. 카톡은 사업자 등록이 앞이라 못 열고 RSS는 지금 열린다. 대문에 잰 오류율을 본문으로 올렸다 |
| 25 | **말뭉치 245편 전수 사실 검증** | 13배치·Codex. 지적 110편 중 105건 적용. CAS가 뜻이 뒤집혀 있었고, 없는 용어(`Senatorial Atomic`)를 지어낸 자리와 문장이 깨진 자리 여덟 곳을 찾았다. 교정 도구가 역슬래시를 안 막아 조용히 빗나가던 것도 잡았다 |
| 24 | 추천을 분야별로 돌아가며 | 배포하고 폰으로 보니 처음 온 사람에게 다섯 중 넷이 같은 분야였다. 화면을 안 봤으면 못 잡았다 |
| 23 | **내가 판 자리**(`/me`) — 잔디·누적·추천 | 로그인 없이 붙였다. 무엇을 팠는지는 여정이 이미 알고 있었고 언제 팠는지만 없었다. 시험 33개(격자 경계·상한·연속·추천 규칙). 사실 교정 28건 동봉 |
| 22 | **고쳤는데 사이트는 그대로였다** | 시드가 질문 해시로 id를 만드는데 물어봐서 생긴 행은 임의 uuid라 안 만난다. 291행이 317행이 됐고 화면은 옛 행을 내보냈다. 꼬리질문은 `do nothing`이라 44개 교정이 통째로 안 나갔다. 배포 뒤 폰 화면을 열어 보고서야 알았다 |
| 21 | 말뭉치 전체가 규칙을 통과한다 | **25편 → 0편/291.** 낡은 사실 7건(TLS 키 교환·상호 인증·교착 회피·다익스트라·캐시 무효화), 35자 넘는 단추 44개, 경어체 단추 15개, 겹친 조사 10곳, 사라진 도식 1개, 707자 문단. 도구 3개와 지키는 시험을 남겼다 |
| 20 | **글 26편이 버전 관리 밖에 있었다** | 교정 13건이 전부 "파일에서 못 찾음". 도구가 아니라 글이 DB에만 살았다. `data/on-demand-nodes.ts`로 꺼내고 부팅 시드에 넣었다. 오픈소스인데 남이 고쳐 보낼 파일이 없던 상태 |
| 19 | B7 2차 — **모델이 쓴 것은 더 나쁘다** | 258편에서 분야별 층화 20편 표본. Codex 판정 **틀림 15·부정확 2·맞음 3 = 사실 오류 75%**. 내가 쓴 것(64%)보다 **11%p 나쁘다**. 9문장 수정. 수정하다 문단이 189자로 붇고 도식이 4번째로 밀려 관문이 막았다 — 검사기가 잡아냈다 |
| 11 | T1 착수 (우선순위 변경) | 표만 167편 뽑아 분류자 셋에 넘김. 열 수 **3열 127편(76.5%)**, 첫 칸 **`기준` 117편(70.5%)** 실측 |
| 18 | B7 사실 검증 — **내가 쓴 11편 중 9편이 틀렸다** | Codex 판정 틀림 7·부정확 2. 전부 고쳐 재배포. 근거 없는 수치 하나 제거, 정확도 때문에 state 도식 하나 포기 |
| 17 | A8 시안 제거 + B7 착수 | `/zz-proto`·`/zz-svg` 등 13파일 제거(프로덕션 공개 주소였다). 오늘 쓴 11편 사실 검증을 Codex에 넘김 |
| 16 | B3 조사 + B2 3차 | 조사 11개 중 **8개는 이미 있었다**(키워드 오탐 주의). 288→291편 · **state 2→3**. 실제 0건이던 것만 씀 |
| 15 | B2 2차 (기둥 4편 더) | **283→288편** · flow 74→78. OAuth·CORS·재처리·CDN 전부 제목에 0건이던 것. 번호 278~282로 **작게 나온다** |
| 14 | 번호 새는 것 차단 | 시퀀스 **29763 → 277**. 부팅마다 283개씩 태우던 것. 내 시험이 두 번 헛돌아 두 번 고쳤다 |
| 13 | B2 1차 (빈 분야 4편 새로 씀) | **279→283편.** 전부 도식 딸림 — 기둥 2·사슬 1·층 1. 390px 확인. 빈 분야 측정이 `테스트`/`시험` 낱말 차이로 틀렸던 것도 발견 |
| 12 | T1~T3 표 판정 종료 | 분류자 셋 교차. 표유지 Codex 85% · A 98.8% · B 97.6%. 실제로 바꾼 것 **4편**(표만 167→162). 그중 하나는 **표가 사실을 뒤집고 있던 것**(JRE⊃JVM). **표로는 그림이 안 는다 → B2로 전환** |
