/**
 * 난이도 3단. **판정 기준을 여기 명문화한다.**
 *
 * LeetCode의 Easy/Medium/Hard는 정답률 실측이 근거다. 이 서비스는 아직
 * 트래픽이 없어 그 근거가 없고, 근거 없이 "상중하 매겨줘"는 조건부를
 * 법칙처럼 단정하는 실패(B7)의 반복이다. 그래서 **기준(rubric)을 먼저
 * 적고, 판정은 기준을 가리킨다.** 개별 판정의 근거는 이 파일이다.
 *
 * 보정 계획 (지금 값은 출발점이다):
 * 1. `prerequisite` 관계가 쌓이면 선행 사슬 깊이로 검증 — "이걸 알려면
 *    먼저 알아야 하는 것이 n개"는 실측 가능한 구조적 난이도다
 * 2. 트래픽이 생기면 행동(확장 깊이·재방문)으로 검증
 */
export type Level = '기초' | '심화' | '깊이'

export const LEVELS: Array<{ name: Level; rubric: string }> = [
  {
    name: '기초',
    rubric:
      '한 개념의 정의·존재 이유를 묻는다. 신입 면접 단골이고 CS 과목 수업을 들었으면 답할 수 있다. 예: "스택과 큐의 차이", "인덱스를 쓰는 이유"',
  },
  {
    name: '심화',
    rubric:
      '개념 사이의 비교·트레이드오프·적용 판단을 묻는다. 답에 "상황에 따라"가 정당하게 들어간다. 예: "낙관적 락과 비관적 락 중 언제 무엇을", "격리 수준을 올리면 무엇을 잃는가"',
  },
  {
    name: '깊이',
    rubric:
      '특정 구현의 내부·진단·표준의 예외를 묻는다. 실무에서 그 부분을 직접 판 사람이 답할 수 있다. 예: "Hash Anti Join의 내부 동작", "TIME_WAIT 누적으로 인한 포트 고갈 진단"',
  },
]

export const LEVEL_NAMES = new Set<string>(LEVELS.map((l) => l.name))
