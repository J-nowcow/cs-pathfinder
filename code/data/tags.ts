/**
 * 태그 통제 어휘. **여기 없는 태그는 달 수 없다.**
 *
 * 자유 태그를 허용하면 `동시성`/`concurrency`/`멀티스레딩`이 전부 따로
 * 생긴다 — 낱말이 갈라지면 필터가 갈라지고, 필터가 갈라지면 태그가
 * 없는 것과 같다. 그래서 목록을 닫아 두고, 태그를 더하고 싶으면
 * **이 파일을 고치는 PR**이 먼저다.
 *
 * 분야(primary_category)와 다른 축이다. 분야는 하나만 갖는 소속이고
 * 태그는 여럿 갖는 주제다. `인프라 · 보안`이 미분류 수용소가 됐던 이유가
 * 단일 분류의 한계였다 — "뮤텍스는 운영체제이면서 동시성"을 분야는 못
 * 담고 태그는 담는다.
 *
 * 이름은 화면에 그대로 나간다. 한국어, 짧게.
 */
export type Tag = {
  /** 화면과 데이터 양쪽에서 쓰는 이름 그대로 */
  name: string
  /** 이 태그가 다루는 범위 한 줄. 태깅하는 사람이 이것만 보고 판단한다 */
  scope: string
}

/*
 * 어휘 확장(2026-08-10). 16개 → 18개. 근거는 `node-tags.ts`가 남긴 무태그
 * 사유다 — 2차 태깅에서 "어휘에 자리가 없어" 못 붙인 묶음을 훑고 **둘만**
 * 열었다. 후보로 검토했다가 접은 자리를 같이 적어 둔다. 닫아 둔 이유가
 * 안 적혀 있으면 다음 사람이 같은 검토를 되풀이한다.
 *
 * `보안 공격` — 공격 기법이 논점인 6문항이 `인증·인가`와 `암호화`에 얹혀
 *   있거나 무태그였다. 둘 다 **방어 수단**의 이름이라 공격을 담으면 뜻이
 *   넓어진다(`XSS로 훔칠 수 있는 데이터`가 `인증·인가`로 잡혀 있던 게 그
 *   증상이다). 6문항이 인프라·보안/프론트엔드/네트워크/언어·런타임/모바일
 *   다섯 분야에 흩어져 있어 분야로는 못 모은다 — 태그가 할 일이 맞다.
 *
 * `언어 의미론` — 12문항. 프로토타입 사슬·TDZ·this 바인딩·제네릭 소거·
 *   구조적 타이핑처럼 "언어가 이 코드를 어떻게 읽는가"가 논점인 것들이다.
 *   `메모리`로 잡아 두면 어긋난다 — `대입 뒤 한쪽을 바꿀 때`의 본문은
 *   값/참조 구분이 스택·힙 배치와 **같지 않다**고 못 박는다.
 *   `언어 · 런타임` 분야(42문항)의 일부(12)라 분야를 되풀이하지 않는다.
 *   scope 끝에 "메모리 배치·실행 최적화 제외"를 붙인 것은 GC·JIT·지연 평가가
 *   "런타임이 코드를 해석한다"는 말에 딸려 들어오기 때문이다.
 *
 * 접은 자리 둘.
 *   **화면 규약**(박스 모델·플렉스/그리드·포커스 이동·시맨틱 마크업) — 후보
 *   4문항이 전부 `프론트엔드` 분야다. 태그로 거른 결과가 분야로 거른 것과
 *   겹치면 정보가 0이다.
 *   **값 표현**(문자 인코딩·부동소수점) — 후보가 3문항뿐인데 그마저 인코딩
 *   1 + 소수점 2로 갈린다. 게다가 `고정 소수점과 부동 소수점은 무엇으로
 *   구분하는가`의 본문은 오차가 아니라 표현 방식 비교라 "오차" 축으로도
 *   안 묶인다. 무태그를 줄이려고 어휘를 여는 것은 순서가 거꾸로다 —
 *   **커버리지는 어휘 확장의 근거가 아니다.** 엔디언·정수 오버플로·2의
 *   보수처럼 같은 축의 문항이 쌓이면 그때 다시 본다.
 *
 * `설계 원칙`의 scope도 고쳤다. 코드 한정인지 스키마까지인지 안 갈려서
 * 검수 때 흔들렸던 자리다. **스키마와 외부 계약까지 포함**으로 확정했다 —
 * 확장 전 36건 중 정규화·스키마 설계·리소스 API 성숙도·메시지 형식 진화 등이
 * 이미 코드 밖 구조에 붙어 있었고, 좁히면 정규화·스키마 설계가 무태그가
 * 된다. 다만 "기존에 그렇게 붙어 있다"는 것만으로 범위를 넓히면 순환
 * 논증이라, 판단 기준을 **변경의 영향을 가두는가**로 새로 적었다. 목적이
 * 성능·확장성·장애 대응인 구조 선택은 명시적으로 뺐다(그쪽 태그가 따로 있다).
 */
export const TAGS: Tag[] = [
  { name: '동시성', scope: '스레드·락·경쟁 상태·데드락·원자성 — 동시에 실행될 때 생기는 문제' },
  { name: '비동기', scope: '이벤트 루프·콜백·프로미스·논블로킹 — 기다리지 않는 실행 흐름' },
  { name: '메모리', scope: '힙·스택·GC·누수·가상 메모리 — 메모리가 어떻게 쓰이고 회수되는가' },
  { name: '캐싱', scope: '캐시 계층·TTL·무효화·CDN — 한 번 만든 것을 다시 쓰는 전략' },
  { name: '인증·인가', scope: 'JWT·세션·쿠키·OAuth·권한 — 누구인지와 무엇을 할 수 있는지' },
  { name: '암호화', scope: 'TLS·해시·대칭/비대칭 키·서명 — 내용을 지키는 수단' },
  { name: '보안 공격', scope: '인젝션·XSS·CSRF·세션 탈취·ReDoS — 남이 보낸 입력이 공격이 되는 원리와 그 길을 막는 법' },
  { name: '프로토콜', scope: 'TCP·UDP·HTTP·DNS의 동작 규약 자체' },
  { name: '트랜잭션', scope: '격리 수준·ACID·커밋/롤백 — 전부 되거나 전부 안 되거나' },
  { name: '인덱스·질의', scope: '인덱스·실행 계획·옵티마이저·조인 — 질의가 빨라지는 원리' },
  { name: '확장성', scope: '샤딩·로드 밸런싱·수평 확장 — 커져도 버티는 구조' },
  { name: '장애 대응', scope: '재시도·타임아웃·서킷 브레이커·복구 — 실패를 견디는 설계' },
  { name: '성능', scope: '병목·지연·처리량 측정과 개선 — 왜 느리고 어떻게 빨라지는가' },
  { name: '자료구조', scope: '해시·트리·그래프·큐·스택의 구조와 선택 기준' },
  { name: '언어 의미론', scope: '스코프·바인딩·대입·타입 동일성·제네릭 소거 — 언어가 이름과 값을 읽는 규칙(메모리 배치·실행 최적화 제외)' },
  { name: '빌드·배포', scope: 'CI/CD·빌드·배포 전략·롤백 — 코드가 서비스가 되는 길' },
  { name: '컨테이너', scope: '도커·이미지·쿠버네티스 — 격리된 실행 단위의 운영' },
  { name: '설계 원칙', scope: '책임·의존·무결성 — 코드·스키마·외부 계약에서 변경의 영향을 가두는 규칙(성능·확장성 목적은 제외)' },
]

export const TAG_NAMES = new Set(TAGS.map((t) => t.name))
