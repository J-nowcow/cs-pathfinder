/**
 * 태그 통제 어휘. **여기 없는 태그는 달 수 없다.**
 *
 * 자유 태그를 허용하면 `동시성`/`concurrency`/`멀티스레딩`이 전부 따로
 * 생긴다 — 낱말이 갈라지면 필터가 갈라지고, 필터가 갈라지면 태그가
 * 없는 것과 같다. 그래서 목록을 닫아 두고, 태그를 더하고 싶으면
 * **이 파일을 고치는 PR**이 먼저다.
 *
 * 분야(primary_category)와 다른 축이다. 분야는 하나만 갖는 소속이고
 * 태그는 여럿 갖는 주제다. `인프라 · 보안`이 미분류 수용소가 됐던 이유가
 * 단일 분류의 한계였다 — "뮤텍스는 운영체제이면서 동시성"을 분야는 못
 * 담고 태그는 담는다.
 *
 * 이름은 화면에 그대로 나간다. 한국어, 짧게.
 */
export type Tag = {
  /** 화면과 데이터 양쪽에서 쓰는 이름 그대로 */
  name: string
  /** 이 태그가 다루는 범위 한 줄. 태깅하는 사람이 이것만 보고 판단한다 */
  scope: string
}

export const TAGS: Tag[] = [
  { name: '동시성', scope: '스레드·락·경쟁 상태·데드락·원자성 — 동시에 실행될 때 생기는 문제' },
  { name: '비동기', scope: '이벤트 루프·콜백·프로미스·논블로킹 — 기다리지 않는 실행 흐름' },
  { name: '메모리', scope: '힙·스택·GC·누수·가상 메모리 — 메모리가 어떻게 쓰이고 회수되는가' },
  { name: '캐싱', scope: '캐시 계층·TTL·무효화·CDN — 한 번 만든 것을 다시 쓰는 전략' },
  { name: '인증·인가', scope: 'JWT·세션·쿠키·OAuth·권한 — 누구인지와 무엇을 할 수 있는지' },
  { name: '암호화', scope: 'TLS·해시·대칭/비대칭 키·서명 — 내용을 지키는 수단' },
  { name: '프로토콜', scope: 'TCP·UDP·HTTP·DNS의 동작 규약 자체' },
  { name: '트랜잭션', scope: '격리 수준·ACID·커밋/롤백 — 전부 되거나 전부 안 되거나' },
  { name: '인덱스·질의', scope: '인덱스·실행 계획·옵티마이저·조인 — 질의가 빨라지는 원리' },
  { name: '확장성', scope: '샤딩·로드 밸런싱·수평 확장 — 커져도 버티는 구조' },
  { name: '장애 대응', scope: '재시도·타임아웃·서킷 브레이커·복구 — 실패를 견디는 설계' },
  { name: '성능', scope: '병목·지연·처리량 측정과 개선 — 왜 느리고 어떻게 빨라지는가' },
  { name: '자료구조', scope: '해시·트리·그래프·큐·스택의 구조와 선택 기준' },
  { name: '빌드·배포', scope: 'CI/CD·빌드·배포 전략·롤백 — 코드가 서비스가 되는 길' },
  { name: '컨테이너', scope: '도커·이미지·쿠버네티스 — 격리된 실행 단위의 운영' },
  { name: '설계 원칙', scope: 'SOLID·리팩토링·계층 분리 — 코드를 오래 살게 하는 규칙' },
]

export const TAG_NAMES = new Set(TAGS.map((t) => t.name))
