/**
 * 아직 심지 않은 손으로 쓴 노드.
 *
 * **`bootstrap.ts`가 이 파일을 안 읽는다.** 일부러 그렇게 뒀다. `AUTHORED_NODES`에
 * 넣으면 다음 부팅에 운영 DB로 들어가는데, 그건 되돌리기 어려운 쓰기라 사람이
 * 정할 일이다. 심을 때는 `bootstrap.ts`의 배열에 한 줄 더하면 된다.
 *
 * 왜 이것들을 쓰는가. 도식 어휘가 여섯인데 **운영 코퍼스 276편에서 timeline이
 * 0개, memory가 0개다.** 렌더러도 파서도 프롬프트도 다 있는데 그것을 쓰는 글이
 * 하나도 없다. 모델은 자기가 안 본 형태를 잘 안 고른다.
 *
 * 여기 둘은 **그 형태가 아니면 손해가 큰 질문**만 골랐다. 억지로 도식을 붙일
 * 수 있는 질문은 뺐다.
 *
 * - 데드락은 두 스레드가 **같은 시각에** 서로의 락을 붙들고 있는 것이 전부다.
 *   순서 도식으로 그리면 그 겹침이 시간 순서로 눕혀져 사라진다
 * - 스택과 힙은 **마주 자란다**는 것이 요점이다. 계층으로 그리면 그 사실이
 *   통째로 없어진다
 *
 * **셋째로 "논블로킹은 블로킹과 무엇이 다른가"를 썼다가 뺐다.** 규칙 검사는
 * 통과했는데 Codex 대조에서 개념을 틀린 것이 드러났다. 논블로킹은 **호출이
 * 즉시 돌아오는 성질**(데이터가 없으면 `EAGAIN`)이고, 걸어 두고 나중에 결과를
 * 받는 것은 **비동기 완료 모델**이다. 그 둘을 섞어 썼고 타임라인도 사실상
 * 비동기 I/O를 그려 놓고 논블로킹이라 불렀다. 더 나쁜 것은 꼬리질문 첫째가
 * "비동기와 논블로킹은 같은 말인가?"였다는 점이다 — 본문이 자기 꼬리질문의
 * 정답과 어긋나 있었다.
 *
 * 형태 검사는 이런 것을 못 잡는다. 손으로 썼다고 정확한 것도 아니다.
 *
 * 규칙은 기준선과 같다. 답 먼저 → 도식 → 근거. 문단 150자 이내.
 * 질문 40자 이내. 꼬리질문 5개·35자 이내·평어체.
 */
import type { ExampleNode } from './example-nodes'

export const PENDING_NODES: ExampleNode[] = [
  {
    identityScope: 'generic',
    category: '운영체제',
    question: '데드락은 어떻게 만들어지는가?',
    body: [
      '둘이 서로가 쥔 것을 기다릴 때 생긴다. 각자 하나씩 잡은 뒤 상대 것을 달라고 하면 둘 다 영원히 멈춘다.',
      '',
      ':::timeline',
      '스레드 A | 락 1을 잡는다 |  | 락 2를 달라고 한다 | 기다린다',
      '스레드 B |  | 락 2를 잡는다 | 락 1을 달라고 한다 | 기다린다',
      ':::',
      '',
      '**세 번째 칸에서 이미 끝났다.** 둘 다 상대가 놓아야 나아갈 수 있는데 놓을 수 있는 쪽이 없다. 시간이 지나도 상태가 안 바뀐다.',
      '',
      '네 조건이 동시에 서야 이렇게 된다. 한 번에 하나만 쓸 수 있고, 쥔 채로 더 요구하고, 뺏을 수 없고, 기다림이 원을 그리는 것이다.',
      '',
      '여러 락을 함께 잡을 때 흔히 쓰는 예방책은 **전역 락 순서를 정하는 것**이다. 모두가 락 1을 먼저 잡기로 하면 원이 안 생긴다. 위 그림에서 B가 락 1부터 잡으면 둘째 칸에서 그냥 기다렸다가 순서대로 지나간다.',
    ].join('\n'),
    suggestions: [
      '락 순서는 어떻게 정하는가?',
      '데드락 예방과 회피는 무엇이 다른가?',
      '타임아웃으로 푸는 방법의 대가는?',
      'DB는 데드락을 어떻게 감지하는가?',
      '라이브락은 데드락과 무엇이 다른가?',
    ],
  },
  {
    identityScope: 'generic',
    category: '언어 · 런타임',
    question: '스택과 힙은 무엇이 다른가?',
    body: [
      '수명을 누가 정하느냐가 다르다. 스택 프레임은 함수가 끝나면 같이 사라지고, 힙에 얻은 것은 놓거나 수집기가 걷어 갈 때까지 남는다.',
      '',
      ':::memory',
      '스택 | 함수 호출마다 쌓인다. 지역 변수와 돌아갈 주소 | 아래로',
      '빈 공간 | 둘이 서로를 향해 자란다',
      '힙 | 실행 중에 얻어 쓰는 것. 놓을 때까지 남는다 | 위로',
      '정적 데이터 | 전역 변수와 정적 변수',
      '코드 | 프로그램 명령. 대개 읽기 전용이다',
      ':::',
      '',
      '**마주 보고 자라는 것이 요점이다.** 둘 사이의 빈 공간을 나눠 쓰므로 한쪽이 많이 쓰면 다른 쪽이 쓸 것이 준다.',
      '',
      '위 그림은 전통적인 유닉스 배치다. 요즘은 힙을 `mmap`으로도 얻고 공유 라이브러리가 가운데 끼어들어 실제 배치는 더 복잡하다. 그래도 스택이 높은 쪽에서 아래로 자란다는 뼈대는 같다.',
      '',
      '스택 할당은 대개 포인터 하나를 옮기는 것이 전부라 싸다. 힙은 쓸 만한 자리를 찾아야 하고, 어떻게 찾고 언제 걷어 가는지는 할당기와 수집기마다 다르다.',
      '',
      '그래서 깊은 재귀가 스택을 넘치게 한다. 한도가 정해진 영역인데 호출마다 한 칸씩 쌓이기 때문이다. 꼬리 호출을 최적화하는 언어에서는 이 자리가 안 늘기도 한다.',
    ].join('\n'),
    suggestions: [
      '스택 프레임에는 무엇이 들어가는가?',
      '힙 조각화는 왜 문제가 되는가?',
      '재귀 말고 스택을 넘치게 하는 것은?',
      '값 타입과 참조 타입은 어디에 놓이는가?',
      '스레드마다 스택을 따로 갖는 이유는?',
    ],
  },
]
