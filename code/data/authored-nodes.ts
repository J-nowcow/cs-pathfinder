/**
 * 손으로 쓴 보충 노드.
 *
 * `example-nodes.ts`(30개)와 **따로 둔다.** 그쪽은 생성 규칙의 기준선이고
 * 시험이 그것을 상대로 걸려 있다. 여기 것을 섞으면 기준선이 흔들린다.
 * `generated-nodes.ts`와도 다르다 — 그쪽은 모델이 쓴 것이다.
 *
 * 왜 손으로 쓰는가. 실측에서 **분야가 통째로 빠진 것**이 나왔다. 질문 251개에
 * 제목 기준으로 컨테이너·쿠버네티스·합의 알고리즘·SQL 인젝션이 **한 건도**
 * 없었다. 본문에 스쳐 지나간 것이 몇 개 있을 뿐이다. 개별 누락이 아니라
 * 분야 부재라 주제어 시드를 돌려서는 잘 안 메워진다.
 *
 * 새로 만든 도식(`:::state`·`:::tree`)을 실제로 쓰는 첫 콘텐츠이기도 하다.
 * 렌더러와 프롬프트만 있고 그것을 쓰는 글이 없으면 만든 값이 안 드러난다.
 *
 * 규칙은 기준선과 같다. 답 먼저 → 도식 → 근거. 문단 150자 이내.
 * 질문 40자 이내. 꼬리질문 5개·35자 이내·평어체.
 */
import type { ExampleNode } from './example-nodes'

export const AUTHORED_NODES: ExampleNode[] = [
  {
    identityScope: 'generic',
    category: '인프라 · 보안',
    question: '컨테이너는 가상 머신과 무엇이 다른가?',
    body: [
      '커널을 공유한다. 가상 머신은 게스트 운영체제를 통째로 올리지만 컨테이너는 호스트 커널 위에서 프로세스로 돈다.',
      '',
      ':::stack',
      '컨테이너 | 앱과 라이브러리만. 커널은 호스트 것을 쓴다',
      '컨테이너 런타임 | 격리와 자원 제한을 건다',
      '호스트 커널 | 하나를 모두가 나눠 쓴다',
      ':::',
      '',
      '격리의 뼈대는 커널 기능 둘이다. 네임스페이스가 보이는 것을 가르고 cgroup이 쓸 수 있는 양을 정한다. seccomp이나 SELinux 같은 것이 그 위에 더 얹힌다.',
      '',
      '그래서 뜨는 것이 빠르고 이미지가 작다. 대신 커널을 나눠 쓰므로 탈출로 이어지는 커널 취약점이 나오면 격리가 뚫린다. 다른 커널이 필요한 워크로드도 못 올린다.',
      '',
      '가상 머신과 고르는 문제가 아니다. 실무에서는 가상 머신 위에 컨테이너를 올린다.',
    ].join('\n'),
    suggestions: [
      '네임스페이스는 무엇을 가르는가?',
      'cgroup은 어떤 자원을 제한하는가?',
      '컨테이너 이미지 계층은 왜 나누는가?',
      '루트 권한 컨테이너는 왜 위험한가?',
      '컨테이너가 죽으면 데이터는 어떻게 되는가?',
    ],
  },
  {
    identityScope: 'generic',
    category: '아키텍처 · 분산시스템',
    question: 'Raft는 왜 과반수를 요구하는가?',
    body: [
      '두 무리가 동시에 결정을 내리는 것을 막으려면 겹치는 표가 있어야 한다. 과반수끼리는 반드시 하나 이상 겹친다.',
      '',
      '노드 다섯에서 과반수는 셋이다. 어떤 두 무리를 잡아도 최소 하나를 공유한다. 겹침만으로는 부족하고, 한 임기에 한 번만 투표한다는 규칙이 함께 있어야 결정이 갈리지 않는다.',
      '',
      ':::state',
      '팔로워 -> 후보: 리더에게서 소식이 끊긴다',
      '후보 -> 리더: 과반수의 표를 받았다',
      '후보 -> 팔로워: 임기가 같거나 높은 리더를 만났다',
      '리더 -> 팔로워: 더 높은 임기를 보았다',
      ':::',
      '',
      '임기 번호가 오래된 리더를 밀어낸다. 그물이 갈렸다가 붙었을 때 옛 리더가 돌아와도 자기 임기가 낮은 것을 보고 물러난다.',
      '',
      '대가는 지연이다. 쓰기마다 과반수의 응답을 기다리므로 **과반수를 채우는 마지막 응답**만큼 걸린다. 느린 소수는 안 기다리지만 복제본을 늘린다고 쓰기가 빨라지지는 않는다.',
    ].join('\n'),
    suggestions: [
      '임기 번호는 무엇을 막는가?',
      '노드가 짝수면 무엇이 나빠지는가?',
      '리더 없이 합의하는 방식도 있는가?',
      '읽기도 과반수를 거쳐야 하는가?',
      '그물이 갈리면 어느 쪽이 멈추는가?',
    ],
  },
  {
    identityScope: 'generic',
    category: '인프라 · 보안',
    question: 'SQL 인젝션은 무엇으로 막는가?',
    body: [
      '질의문과 값을 갈라 두면 막힌다. 값을 문자열로 이어 붙이는 순간 값이 문법이 될 수 있다.',
      '',
      '| 기준 | 이어 붙이기 | 값 바인딩 |',
      '| --- | --- | --- |',
      '| 값의 신분 | 질의문의 일부가 된다 | 끝까지 값이다 |',
      '| 막는 방법 | 위험한 글자를 지운다 | 문법 자리에 못 들어간다 |',
      '| 새는 곳 | 인코딩·숫자 자리 | 틀을 동적으로 만들 때 |',
      '',
      '이스케이프로 막으려는 시도가 흔한데 새는 자리가 계속 나온다. 문자 인코딩이 다르거나 숫자 자리에는 따옴표가 없어서다.',
      '',
      '틀을 동적으로 만들어야 할 때가 남는다. 정렬 기준이나 표 이름은 값 자리가 될 수 없다. 그때는 허용 목록으로 고른다.',
      '',
      '권한도 함께 줄인다. 앱 계정에 `DROP`이 없으면 뚫려도 잃는 것이 적다.',
    ].join('\n'),
    suggestions: [
      '허용 목록은 어디에 두어야 하는가?',
      'ORM을 쓰면 저절로 안전한가?',
      '저장 프로시저는 인젝션을 막는가?',
      '오류 메시지는 왜 감춰야 하는가?',
      '읽기 전용 계정은 무엇까지 막는가?',
    ],
  },
  {
    identityScope: 'generic',
    category: '자료구조 · 알고리즘',
    question: 'B-tree는 왜 디스크에 유리한가?',
    body: [
      '한 번 읽을 때 많이 가져오기 때문이다. 디스크는 블록 단위로 읽으므로 노드 하나를 블록만큼 크게 잡으면 읽기 횟수가 준다.',
      '',
      ':::tree',
      '[30 60] | 루트 하나에 열쇠가 여럿이다',
      '  [10 20] | 30보다 작은 것',
      '  [40 50] | 30과 60 사이',
      '  [70 80] | 60보다 큰 것',
      ':::',
      '',
      '균형 잡힌 이진 트리도 백만 건이면 깊이가 스물이다. 갈래를 백으로 늘리면 셋이면 닿는다. 캐시가 비어 있다면 깊이만큼 디스크를 읽는다.',
      '',
      '모든 잎이 같은 깊이에 있다. 넣고 지울 때 노드를 쪼개거나 합쳐서 균형을 지키므로 최악도 `O(log n)`을 지킨다.',
      '',
      '위쪽 노드는 대개 버퍼 캐시에 남아 있어 실제 읽기는 깊이보다 적다. 메모리만 쓰는 색인에서는 이 이점이 줄어든다.',
    ].join('\n'),
    suggestions: [
      '노드가 가득 차면 어떻게 쪼개는가?',
      'B+tree는 무엇이 다른가?',
      '색인이 있어도 왜 안 타는가?',
      '해시 색인은 어디서 더 나은가?',
      '범위 검색은 왜 B-tree가 유리한가?',
    ],
  },
  {
    identityScope: 'generic',
    category: '프레임워크',
    question: '단위 시험과 통합 시험은 무엇으로 가르는가?',
    body: [
      '무엇을 함께 확인하는지로 가른다. 단위 시험은 작은 동작 하나를 따로 보고 통합 시험은 여러 조각이 맞물리는 자리를 본다.',
      '',
      '| 기준 | 단위 | 통합 |',
      '| --- | --- | --- |',
      '| 보는 범위 | 동작 하나 | 조각들이 만나는 곳 |',
      '| 둘레 | 자주 가짜로 바꾼다 | 대개 실제 것을 쓴다 |',
      '| 대개의 속도 | 빠르다 | 느리다 |',
      '',
      '가짜로 바꾸는 것은 경향이지 정의가 아니다. 단위 시험이 실제 협력 객체를 써도 되고, 통합 시험이 바깥 시스템만 가짜로 둬도 된다.',
      '',
      '비율보다 중요한 것은 **되돌렸을 때 빨간불이 뜨는가**다. 고친 자리를 원래대로 돌려도 통과하면 그 시험은 적어도 이 회귀를 못 잡는다.',
    ].join('\n'),
    suggestions: [
      '가짜 객체는 어디까지 써야 하는가?',
      '시험이 느려지면 무엇부터 보는가?',
      '깨지기 쉬운 시험은 왜 생기는가?',
      '커버리지 숫자는 무엇을 못 말하는가?',
      '시험을 먼저 쓰면 무엇이 달라지는가?',
    ],
  },

  /*
   * 아래 넷은 **도식이 제 값을 하는 것만** 골랐다.
   *
   * 표 167편을 판정해 보니 정말 구조인 것이 넷뿐이었다. 있는 글을 옮겨서는
   * 그림이 안 는다는 뜻이라, 처음부터 그림이 답인 질문을 쓴다.
   *
   * 빈 분야를 다시 셌더니 앞선 측정이 부분적으로 틀려 있었다 -- `테스트`로
   * 찾아서 위의 `단위 시험과 통합 시험`을 못 잡았다. 실제로 비어 있는 것은
   * 쿠버네티스·도커 이미지·CI/CD·로드밸런싱이다.
   */
  {
    identityScope: 'generic',
    category: '인프라 · 보안',
    question: '죽은 파드는 누가 다시 만드는가?',
    body: [
      '상위 컨트롤러가 만든다. Deployment나 ReplicaSet이 원하는 개수와 지금을 견주고 모자란 만큼 채운다.',
      '',
      ':::flow',
      '사용자 -> API 서버: 파드를 셋 두라고 적는다',
      'API 서버 -> 컨트롤러: 하나가 사라졌다고 알린다',
      '컨트롤러 -> API 서버: 하나 더 만들라고 적는다',
      ':::',
      '',
      '요점은 **명령이 아니라 상태를 적는다**는 것이다. "파드를 만들어라"가 아니라 "셋이어야 한다"를 남기고, 그 차이를 메우는 일은 컨트롤러가 되풀이한다.',
      '',
      '컨트롤러는 매번 묻지 않는다. API 서버가 바뀔 때마다 알려주고, 컨트롤러는 그것을 모아 둔 사본을 기준으로 맞춘다.',
      '',
      '예외가 둘 있다. 상위 컨트롤러 없이 만든 파드는 사라지면 그만이다. 컨테이너만 죽은 경우에는 kubelet이 같은 파드 안에서 다시 띄운다.',
    ].join('\n'),
    suggestions: [
      '원하는 상태는 어디에 저장되는가?',
      '컨트롤러는 얼마나 자주 확인하는가?',
      '파드와 컨테이너는 무엇이 다른가?',
      '노드가 죽으면 얼마 만에 알아채는가?',
      '되돌리기는 어떻게 이루어지는가?',
    ],
  },
  {
    identityScope: 'generic',
    category: '인프라 · 보안',
    question: '이미지를 고쳐도 조금만 받는 이유는?',
    body: [
      '층마다 고유한 지문이 있어 이미 가진 층은 안 받기 때문이다. 바뀐 층만 새로 내려온다.',
      '',
      ':::stack',
      '내 앱 코드 | 커밋마다 바뀐다. 대개 이 층만 다시 받는다',
      '의존성 설치 | 잠금 파일이 바뀔 때만',
      '런타임 | 버전을 올릴 때만',
      '베이스 이미지 | 여러 이미지가 함께 쓴다',
      ':::',
      '',
      '만들 때는 다른 이야기다. 한 명령에서 캐시를 못 쓰면 **그 명령과 뒤따르는 명령이 전부 다시 실행된다.** 그래서 자주 바뀌는 것을 뒤에 둔다.',
      '',
      '앱 코드를 먼저 복사하면 그 뒤 설치 명령이 매번 다시 돈다. 순서 하나로 빌드 시간이 갈린다.',
      '',
      '지운 파일도 아래층에 남는다. 비밀값을 넣었다가 다음 층에서 지워도 이미지 안에는 그대로 있다.',
    ].join('\n'),
    suggestions: [
      '층은 무엇을 기준으로 나뉘는가?',
      '앱 코드를 언제 복사해야 하는가?',
      '이미지에서 비밀값을 어떻게 빼는가?',
      '다단계 빌드는 무엇을 줄이는가?',
      '이미지 크기는 무엇으로 줄이는가?',
    ],
  },
  {
    identityScope: 'generic',
    category: '인프라 · 보안',
    question: '빌드는 통과했는데 배포에서 깨지는 이유는?',
    body: [
      '시험한 것과 배포한 것이 다르기 때문이다. 단계마다 다시 만들면 같은 소스라도 다른 결과물이 나갈 수 있다.',
      '',
      ':::flow',
      '커밋 -> 빌드: 결과물을 한 번만 만든다',
      '빌드 -> 시험: 방금 만든 그것으로 돌린다',
      '시험 -> 배포: 통과한 그것을 그대로 올린다',
      ':::',
      '',
      '**결과물을 한 번만 만들어 끝까지 들고 간다.** 배포 단계에서 다시 빌드하면 그사이 바뀐 의존성이 섞여 들어가고, 시험이 확인한 것은 배포된 것이 아니게 된다.',
      '',
      '설정은 결과물 밖에 둔다. 환경마다 다른 값을 안에 넣으면 환경 수만큼 결과물이 생긴다.',
      '',
      '그래도 남는 차이가 있다. 시험 환경과 운영 환경의 자원·권한·이웃 서비스는 같게 만들기 어렵다.',
    ].join('\n'),
    suggestions: [
      '결과물에 무엇을 넣지 말아야 하는가?',
      '환경별 설정은 어디에 두는가?',
      '같은 소스가 다른 결과를 내는 이유는?',
      '배포를 되돌리려면 무엇이 필요한가?',
      '스테이징은 무엇을 못 잡는가?',
    ],
  },
  {
    identityScope: 'generic',
    category: '네트워크',
    question: '죽은 서버로 요청이 안 가는 이유는?',
    body: [
      '로드밸런서가 주기적으로 물어보고 대답이 없으면 목록에서 뺀다. 요청이 올 때 확인하는 것이 아니라 미리 확인해 둔다.',
      '',
      ':::flow',
      '로드밸런서 -> 서버: 살아 있냐고 주기적으로 묻는다',
      '서버 -> 로드밸런서: 대답이 온다. 목록에 남긴다',
      '로드밸런서 -> 서버: 대답이 없다. 연속 실패를 센다',
      '서버 -> 로드밸런서: 다시 대답한다. 목록에 되돌린다',
      ':::',
      '',
      '**몇 번 실패해야 빼는지는 제품과 설정에 달렸다.** 한 번의 명시적 오류로 바로 빼는 것도 있고 연속 실패를 세는 것도 있다.',
      '',
      '빼도 **이미 열린 연결은 이어질 수 있다.** 새로 배정하는 것을 멈추는 것이지 쓰던 것을 끊는 것이 아니다.',
      '',
      '확인하는 주소가 중요하다. 웹 서버만 살아 있고 데이터베이스가 끊긴 상태에서 정적 파일을 돌려주면 죽은 서버가 계속 목록에 남는다.',
    ].join('\n'),
    suggestions: [
      '확인 주소는 무엇을 검사해야 하는가?',
      '실패 몇 번에 빼는 것이 맞는가?',
      '뺀 서버를 언제 되돌리는가?',
      '연결이 남아 있으면 어떻게 되는가?',
      '전부 죽으면 어떻게 동작하는가?',
    ],
  },

  /*
   * 2차. 제목에 한 번도 안 나오던 주제 중 **도식이 답인 것**만 골랐다.
   * 넷 다 주고받음이라 기둥과 화살표가 된다 -- 표로 눕히면 누가 무엇을
   * 쥐고 있는지가 사라지는 자리다.
   */
  {
    identityScope: 'generic',
    category: '인프라 · 보안',
    question: '비밀번호를 앱에 안 주고 로그인하는 방법은?',
    body: [
      '인가 서버에서만 비밀번호를 넣고 앱은 코드를 받아 토큰으로 바꾼다. 앱은 비밀번호를 한 번도 보지 않는다.',
      '',
      ':::flow',
      '사용자 -> 앱 서버: 로그인을 누른다',
      '앱 서버 -> 사용자: 인가 서버로 보낸다',
      '사용자 -> 인가 서버: 아이디와 비밀번호를 넣는다',
      '인가 서버 -> 앱 서버: 인가 코드를 준다. 토큰이 아니다',
      '앱 서버 -> 인가 서버: 코드와 앱 비밀키를 주고 토큰을 받는다',
      ':::',
      '',
      '**코드와 토큰을 나누는 이유**가 요점이다. 코드는 주소창을 거쳐 오므로 주소 기록이나 referrer에 남을 수 있다. 그래서 코드만으로는 토큰이 안 된다.',
      '',
      '토큰을 주소창으로 바로 받는 방식도 있었지만 지금은 권하지 않는다. 주소는 이력과 로그에 남는다.',
      '',
      '무엇으로 묶는지는 앱 종류에 따라 다르다. 서버가 있는 앱은 비밀키로 자기를 밝히고, 브라우저나 모바일 앱처럼 비밀키를 숨길 데가 없으면 PKCE로 묶는다. **지금 권고는 어느 쪽이든 PKCE를 쓰는 것이다.**',
    ].join('\n'),
    suggestions: [
      'PKCE는 무엇을 막는가?',
      '상태 값은 왜 함께 보내는가?',
      '갱신 토큰은 어디에 두어야 하는가?',
      '토큰을 훔치면 무엇까지 할 수 있는가?',
      '로그아웃은 무엇을 지워야 하는가?',
    ],
  },
  {
    identityScope: 'generic',
    category: '프론트엔드',
    question: '브라우저가 요청을 미리 한 번 더 보내는 이유는?',
    body: [
      '보내도 되는 요청인지 서버에 먼저 묻기 때문이다. 답을 받은 뒤에야 진짜 요청을 보낸다.',
      '',
      ':::flow',
      '브라우저 -> 서버: 이 요청을 보내도 되냐고 묻는다',
      '서버 -> 브라우저: 허용하는 출처와 메서드를 답한다',
      '브라우저 -> 서버: 맞으면 진짜 요청을 보낸다',
      '서버 -> 브라우저: 응답에도 허용 출처를 붙인다',
      ':::',
      '',
      '**막는 쪽은 브라우저다.** 단순한 요청은 서버까지 가서 처리되고, 브라우저가 응답만 못 읽게 막는다. 그래서 서버 로그에는 남는다.',
      '',
      '먼저 묻는 경우는 다르다. **사전 요청이 거절되면 진짜 요청은 아예 안 간다.** `GET`·`HEAD`·`POST`이면서 허용된 헤더와 본문 종류만 쓸 때 이 단계를 건너뛴다.',
      '',
      '`application/json`으로 보내거나 `Authorization`을 붙이면 먼저 묻는다. 자격 증명까지 보내려면 허용 출처에 `*`를 못 쓰고 정확한 주소를 적어야 한다.',
    ].join('\n'),
    suggestions: [
      '단순 요청의 기준은 무엇인가?',
      '쿠키를 함께 보내려면 무엇이 필요한가?',
      '사전 요청 결과는 얼마나 재사용되는가?',
      '프록시로 우회하면 무엇이 달라지는가?',
      '서버는 왜 처리까지 하는가?',
    ],
  },
  {
    identityScope: 'generic',
    category: '아키텍처 · 분산시스템',
    question: '처리에 실패한 메시지는 어디로 가는가?',
    body: [
      '큐와 소비자 설정에 달렸다. 다시 주는 것도, 몇 번 만에 따로 둔 큐로 옮기는 것도 저절로 되는 것이 아니라 정해 두는 것이다.',
      '',
      ':::flow',
      '큐 -> 소비자: 메시지를 준다',
      '소비자 -> 큐: 처리하지 못했다고 알린다',
      '큐 -> 소비자: 잠시 기다렸다 다시 준다',
      '큐 -> 실패 큐: 횟수를 넘기면 옮긴다',
      ':::',
      '',
      '**순서를 지키는 자리에서는 옮기지 않으면 줄이 막힌다.** 맨 앞이 계속 실패하면 뒤엣것이 영영 처리되지 않는다. 다만 옮기는 순간 원래 순서는 깨진다.',
      '',
      '다시 주는 간격을 점점 늘리는 것도 정책이다. 같은 간격으로 몰아치면 이미 힘든 곳을 더 때린다.',
      '',
      '옮긴 뒤가 진짜 일이다. 실패 큐에 쌓인 것을 아무도 안 보면 조용히 잃은 것과 같다. 쌓이면 알리도록 걸어 둔다.',
    ].join('\n'),
    suggestions: [
      '몇 번까지 다시 주는 것이 맞는가?',
      '실패 큐에 쌓인 것을 어떻게 되돌리는가?',
      '같은 메시지가 두 번 와도 괜찮은가?',
      '순서를 지켜야 하면 무엇이 달라지는가?',
      '독이 든 메시지는 어떻게 알아내는가?',
    ],
  },
  {
    identityScope: 'generic',
    category: '네트워크',
    question: '먼 나라 사용자도 빠르게 받는 방법은?',
    body: [
      '가까운 서버에 사본을 두고 거기서 준다. 사본이 아직 쓸 만하면 원본까지 안 간다.',
      '',
      ':::flow',
      '사용자 -> 엣지: 파일을 달라고 한다',
      '엣지 -> 사용자: 가지고 있으면 바로 준다',
      '엣지 -> 원본: 없으면 받아온다',
      '원본 -> 엣지: 파일과 보관 기간을 준다',
      ':::',
      '',
      '**거리가 곧 시간이다.** 멀수록 왕복 하나가 길어지고, 연결을 맺는 데만 그 왕복이 여러 번 든다. 가까운 곳에서 끝나면 그것이 통째로 사라진다.',
      '',
      '어려운 것은 지우는 일이다. 파일을 바꿔도 사본은 보관 기간이 끝날 때까지 옛것을 준다. 그래서 파일 이름에 내용 해시를 넣어 **다른 파일로 만든다.**',
      '',
      '사본이 있어도 원본에 가는 때가 있다. 기간이 지나 다시 확인할 때, 캐시를 건너뛰라고 적혀 있을 때, 애초에 캐시할 수 없는 응답일 때다.',
    ].join('\n'),
    suggestions: [
      '보관 기간은 무엇을 기준으로 정하는가?',
      '파일 이름에 해시를 왜 넣는가?',
      '사본을 강제로 지울 수 있는가?',
      '개인화된 응답도 사본을 둘 수 있는가?',
      '첫 요청이 느린 것을 줄일 수 있는가?',
    ],
  },

  /*
   * 3차. 면접 후기 조사(`interview-research`)가 짚은 것 중 **코드로 없음을
   * 확인한 것**만 썼다. 조사가 짚은 열한 개 가운데 여덟은 이미 있었다 --
   * 키워드로 세면 `프록시`가 스프링 AOP를 잡는 식으로 오탐이 나서, 제목을
   * 직접 훑어 갈랐다.
   *
   * 실제로 0건이던 것: `스케일`·`부하`·`트래픽`, 그리고 리버스 프록시.
   */
  {
    identityScope: 'generic',
    category: '아키텍처 · 분산시스템',
    question: '부하가 늘면 서버를 어떻게 늘리는가?',
    body: [
      '상태를 보고 판단하는 것이 아니라 **고리를 돈다.** 주기마다 지표를 읽어 필요한 대수를 다시 셈하고 그 값을 반영한다.',
      '',
      ':::flow',
      '지표 -> 오토스케일러: 주기마다 읽는다',
      '오토스케일러 -> 목표 대수: 지금 지표로 다시 셈한다',
      '목표 대수 -> 워크로드: 완충 규칙을 통과하면 반영한다',
      ':::',
      '',
      '**바로 반응하면 오르내린다.** 잠깐 튄 값에 늘렸다가 곧 줄이기를 되풀이한다. 그래서 작은 차이는 무시하고, 정해 둔 시간만큼 지켜본 뒤에 움직인다.',
      '',
      '쿠버네티스 기본값은 15초마다 셈하고, 줄일 때만 300초를 지켜본다. 늘릴 때는 0초다. **다만 이건 설정값이지 모든 오토스케일러의 법칙이 아니다.**',
      '',
      '늘리는 데 걸리는 시간이 진짜 제약이다. 서버가 뜨고 받을 준비를 마칠 때까지 몇 분이면 그 몇 분은 이미 밀린다. 줄일 때 연결을 마저 끝내는 일도 오토스케일러가 아니라 다른 장치가 한다.',
    ].join('\n'),
    suggestions: [
      '무엇을 지표로 삼아야 하는가?',
      '완충 시간은 무엇을 막는가?',
      '늘리는 시간을 줄일 수 있는가?',
      '줄일 때 연결은 누가 마무리하는가?',
      '미리 늘려 두는 것이 나은 때는?',
    ],
  },
  {
    identityScope: 'generic',
    category: '네트워크',
    question: '서버 앞에 하나를 더 두는 이유는?',
    body: [
      '여러 서버를 하나처럼 보이게 하고, 서버마다 하던 일을 한곳으로 모으기 위해서다.',
      '',
      ':::flow',
      '브라우저 -> 리버스 프록시: 요청을 보낸다',
      '리버스 프록시 -> 앱 서버: 살아 있는 곳을 골라 넘긴다',
      '앱 서버 -> 리버스 프록시: 응답을 돌려준다',
      '리버스 프록시 -> 브라우저: 압축하고 헤더를 붙여 준다',
      ':::',
      '',
      '**브라우저는 앱 서버를 모른다.** 그래서 서버를 늘리거나 옮겨도 바깥 주소가 그대로다. 인증서와 압축, 정적 파일도 여기서 한 번에 처리한다.',
      '',
      '방향이 반대인 것이 포워드 프록시다. 그쪽은 나가는 쪽에 서서 클라이언트를 감추고, 리버스는 들어오는 쪽에 서서 서버를 감춘다.',
      '',
      '대신 여기가 죽으면 전부 죽는다. 그래서 이 자리도 여러 대를 두거나 앞단을 따로 둔다.',
    ].join('\n'),
    suggestions: [
      '포워드 프록시는 무엇을 감추는가?',
      '인증서를 여기서 끝내면 무엇이 좋은가?',
      '실제 접속자 주소는 어떻게 아는가?',
      '이 자리가 죽으면 어떻게 하는가?',
      '정적 파일을 여기서 주는 이유는?',
    ],
  },
  {
    identityScope: 'generic',
    category: '인프라 · 보안',
    question: '이미지와 컨테이너는 무엇이 다른가?',
    body: [
      '이미지는 읽기만 되는 층들이고, 컨테이너는 그 위에 쓰기 층 하나를 얹어 돌린 것이다.',
      '',
      ':::stack',
      '쓰기 층 | 이 컨테이너만의 것. 지우면 함께 사라진다',
      '앱 층 | 여기부터 이미지다. 읽기만 된다',
      '런타임 층 | 여러 컨테이너가 같은 것을 함께 쓴다',
      '베이스 층 | 디스크에 한 벌만 있다',
      ':::',
      '',
      '**같은 이미지로 여럿 띄우면 읽기 전용 층을 함께 쓴다.** 처음 느는 것은 각자의 얇은 쓰기 층뿐이다. 그 뒤로는 각자 쓴 파일과 로그만큼 는다.',
      '',
      '컨테이너를 지우면 쓰기 층이 사라진다. 바깥에 붙인 저장소에 둔 것은 남는다. 남겨야 하는 것을 거기 두는 이유다.',
      '',
      '"컨테이너를 고쳐 저장한다"는 방식은 권하지 않는다. 무엇이 들었는지 아무도 모르는 이미지가 된다. 고칠 것은 만드는 파일 쪽이다.',
    ].join('\n'),
    suggestions: [
      '컨테이너 안 데이터는 어디에 두는가?',
      '같은 이미지로 여럿 띄우면 무엇이 공유되는가?',
      '쓰기 층은 언제 사라지는가?',
      '이미지를 손으로 고치면 무엇이 나쁜가?',
      '이미지 이름표는 무엇을 가리키는가?',
    ],
  },
  {
    identityScope: 'tcp',
    category: '네트워크',
    question: 'TCP 연결은 어떤 상태를 지나 닫히는가?',
    body: [
      '양쪽이 각자 닫아야 해서 한 번에 안 끝난다. 먼저 닫는 쪽과 받는 쪽이 서로 다른 상태를 지난다.',
      '',
      ':::state',
      '연결됨 -> FIN_WAIT_1: 먼저 닫는 쪽이 FIN을 보낸다',
      'FIN_WAIT_1 -> FIN_WAIT_2: 상대가 그 FIN에 ACK만 보낸다',
      'FIN_WAIT_2 -> TIME_WAIT: 상대도 다 보내고 FIN을 보낸다',
      'TIME_WAIT -> 닫힘: 2MSL을 기다린 뒤 없앤다',
      ':::',
      '',
      '받는 쪽은 다른 길을 간다. FIN을 받으면 CLOSE_WAIT로 가서 **아직 보낼 것을 마저 보낸다.** 다 보내고 자기 FIN을 보내면 LAST_ACK가 되고, 마지막 ACK를 받으면 닫힌다.',
      '',
      'CLOSE_WAIT가 쌓여 있으면 상대 탓이 아니다. 내 쪽 코드가 소켓을 안 닫고 있다는 뜻이다.',
      '',
      'TIME_WAIT가 필요한 이유는 마지막 ACK가 유실될 수 있어서다. 그러면 상대가 FIN을 다시 보내는데, 그때 응답할 쪽이 남아 있어야 한다. 그 사이 늦게 도착한 옛 패킷이 새 연결로 섞이는 것도 막는다.',
      '',
      '면접에서는 TIME_WAIT가 **먼저 닫은 쪽에** 생긴다는 점을 짚는다. 서버에 쌓였다면 서버가 먼저 끊고 있다는 신호다. 양쪽이 동시에 닫으면 둘 다 생긴다.',
    ].join('\n'),
    suggestions: [
      'CLOSE_WAIT가 쌓이면 어디를 보는가?',
      'TIME_WAIT는 왜 2MSL인가?',
      '양쪽이 동시에 닫으면 어떻게 되는가?',
      'RST로 끊으면 무엇이 달라지는가?',
      '먼저 끊는 쪽을 바꾸면 무엇이 나아지는가?',
    ],
  },
  {
    identityScope: 'tcp',
    category: '네트워크',
    question: 'TCP는 보낼 양을 어떻게 늘렸다 줄이는가?',
    body: [
      '작게 시작해 두 배씩 늘리다가, 문턱을 넘으면 하나씩만 늘린다. 손실이 보이면 줄인다.',
      '',
      ':::timeline',
      '보낼 양 | 1 | 2 | 4 | 8 | 9',
      '무슨 일 | 느린 시작 | 두 배 | 두 배 | 문턱 | 하나씩',
      ':::',
      '',
      '이름과 달리 **느린 시작은 느리지 않다.** 시작이 작을 뿐 늘어나는 속도는 왕복마다 두 배다. 문턱(ssthresh)을 넘으면 왕복마다 하나씩만 늘리는 혼잡 회피로 바꾼다.',
      '',
      '손실을 어떻게 알았느냐에 따라 대응이 다르다. 같은 ACK가 세 번 겹쳐 오면 길이 아주 막힌 것은 아니라고 보고 절반으로 줄인다.',
      '',
      '타임아웃이면 다르다. 아무 소식이 없다는 뜻이라 보낼 양을 1로 되돌리고 느린 시작부터 다시 한다.',
      '',
      '면접에서는 **이것이 라우터가 아니라 보내는 쪽이 하는 일**이라는 점을 짚는다. 네트워크는 대개 혼잡하다고 알려 주지 않아서 손실로 짐작한다. ECN을 켠 길에서는 라우터가 손실 없이 알려 주기도 한다.',
    ].join('\n'),
    suggestions: [
      '중복 ACK 세 번이 왜 기준인가?',
      '타임아웃과 중복 ACK는 왜 다르게 다루는가?',
      '흐름 제어와는 무엇이 다른가?',
      'BBR은 무엇을 다르게 보는가?',
      '무선에서 손실 기반 판단이 왜 불리한가?',
    ],
  },
  {
    identityScope: 'process',
    category: '운영체제',
    question: '좀비 프로세스는 왜 남는가?',
    body: [
      '자식이 어떻게 끝났는지를 부모가 물어볼 때까지 커널이 그 자리를 남겨 두기 때문이다.',
      '',
      ':::state',
      '실행 -> 좀비: 자식이 끝났는데 부모가 아직 안 거둔다',
      '좀비 -> 사라짐: 부모가 wait으로 종료 상태를 가져간다',
      '실행 -> 고아: 부모가 먼저 죽는다',
      '고아 -> 사라짐: init이 입양해 대신 거둔다',
      ':::',
      '',
      '좀비는 쓰던 메모리를 이미 내놓았다. 남는 것은 프로세스 표의 한 줄과 종료 상태, 그리고 그 번호다. 그래서 하나둘로는 표가 안 난다.',
      '',
      '문제는 개수다. 프로세스 표에는 한도가 있어서 좀비가 계속 쌓이면 **새 프로세스를 못 만든다.** 자식을 많이 띄우는 서버에서 실제로 겪는 자리다.',
      '',
      '고아는 좀비가 아니다. 부모가 먼저 죽은 것뿐이고 init이 입양해 거둬 준다. 오히려 부모가 살아서 방치할 때 남는다.',
      '',
      '면접에서는 **부모를 죽이면 좀비가 사라진다**는 점을 짚는다. 좀비 자체는 이미 죽어서 신호를 받지 않는다.',
    ].join('\n'),
    suggestions: [
      '좀비에 kill을 보내면 어떻게 되는가?',
      'SIGCHLD를 무시하면 무엇이 달라지는가?',
      '프로세스 표 한도는 어디서 정하는가?',
      '컨테이너의 1번 프로세스는 무엇을 해야 하는가?',
      '고아와 좀비는 무엇이 다른가?',
    ],
  },
  {
    identityScope: 'transaction',
    category: '데이터베이스',
    question: '갑자기 꺼져도 커밋한 것이 남는 이유는?',
    body: [
      '데이터를 고치기 전에 **무엇을 고칠지부터 로그에 적고 그 로그를 먼저 디스크에 내리기** 때문이다.',
      '',
      ':::timeline',
      '로그 | 바꿀 내용을 적는다 | 디스크에 내린다 | | 재시작 때 다시 읽는다',
      '데이터 | 메모리에서 고친다 | | 나중에 내려간다 | 로그대로 맞춘다',
      ':::',
      '',
      '커밋이 끝났다고 답하는 시점은 데이터 페이지가 아니라 **로그가 디스크에 닿은 시점**이다. 데이터는 아직 메모리에만 있어도 된다.',
      '',
      '이렇게 하는 이유는 속도다. 여기저기 흩어진 페이지를 매번 내리면 디스크 헤드가 뛰어다닌다. 로그는 뒤에 이어 붙이기만 하므로 한 줄로 쓴다.',
      '',
      '재시작하면 로그를 읽어 커밋된 것을 다시 적용한다. 커밋 안 된 것을 어떻게 치울지는 구현마다 다르다. 되돌리는 쪽도 있고 그냥 안 보이게 두는 쪽도 있다.',
      '',
      '체크포인트는 그 시작점을 앞으로 당긴다. 없으면 처음부터 다 읽어야 해서 복구가 길어진다.',
    ].join('\n'),
    suggestions: [
      '체크포인트는 무엇을 기록하는가?',
      '로그도 유실되면 어떻게 되는가?',
      'fsync를 건너뛰면 무엇이 깨지는가?',
      '되돌리기용 정보는 어디에 두는가?',
      '로그가 계속 쌓이면 어떻게 지우는가?',
    ],
  },
  {
    identityScope: 'java',
    category: '언어 · 런타임',
    question: '같은 문자열 리터럴이 같은 객체인 이유는?',
    body: [
      '리터럴은 풀에 한 벌만 두고 그 자리를 함께 가리키기 때문이다.',
      '',
      ':::memory',
      '스택 | 변수는 자리만 가리킨다',
      '힙 | new로 만든 것은 여기 따로 생긴다 | 위로',
      '문자열 풀 | 리터럴은 여기 한 벌만 있고 함께 쓴다',
      ':::',
      '',
      '`String a = "cs"`와 `String b = "cs"`는 같은 자리를 가리킨다. 그래서 `a == b`가 참이다.',
      '',
      '`new String("cs")`는 다르다. 풀에 있든 없든 힙에 새로 하나를 만든다. 그래서 `==`로 견주면 거짓이고 `equals`로 견줘야 참이다.',
      '',
      '`intern()`을 부르면 풀에 같은 내용이 있는지 보고 있으면 그 자리를, 없으면 넣고 그 자리를 준다.',
      '',
      '면접에서는 **`==`가 내용이 아니라 자리를 견준다**는 점이 본론이다. 문자열이 특별해서가 아니라 참조 비교라서 그렇다.',
    ].join('\n'),
    suggestions: [
      '`==`와 `equals`는 무엇을 견주는가?',
      'intern을 남발하면 무엇이 나쁜가?',
      '문자열이 불변인 이유는?',
      '더하기로 문자열을 잇는 것이 왜 느린가?',
      '풀은 어느 메모리 영역에 있는가?',
    ],
  },
  {
    identityScope: 'datastructure',
    category: '자료구조 · 알고리즘',
    question: 'B-Tree가 디스크에 맞는 이유는?',
    body: [
      '노드 하나에 키를 많이 담아 트리 높이를 낮추기 때문이다. 디스크는 한 번 갈 때마다 값을 치른다.',
      '',
      ':::tree',
      '뿌리 노드 | 키 여러 개가 한 블록에 들어간다',
      '  자식 A | 여기도 키 여러 개',
      '  자식 B | 여기도 키 여러 개',
      '  자식 C | 여기도 키 여러 개',
      ':::',
      '',
      '이진 탐색 트리는 노드마다 키가 하나다. 균형이 잡혀 있어도 백만 개면 높이가 스물쯤 되고 **내려가는 만큼 디스크를 읽는다.** 한쪽으로 기울면 더 나빠진다.',
      '',
      'B-Tree는 한 노드를 디스크 블록 크기에 맞춘다. 한 번 읽어 온 블록 안에서 여러 번 비교하므로 내려가는 횟수가 준다. 같은 백만 개라도 서너 번이면 닿는다.',
      '',
      '메모리 안에서만 논다면 이 이점이 거의 없다. 읽어 오는 값이 싸기 때문이다. 그래서 인메모리 구조로는 다른 것을 쓴다.',
      '',
      '면접에서는 **비교 횟수가 아니라 읽기 횟수를 줄이는 설계**라는 점을 짚는다. 총 비교 횟수는 오히려 늘 수도 있다.',
    ].join('\n'),
    suggestions: [
      'B+Tree는 무엇을 더 하는가?',
      '노드 크기는 무엇에 맞추는가?',
      '범위 검색에 왜 유리한가?',
      '삽입이 넘치면 어떻게 되는가?',
      '인메모리에서는 무엇을 쓰는가?',
    ],
  },
  {
    identityScope: 'cpu',
    category: '운영체제',
    question: '코어마다 캐시가 따로인데 값이 어긋나지 않는 이유는?',
    body: [
      '캐시 줄마다 상태를 달아 두고 남이 같은 자리를 건드리면 내 것의 상태를 바꾸기 때문이다. 서로 엿듣기도 하고, 코어가 많으면 관리하는 자리를 따로 두고 거기서 알리기도 한다.',
      '',
      ':::state',
      'Exclusive -> Shared: 다른 코어가 같은 줄을 읽어 간다',
      'Shared -> Modified: 내가 그 줄에 쓴다. 남의 것은 무효가 된다',
      'Modified -> Shared: 남이 읽으려 해서 내려 주고 함께 쓴다',
      'Shared -> Invalid: 남이 그 줄에 썼다',
      ':::',
      '',
      '핵심은 **쓸 때 나만 갖는다**는 것이다. Modified나 Exclusive면 나 혼자이므로 마음대로 써도 된다. Shared였다면 남의 것을 먼저 무효로 만들고 써야 한다.',
      '',
      '그래서 여러 코어가 같은 줄을 번갈아 쓰면 값이 계속 오간다. 서로 다른 변수인데 한 줄에 붙어 있어도 그렇다.',
      '',
      '이것을 거짓 공유라 부른다. 변수 사이를 벌려 다른 줄에 놓으면 사라진다.',
      '',
      '면접에서는 **이 일이 하드웨어가 알아서 한다**는 점을 짚는다. 코드로 맞추는 것이 아니라 값이 어긋나지 않게 이미 지켜진다. 프로그래머가 다루는 것은 순서와 가시성 쪽이다.',
    ].join('\n'),
    suggestions: [
      '거짓 공유는 어떻게 알아채는가?',
      '메모리 장벽은 무엇을 막는가?',
      'volatile은 무엇을 보장하는가?',
      '캐시 줄 크기는 왜 64바이트인가?',
      '원자적 연산은 어떻게 이뤄지는가?',
    ],
  },
  {
    identityScope: 'process',
    category: '운영체제',
    question: '급한 일이 안 급한 일에 밀리는 경우가 있는가?',
    body: [
      '있다. 급한 쪽이 기다리는 잠금을 안 급한 쪽이 쥐고 있는데, 그 안 급한 쪽마저 다른 일에 밀릴 때다.',
      '',
      ':::timeline',
      '급한 일 | | 잠금을 기다린다 | 계속 기다린다 | 받는다',
      '중간 일 | | | 끼어들어 돈다 | 끝난다',
      '느린 일 | 잠금을 쥔다 | | 밀려서 못 끝낸다 | 놓는다',
      ':::',
      '',
      '급한 일은 느린 일이 잠금을 놓기만 기다린다. 그런데 느린 일은 중간 일에 밀려 돌지 못한다. **결국 급한 일이 중간 일에 밀린 셈**이 된다.',
      '',
      '이름이 역전인 이유가 거기 있다. 우선순위대로면 일어날 수 없는 순서가 만들어진다.',
      '',
      '푸는 방법은 잠금을 쥔 쪽의 우선순위를 잠깐 올리는 것이다. 급한 쪽만큼 올려 주면 중간 일이 끼어들지 못한다.',
      '',
      '화성 탐사선 패스파인더가 이것으로 계속 재시작했다. 지구에서 우선순위 상속을 켜는 설정을 올려 고쳤다.',
    ].join('\n'),
    suggestions: [
      '우선순위 상속은 어떻게 동작하는가?',
      '우선순위 천장 기법은 무엇이 다른가?',
      '기아 상태와는 무엇이 다른가?',
      '실시간 시스템에서 왜 치명적인가?',
      '잠금 대신 무엇을 쓸 수 있는가?',
    ],
  },
  {
    identityScope: 'process',
    category: '운영체제',
    question: '파일 디스크립터 3은 무엇을 가리키는가?',
    body: [
      '숫자 하나가 파일을 직접 가리키지 않는다. 표를 몇 겹 지나야 닿는다. 일반 파일이면 아래처럼 세 겹이고, 소켓이나 파이프면 마지막 겹이 다르다.',
      '',
      ':::tree',
      '프로세스별 표 | 3번 칸에 화살표가 있다',
      '  열린 파일 표 | 어디까지 읽었는지와 열 때 준 옵션',
      '    아이노드 | 실제 파일. 크기와 블록 위치',
      ':::',
      '',
      '가운데 표가 있는 이유는 **같은 파일을 두 번 열면 읽는 위치가 따로**여야 하기 때문이다. 각자 자기 자리를 기억한다.',
      '',
      '`dup`으로 복제하면 다르다. 새 번호가 생기지만 가운데 표의 같은 칸을 가리킨다. 그래서 한쪽에서 읽으면 다른 쪽 위치도 함께 움직인다.',
      '',
      '`2>&1`이 되는 것도 이 구조 덕이다. 2번 칸이 1번 칸과 같은 곳을 보게 만드는 일이다.',
      '',
      '면접에서는 **0, 1, 2가 특별하지 않다**는 점을 짚는다. 그냥 먼저 열려 있을 뿐이고 닫으면 다음 열기가 그 번호를 받는다.',
    ].join('\n'),
    suggestions: [
      '`2>&1`은 무엇을 바꾸는가?',
      'dup과 다시 여는 것은 무엇이 다른가?',
      '파일을 닫지 않으면 무엇이 새는가?',
      '한도를 넘으면 어떤 오류가 나는가?',
      '소켓도 같은 번호 공간을 쓰는가?',
    ],
  },
  {
    identityScope: 'orm',
    category: '프레임워크',
    question: '목록 하나 읽었는데 쿼리가 백 번 나가는 이유는?',
    body: [
      '목록을 한 번 읽고, 그 안의 항목마다 연결된 것을 또 읽기 때문이다.',
      '',
      ':::flow',
      '앱 -> DB: 글 100건을 달라',
      'DB -> 앱: 글 100건',
      '앱 -> DB: 1번 글의 글쓴이를 달라',
      '앱 -> DB: 2번 글의 글쓴이를 달라. 이렇게 100번',
      ':::',
      '',
      '한 번에 다 가져온 줄 알았는데 아니다. 연결된 것은 실제로 쓸 때 그때 읽어 오도록 미뤄 둔 것이다.',
      '',
      '그래서 목록을 화면에 뿌리는 순간 100번이 더 나간다. 데이터가 적을 때는 표가 안 나고 늘어난 뒤에야 느려진다.',
      '',
      '푸는 길은 미리 함께 읽는 것이다. 조인으로 한 번에 가져오거나, 필요한 것을 모아 `in`으로 한 번에 묻는다.',
      '',
      '조인으로 가져오면 행이 부푸는 것을 조심해야 한다. 하나에 여럿이 달린 관계를 여러 개 겹쳐 조인하면 곱셈이 된다.',
    ].join('\n'),
    suggestions: [
      '페치 조인은 무엇을 한 번에 가져오는가?',
      '배치 크기를 주면 무엇이 달라지는가?',
      '지연 로딩을 끄면 무엇이 나빠지는가?',
      '조인으로 행이 부푸는 것은 어떻게 막는가?',
      '쿼리가 몇 번 나갔는지 어떻게 보는가?',
    ],
  },
  {
    identityScope: 'network',
    category: '네트워크',
    question: '보낼 데이터가 길에서 감당할 크기보다 크면 어떻게 되는가?',
    body: [
      '중간에 잘리거나, 아예 못 가고 되돌아온다. IPv4는 자르지 말라는 표시가 붙었는지로 갈린다. IPv6는 라우터가 아예 자르지 않는다.',
      '',
      ':::flow',
      '보내는 쪽 -> 라우터: 1500바이트 조각',
      '라우터 -> 보내는 쪽: 이 길은 1400까지다. 못 보낸다고 알린다',
      '보내는 쪽 -> 라우터: 1400으로 줄여 다시 보낸다',
      ':::',
      '',
      '자르지 말라는 표시가 붙어 있으면 라우터는 자르지 않고 버린다. 대신 감당할 크기를 알려 준다. 그 답을 보고 보내는 쪽이 줄인다.',
      '',
      'TCP는 아예 처음부터 줄여 보낸다. 연결을 맺을 때 서로 받을 수 있는 크기를 알려 주고 그 안에서 끊어 보낸다.',
      '',
      '중간에서 자르는 방식은 손해가 크다. 조각 하나만 잃어도 전체를 다시 보내야 하고, 받는 쪽은 다 모일 때까지 메모리를 붙들고 있어야 한다.',
      '',
      '그래서 알려 주는 신호가 막히면 조용히 멈춘다. 악수는 됐는데 큰 데이터만 안 가는 증상이 그것이다.',
    ].join('\n'),
    suggestions: [
      'MSS와 MTU는 무엇이 다른가?',
      '조각 하나를 잃으면 어떻게 되는가?',
      '경로 탐색이 막히면 어떤 증상이 나오는가?',
      'IPv6는 왜 중간에서 안 자르는가?',
      '터널을 쓰면 왜 더 줄어드는가?',
    ],
  },
  {
    identityScope: 'distributed',
    category: '아키텍처 · 분산시스템',
    question: '보내는 쪽이 받는 쪽보다 빠르면 무엇이 터지는가?',
    body: [
      '사이에 낀 큐가 먼저 터진다. 메모리가 차거나, 차기 전에 응답이 한없이 늦어진다.',
      '',
      ':::flow',
      '보내는 쪽 -> 큐: 초당 1000건',
      '큐 -> 받는 쪽: 초당 300건만 나간다',
      '큐 -> 보내는 쪽: 자리가 없다. 잠깐 멈춰라',
      ':::',
      '',
      '큐 길이에 한도가 없으면 문제가 늦게 드러난다. 겉으로는 다 받아 주는데 안에서 쌓이다가 어느 순간 메모리가 바닥난다.',
      '',
      '한도를 두면 선택이 생긴다. 자리가 없을 때 보내는 쪽을 멈추게 하거나, 버리거나, 거절한다.',
      '',
      '멈추게 하는 것이 백프레셔다. 느린 쪽의 속도가 앞으로 전해져 전체가 감당할 수 있는 속도로 맞춰진다.',
      '',
      '버리는 쪽을 고를 때도 있다. 오래된 것부터 버리는 편이 나은 자리가 있다 — 실시간 지표처럼 늦은 값이 쓸모없는 것이 그렇다.',
    ].join('\n'),
    suggestions: [
      '큐 길이에 한도를 안 두면 무엇이 나쁜가?',
      '버릴 때는 무엇부터 버리는가?',
      '백프레셔를 어디까지 전해야 하는가?',
      '자동 확장으로는 왜 안 되는가?',
      '큐가 밀리는 것을 어떻게 알아채는가?',
    ],
  },
]
