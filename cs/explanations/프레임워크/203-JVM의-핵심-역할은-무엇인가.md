# JVM의 핵심 역할은 무엇인가?

`#203` · 프레임워크

JVM은 자바 바이트코드를 실행하는 가상 머신이다. OS로 부터 독립적인 실행 환경을 제공하여 'Write Once, Run Anywhere'를 실현한다.

1. **자바 소스코드 → 바이트코드(.class)** — 컴파일러가 변환한다
2. **바이트코드(.class) → 각 OS의 기계어** — JVM이 읽어 해석한다

컴파일러가 소스코드를 바이트코드로 변환하면, JVM은 이를 읽어 각 OS에 맞는 기계어로 바이트코드를 해석한다. 이 과정에서 메모리 관리와 최적화가 핵심이다.

가장 큰 특징은 가비지 컬렉션을 통해 메모리를 자동으로 관리한다는 점이다. 개발자가 직접 해제할 일이 줄어든다. 다만 누수가 없어지는 것은 아니다. 안 쓰는 객체를 어딘가에서 계속 붙들고 있으면 걷히지 않는다.

JIT 컴파일러는 반복적으로 실행되는 코드를 기계어로 미리 변환해 캐싱한다. 이를 통해 인터프리터 방식의 실행 속도를 획기적으로 개선한다.

---

**[꼬리를 물고 더 파고들기 →](https://cs-pathfinder.vercel.app/q/203)** · [프레임워크 목록](README.md) · [전체 목록](../README.md)

> 이 글은 대부분 AI가 썼다. 틀린 곳을 찾으면 이슈로 알려 주면 고친다.
> 도식은 서비스에서 그림으로 그려진다. 여기서는 GitHub이 그릴 수 있는 표와 목록으로 옮겼다.
