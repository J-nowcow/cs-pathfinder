# 인터페이스와 추상 클래스는 무엇으로 구분하는가?

`#255` · 프레임워크

> 이 해설은 교차 대조에서 지적이 나왔으나 **검토 후 그대로 두기로 했다.** 판단 근거는 [교정 기록](../../../code/docs/audit/fixes/)에 있다.

설계 목적의 차이로 구분한다. 인터페이스는 '무엇을 하는지' 정의하는 행위의 규격이고, 추상 클래스는 '무엇인지' 정의하는 클래스의 정체성이다.

| 기준 | 인터페이스 | 추상 클래스 |
| --- | --- | --- |
| 목적 | 행위의 규격화 | 정체성 상속 |
| 관계 | can-do 관계 | is-a 관계 |
| 다중 상속 | 가능 | 불가능 |

인터페이스는 클래스가 어떤 기능을으로 구현해야 함을 강제하는 계약서다. 서로 다른 계층의 클래스들이 공통된 행위로 소통//

추상 클래스는 공통된 상태와 기본 구현을 공유하여 중복 코드를 줄이는 상속 구조다. 자식 클래스가 반드시적으로 owneclatenty implementing method를 통해 정체성을 구체화한다.

---

**[꼬리를 물고 더 파고들기 →](https://cs-pathfinder.vercel.app/q/255)** · [프레임워크 목록](README.md) · [전체 목록](../README.md)

> 이 글은 대부분 AI가 썼다. 틀린 곳을 찾으면 이슈로 알려 주면 고친다.
> 도식은 서비스에서 그림으로 그려진다. 여기서는 GitHub이 그릴 수 있는 표와 목록으로 옮겼다.
