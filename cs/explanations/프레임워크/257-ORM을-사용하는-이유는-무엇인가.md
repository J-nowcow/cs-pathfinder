# ORM을 사용하는 이유는 무엇인가?

`#257` · 프레임워크

객체지향적 설계와 관계형 데이터베이스 간의 패러다임 불일치를 해결하기 때문이다. 이를 통해 개발자는 SQL로 인한 반복적인 CRUD 작업을 줄이고 비즈니스 로직에 집중할 수 있다.

| 기준 | SQL 매퍼 | ORM |
| --- | --- | --- |
| 중심 관점 | 테이블/SQL | 객체/엔티티 |
| 생산성 | 쿼리 직접 작성 | 메서드 호출로 처리 |
| 유지보수 | SQL과 매핑을 손봐야 한다 | 엔티티를 고치고 DB 마이그레이션도 따로 해야 한다 |

위 표는 SQL 매퍼와 ORM의 결정적인 차이를 보여준다. ORM은 데이터베이스의 행을 객체로 매핑하여 자바 객체로서의 상속이나 참조로 데이터를 처리한다.

하지만 ORM은 학습 곡선이 높고, 복잡한 쿼리 작성 시 성능 최적화가 어려울 수 있다. 특히 N+1 문제는 자주 나오며, 이를 해결하기 위해 페치 조인이나 @EntityGraph 등을 적절히 사용해야 한다.

---

**[꼬리를 물고 더 파고들기 →](https://cs-pathfinder.vercel.app/q/257)** · [프레임워크 목록](README.md) · [전체 목록](../README.md)

> 이 글은 대부분 AI가 썼다. 틀린 곳을 찾으면 이슈로 알려 주면 고친다.
> 도식은 서비스에서 그림으로 그려진다. 여기서는 GitHub이 그릴 수 있는 표와 목록으로 옮겼다.
