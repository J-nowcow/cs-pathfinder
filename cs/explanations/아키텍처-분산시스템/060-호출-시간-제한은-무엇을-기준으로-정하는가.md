# 호출 시간 제한은 무엇을 기준으로 정하는가?

`#60` · 아키텍처 · 분산시스템

바깥에서 안으로 좁혀 간다. 사용자가 기다릴 수 있는 시간에서 시작해 그 안에 들어가도록 각 단계를 나눈다.

위가 위층이다.

| 층 | 설명 |
| --- | --- |
| 사용자 응답 3초 | 여기서 시작한다 |
| 게이트웨이 2.5초 | 남는 시간에 여유를 뺀다 |
| 주문 서비스 2초 | 다시 그 안에서 |
| 결제 호출 1초 | 재시도까지 넣어도 들어가야 한다 |

안쪽이 바깥쪽보다 길면 안 된다. 그러면 바깥이 이미 포기한 요청을 안쪽이 계속 붙들고 자원을 쓴다.

제한이 아예 없는 것이 가장 위험하다. 응답 없는 호출 하나가 스레드를 묶고, 그 스레드가 다 떨어지면 멀쩡한 요청까지 못 받는다.

면접에서는 재시도와의 곱을 짚는다. 1초 제한에 재시도 3회면 최악 4초다. 첫 호출도 1초를 쓴다. 백오프까지 더하면 더 는다. 바깥 예산이 2초라면 이미 넘는다.

---

**[꼬리를 물고 더 파고들기 →](https://cs-pathfinder.vercel.app/q/60)** · [아키텍처 · 분산시스템 목록](README.md) · [전체 목록](../README.md)

> 이 글은 대부분 AI가 썼다. 틀린 곳을 찾으면 이슈로 알려 주면 고친다.
> 도식은 서비스에서 그림으로 그려진다. 여기서는 GitHub이 그릴 수 있는 표와 목록으로 옮겼다.
