# 자료구조 · 알고리즘

질문 26개 · 지적이 나온 해설 11개. [서비스에서 보기](https://cs-pathfinder.vercel.app/questions#c-자료구조-알고리즘)

> 이 글은 대부분 AI가 썼다. 전수 대조에서 지적이 나온 해설에는 제목 바로 아래
> ⚠️ 표시를 달았다. **표시가 없으면 대조에서 지적이 안 나온 것**이다.
> 지적 전문은 [하드 오류 목록](../../code/docs/audit/2026-08-07-hard-errors.md)에 있다.
> 틀린 곳을 찾으면 이슈로 알려 주면 고친다.

> 도식은 서비스에서 그림으로 그려진다. 여기서는 GitHub이 그릴 수 있는
> 표와 목록으로 옮겼다.

## 해시 테이블의 평균 O(1)이 무너지는 경우는?

> ⚠️ 이 해설은 교차 대조에서 **사실 지적이 나왔다**(사람 검토 전). 지적 내용은 [하드 오류 목록](../../code/docs/audit/2026-08-07-hard-errors.md)에 있다.

키가 한 버킷으로 몰릴 때다. 평균 O(1)은 해시가 고르게 흩어진다는 전제 위의 값이라, 그 전제가 깨지면 최악 O(n)으로 간다. 완화는 충돌한 것을 어떻게 담느냐와 언제 늘리느냐 두 갈래다.

| 갈래 | 하는 일 | 최악을 어디까지 |
| --- | --- | --- |
| 담는 방식 | 한 버킷이 길어지면 연결 리스트를 트리로 | `O(n)` → `O(log n)` |
| 늘리는 시점 | load factor를 넘으면 버킷을 두 배로 | 애초에 안 몰리게 |

자바 HashMap은 둘을 함께 쓴다. 앞쪽은 이미 몰린 뒤의 방어고, 뒤쪽은 몰리기 전의 예방이다.

몰리는 이유는 둘이다. 해시 함수가 나빠서 자연히 겹치거나, 누군가 일부러 겹치게 만들거나. 뒤쪽이 해시 충돌 공격인데, 사용자 입력이 그대로 키가 되는 자리에서는 실제 공격 벡터가 된다.

늘리는 것 자체에도 값이 있다. 리사이즈는 모든 키를 다시 배치하므로 그 한 번이 무겁다. 분할 상환하면 평균은 O(1)이지만 평균이 지연을 보장하지는 않는다 — 응답 시간이 중요한 경로에서는 이 한 번이 튀는 지점으로 나타난다.

면접에서는 왜 버킷 수를 2의 거듭제곱으로 잡는지로 한 단계 더 들어온다. 나머지 연산 대신 비트 마스크를 쓸 수 있어서다. 대신 하위 비트만 보게 되므로 해시를 한 번 더 섞어야 한다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/2b02b81f-fd8d-4c73-bea0-074def8764e6)

---

## 정렬의 안정성이 실무에서 문제가 되는 때는?

정렬을 두 번 겹쳐 다중 기준을 만들 때다. 안정성이 없으면 앞서 맞춰둔 순서가 뒤 정렬에서 흐트러진다.

안정 정렬은 키가 같은 원소들의 원래 순서를 그대로 둔다. 이름순으로 정렬한 목록을 부서순으로 다시 정렬하면, 안정 정렬에서는 부서 안이 이름순으로 남는다. 불안정 정렬에서는 그 안이 뒤섞인다.

1. **원본 → 1차 정렬** — 이름순으로 세운다
2. **1차 정렬 → 2차 정렬** — 부서순으로 다시 세운다
3. **2차 정렬 → 결과** — 안정이면 부서 안이 이름순으로 남고, 불안정이면 뒤섞인다

언어마다 기본값이 다르다. 자바는 객체 배열에 TimSort를 써서 안정적이지만 원시 타입에는 퀵정렬 계열을 써서 그렇지 않다. 값이 같은 int 둘은 구별할 방법이 없으니 순서가 바뀌어도 관측되지 않기 때문이다.

그래서 판단 기준은 간단하다. 같은 키를 가진 두 원소를 사용자가 구별할 수 있으면 안정성이 필요하다. 화면에 이름이 보이는 목록이 그렇고, 숫자 배열은 아니다.

면접에서는 비교자를 하나로 합치는 쪽과 두 번 정렬하는 쪽 중 무엇이 나은지로 들어온다. 합치면 안정성에 기대지 않아 안전하지만, 기준이 런타임에 정해지는 화면에서는 겹쳐 정렬하는 편이 단순하다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/b3ca72d9-b8c3-4d45-9a56-feefe91eb6a7)

---

## 이진 탐색 트리가 한쪽으로 치우치면 무엇이 문제인가?

연결 리스트가 된다. 탐색이 절반씩 줄어든다는 전제가 깨져서 O(log n)이 O(n)으로 내려앉는다. 정렬된 데이터를 순서대로 넣으면 실제로 이 모양이 나온다.

| 상태 | 높이 | 탐색 |
| --- | --- | --- |
| 균형 잡힌 트리 | `log n` | `O(log n)` |
| 한쪽으로 치우친 트리 | `n` | `O(n)` |

그래서 스스로 균형을 잡는 트리를 쓴다. 넣거나 지울 때 높이 차이를 확인하고 회전으로 맞춘다. 회전 비용이 붙지만 최악을 O(log n)으로 묶는 값이다.

해시 테이블과 갈리는 지점은 순서다. 해시는 단건 조회가 더 빠르지만 범위 질의와 정렬 순회를 못 한다. "20에서 30 사이"를 물어야 하면 트리 쪽이다.

데이터베이스 인덱스가 이진 트리 대신 B-트리를 쓰는 이유도 여기 있다. 디스크는 블록 단위로 읽으므로 한 노드에 키를 많이 담아 높이를 낮추는 편이 유리하다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/f4143480-1029-4cdb-b5b5-aea957aad362)

---

## 연결 리스트가 배열보다 항상 삽입이 빠른가?

> ⚠️ 이 해설은 교차 대조에서 **사실 지적이 나왔다**(사람 검토 전). 지적 내용은 [하드 오류 목록](../../code/docs/audit/2026-08-07-hard-errors.md)에 있다.

항상 빠르지는 않으며, 삽입하려는 위치를 찾는 탐색 비용에 따라 더 느려질 수 있다. 맨 앞이나 맨 뒤에 삽입할 때만 연결 리스트가 더 빠르다.

배열과 연결 리스트의 연산별 시간 복잡도를 비교하면 다음과 같다.

| 연산 | 배열 (Array) | 연결 리스트 (Linked List) |
| --- | --- | --- |
| 특정 인덱스 탐색 | O(1) | O(N) |
| 특정 위치에 삽입/삭제 | O(N) | O(1) (노드 포인터 확인 시) |

연결 리스트는 임의의 위치에 삽입할 때 먼저 그 위치까지 노드를 타고 이동해야 한다. 탐색에 O(N)의 시간이 걸리므로, 삽입 자체는 O(1)이라도 총 시간 복잡도는 O(N)이 된다.

반면 배열은 탐색은 O(1)로 빠르지만 삽입 시 뒤의 원소들을 한 칸씩 밀어내는 쉬프트 비용 때문에 O(N)이 걸린다. 결국 데이터가 무작위로 위치할 때는 두 자료구조 모두 한계가 있다.

데이터의 삽입과 삭제가 양 끝에서만 빈번하게 일어난다면 연결 리스트가 배열보다 압도적으로 유리하다. 대표적으로 큐(Queue)를 구현할 때 연결 리스트가 쓰인다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/ee10b43d-e2e3-4f5d-893d-1295abf9b96a)

---

## 스택과 큐는 각각 어떤 상황에서 선택해야 하는가?

스택은 가장 최근 데이터를 먼저 처리해야 할 때 쓰고, 큐는 들어온 순서대로 데이터를 처리해야 할 때 선택한다.

| 기준 | 스택 (Stack) | 큐 (Queue) |
| --- | --- | --- |
| 입출력 방식 | LIFO (후입선출) | FIFO (선입선출) |
| 주요 용도 | 되돌리기, 재귀 처리 | 작업 대기열, 버퍼 |
| 알고리즘 | 깊이 우선 탐색 (DFS) | 너비 우선 탐색 (BFS) |

위 표는 두 자료구조의 핵심 동작 방식과 쓰임새 차이를 보여준다. 스택은 입력된 역순으로 돌아가는 이력 관리에 유용하며, 큐는 유입된 요청을 순서대로 소화하는 완충 지대에 알맞다.

스택은 함수 호출(Call Stack)이나 실행 취소(Undo)처럼 직전의 상태를 기억해야 하는 곳에 필수적이다. 반면 큐는 이메일 발송 대기열이나 프린터 인쇄 작업처럼 순차적인 처리가 보장되어야 하는 비동기 시스템의 기초가 된다.

면접에서는 실무적인 한계도 자주 묻는다. 스택은 메모리 한계로 인한 오버플로우를 주의해야 하고, 큐는 데이터를 꺼낼 때 발생하는 배열 내 요소 이동 비용을 줄이기 위해 원형 큐나 연결 리스트 구조를 고민해야 한다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/df71f7ad-ff91-4396-add5-4ebc09ba6352)

---

## 해시 충돌 발생 시 해결 방법은 무엇인가?

체이닝과 개방 주소법을 사용한다. 체이닝은 연결 리스트로 데이터를 엮고, 개방 주소법은 빈 공간을 찾아 저장한다.

| 기준 | 체이닝 | 개방 주소법 |
| --- | --- | --- |
| 저장 방식 | 외부 리스트 연결 | 내부 빈 슬롯 탐색 |
| 성능 저하 | 리스트 길어질 때 | 클러스터링 발생 시 |
| 메모리 사용 | 추가 메모리 필요 | 미리 할당된 공간 사용 |

체이닝은 삭제가 쉽고 테이블이 꽉 차지 않는다. 하지만 리스트가 길어지면 탐색 시간이 O(n)으로 늘어난다. 최근 Java의 HashMap은 일정 길이 이상이면 트리로 변환해 이를 해결한다.

개방 주소법은 추가 메모리가 없지만, 데이터가 뭉치는 클러스터링 현상이 발생한다. 선형 탐색은 인접한 빈칸을 찾으므로 뭉침이 심해 효율이 떨어진다.

해시 함수가 균등하게 분산시키지 못하면 충돌이 잦아진다. 이 경우 시간 복잡도가 최악의 경우 O(n)까지 떨어진다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/475ef15c-b848-4b08-ac7f-781cc2e74844)

---

## 일반 이진트리 대신 이진탐색트리를 사용하는 이유는 무엇인가?

> ⚠️ 이 해설은 교차 대조에서 **사실 지적이 나왔다**(사람 검토 전). 지적 내용은 [하드 오류 목록](../../code/docs/audit/2026-08-07-hard-errors.md)에 있다.

정렬된 구조를 유지하여 특정 값을 빠르게 찾기 위해서다. 부모 노드를 기준으로 작은 값은 왼쪽, 큰 값은 오른쪽에 배치하여 탐색 범위를 지속해서 줄인다.

| 구분 | 이진트리 | 이진탐색트리 |
| --- | --- | --- |
| 노드 배치 | 규칙 없음 | 왼쪽 < 부모 < 오른쪽 |
| 평균 탐색 | O(N) | O(log N) |

표에서 보듯 이진탐색트리는 탐색 시 매 단계마다 비교 대상을 절반으로 줄여 평균 O(log N) 시간에 데이터에 접근한다. 단순 이진트리는 모든 노드를 확인해야 해 O(N)이 걸린다.

다만 정렬된 데이터가 순서대로 들어오면 한쪽으로 치우친 편향 트리가 된다. 이 경우 선형 탐색과 다를 바 없어 최악의 경우 O(N)으로 성능이 저하된다.

이러한 한계를 극복하기 위해 실무에서는 높이 균형을 자동으로 맞추는 Red-Black 트리나 AVL 트리 같은 자발적 균형 트리를 활용한다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/0e2fdc1c-8df4-4c4b-86c7-86979b559ba7)

---

## 그래프를 인접 행렬과 인접 리스트 중 어떤 방식으로 구현해야 하는가?

간선의 밀도와 탐색 연산의 주된 목적에 따라 결정한다. 간선이 많은 밀집 그래프는 인접 행렬을, 간선이 적은 희소 그래프는 인접 리스트를 선택하는 것이 유리하다.

| 기준 | 인접 행렬 | 인접 리스트 |
| --- | --- | --- |
| 공간 복잡도 | **O(V²)** | **O(V + E)** |
| 간선 존재 확인 | **O(1)** | **O(degree(V))** |
| 인접 정점 탐색 | **O(V)** | **O(degree(V))** |

인접 행렬은 2차원 배열을 써서 두 정점 간의 연결 여부를 상수 시간에 확인한다. 정점 수가 많고 간선이 적다면 메모리 낭비가 크다.

인접 리스트는 각 정점에 연결된 정점만 가변 배열이나 연결 리스트로 저장한다. 메모리를 절약하지만 특정 두 정점이 연결되었는지 확인하려면 리스트를 순회해야 한다.

실무나 일반적인 문제 해결에서는 정점 수에 비해 간선 수가 적은 희소 그래프가 많아 인접 리스트를 주로 사용한다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/9790ab29-7f64-4732-9a0f-f187c14796ce)

---

## 다익스트라와 벨만-포드 알고리즘은 언제 구분하여 쓰는가?

> ⚠️ 이 해설은 교차 대조에서 **사실 지적이 나왔다**(사람 검토 전). 지적 내용은 [하드 오류 목록](../../code/docs/audit/2026-08-07-hard-errors.md)에 있다.

간선의 음수 가중치 존재 여부와 음수 사이클의 유무에 따라 구분하여 쓴다. 음수 가중치가 없다면 다익스트라를 쓰고, 있다면 벨만-포드를 써야 한다.

| 기준 | 다익스트라 | 벨만-포드 |
| --- | --- | --- |
| 음수 가중치 | 허용 안 함 | 허용 |
| 시간 복잡도 | O((V+E) log V) | O(VE) |
| 사이클 감지 | 불가능 | 가능 |

다익스트라는 매번 최단 거리가 가장 짧은 노드를 선택하며 탐색한다. 이미 방문한 노드의 거리는 변하지 않는다고 가정하므로 음수 가중치가 있으면 오동작한다. 대신 우선순위 큐를 사용해 속도가 매우 빠르다.

벨만-포드는 매 단계 모든 간선을 전부 확인하며 거리를 갱신한다. 이 과정을 노드 수만큼 반복하므로 느리지만 음수 가중치를 처리할 수 있다. 마지막 반복에서 값이 또 바뀌면 음수 사이클이 있다고 판단한다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/25db9782-2fdb-4e7e-b0db-7c99840758e5)

---

## 재귀 호출 대신 반복문을 써야 하는 순간은 언제인가?

호출 깊이가 깊어져 스택 오버플로우가 발생할 위험이 있거나 극단의 성능과 메모리 효율이 필요할 때다.

위가 위층이다.

| 층 | 설명 |
| --- | --- |
| 재귀 호출 | 매 호출마다 스택 프레임 생성 (메모리 오버헤드) |
| 반복문 | 동일 스택 프레임 안에서 변수 업데이트 (메모리 고정) |

위 구조처럼 재귀는 함수를 부를 때마다 매개변수, 지역 변수, 복귀 주소를 스택 메모리에 쌓는다. 따라서 가독성은 높지만 오버헤드가 발생한다.

호출 횟수가 입력 크기에 비례해 커지면 스택 영역 한도를 넘겨 프로그램이 종료될 수 있다. 언어가 꼬리 재귀 최적화를 지원하지 않는다면 가독성을 포기하더라도 반복문으로 전환해야 한다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/436fc399-d963-4956-b653-e5c7c949e473)

---

## 최소 신장 트리(MST)를 구성하는 알고리즘으로 무엇을 선택하는가?

> ⚠️ 이 해설은 교차 대조에서 **사실 지적이 나왔다**(사람 검토 전). 지적 내용은 [하드 오류 목록](../../code/docs/audit/2026-08-07-hard-errors.md)에 있다.

가중치 합이 최소가 되는 신장 트리를 찾기 위해 크루스칼이나 프림 알고리즘을 선택한다. 데이터의 밀집도에 따라 선택 기준이 달라진다.

| 기준 | 크루스칼 | 프림 |
| --- | --- | --- |
| 데이터 밀집도 | 희소 그래프 | 밀집 그래프 |
| 구현체 | Union-Find | 우선순위 큐 |

크루스칼 알고리즘은 간선 위주로 선택한다. 가중치가 낮은 간선을 우선적으로 연결하며, 사이클이 발생하지 않는지 Union-Find로 검사한다.

프림 알고리즘은 정점 위주로 선택한다. 현재 트리로 연결된 정점들 중 가장 가중치가 낮은 정점을 추가하며 확장한다.

간선 수가 적은 희소 그래프에서는 크루스칼이 효율적이다. 반면 정점 수가 많고 간선이 빽빽한 밀집 그래프에서는 프림이 유리하다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/929059df-0698-4f86-ac43-2a540fb31a54)

---

## 문자열 검색 효율을 높이는 최적의 자료구조는 무엇인가?

트라이(Trie)다. 문자열을 트리 구조로 저장해 접두사 공유를 통해 탐색 시간을 문자열 길이에 비례하게 줄인다.

위가 위층이다.

| 층 | 설명 |
| --- | --- |
| 트라이 | 문자열의 각 문자를 노드로 저장 |
| 해시 맵 | 전체 문자열을 키로 저장 |

트라이는 문자열의 공통 접두사를 노드로 공유한다. 따라서 동일한 시작 문자를 가진 단어들은 같은 경로를 따라가므로 중복 탐색을 피한다.

KMP나 라빈-카프 알고리즘은 특정 텍스트 내에서 패턴을 찾을 때 쓴다. 트라이는 사전(Dictionary)처럼 여러 단어를 빠르게 검색하거나 자동완성에 적합하다.

시간 복잡도는 O(L)이다. 여기서 L은 검색하려는 문자열의 길이이며, 데이터셋의 크기와 상관없이 일정하게 유지된다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/a800d17a-9f99-4247-a5a5-e44f199a00b5)

---

## 그리디 알고리즘과 동적 계획법은 무엇으로 구분하는가?

> ⚠️ 이 해설은 교차 대조에서 **사실 지적이 나왔다**(사람 검토 전). 지적 내용은 [하드 오류 목록](../../code/docs/audit/2026-08-07-hard-errors.md)에 있다.

최적 부분 구조를 가졌으나, 현재의 최적해를 선택하는지 아니면 모든 경우의 수를 고려하여 결정하는지로 구분한다.

| 기준 | 그리디 알고리즘 | 동적 계획법 |
| --- | --- | --- |
| 선택 방식 | 현재의 최적해를 선택 | 모든 부분 문제의 해를 조합 |
| 효율성 | 연산량이 적고 빠름 | 연산량이 많고 정확함 |
| 최적성 보장 | 특정 조건에서만 가능 | 항상 최적해를 보장함 |

그리디 알고리즘은 매 순간 가장 이득이 되는 선택을 하며, 뒤를 돌아보지 않는다. 이 방식은 선택지가로 인해 전체적인 최적해를 구할 수 없는 경우에 대해 취약하다.

반면, 동적 계획법은 작은 문제의 답을 메모이제이션을 통해 저장하여 중복으로 계산하지 않는다. 모든 가능성을 탐색하며 과거의 것을 활용해 최종적인 최적해를 구성한다.

두 방식의 차이점을 이해하는 것은 문제의 최적 부분 구조와 중복되는 부분 문제로 구성되어 있는지 판단하는 정확한 정확한 기준을 세우는 데 중요하다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/95ba245f-289f-4640-b244-f70a914fafe1)

---

## 스택과 큐의 가장 큰 차이는 무엇인가?

데이터가 나가는 순서의 차이다. 스택은 마지막에 들어온 것이 먼저 나가고, 큐는 먼저 들어온 것이 먼저 나간다.

| 기준 | 스택 | 큐 |
| --- | --- | --- |
| 처리 순서 | LIFO (후입선출) | FIFO (선입선출) |
| 입출력 지점 | 한쪽 끝에서만 가능 | 입구와 출구가 다름 |

스택은 되돌리기(Undo)나 함수 호출 스택처럼 가장 최근의 상태를 기억해야 할 때 쓴다. 반면 큐는 프로세스 스케줄링이나 프린터 대기열처럼 순서대로 처리해야 할 때 적합하다.

두 구조 모두 삽입과 삭제의 시간 복잡도가 O(1)이다. 하지만 큐를 배열로 구현할 때 앞쪽 데이터를 지우면 나머지 데이터를 당겨야 하는 비용이 발생한다. 이를 위해 원형 큐를 주로 사용한다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/48921120-138d-4047-ade4-70261a9610f9)

---

## 그래프 구현 시 인접 행렬과 인접 리스트 중 무엇을 선택하는가?

데이터의 양과 간선의 밀도에 따라 선택한다. 간선이 많다면 인접 행렬을, 적다면 인접 리스트를 쓴다.

| 기준 | 인접 행렬 | 인접 리스트 |
| --- | --- | --- |
| 공간 복잡도 | O(V^2) | O(V+E) |
| 연결 확인 | O(1) | O(degree(V)) |
| 간선 추가/삭제 | O(1) | O(degree(V)) |

인접 행렬은 2차원 배열로 구현한다. 정점의 수가 적을 때 연결 여부를 즉시 확인하기 좋다. 하지만 정점이 늘수록 메모리 낭비가 심하다.

인접 리스트는 연결된 정점만 저장한다. 정점 수 대비 간선 수가 적은 희소 그래프에서 메모리를 획기적으로 아낀다.

실무에서는 대부분의 그래프가 희소 그래프이므로 인접 리스트를 주로 쓴다. 다만, 모든 정점이 서로 연결된 완전 그래프라면 행렬이 유리하다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/95ba7ad5-7f4d-4509-b33a-d3996401875c)

---

## 공유 자료구조를 Thread-Safe하게 만드는 방법은 무엇인가?

> ⚠️ 이 해설은 교차 대조에서 **사실 지적이 나왔다**(사람 검토 전). 지적 내용은 [하드 오류 목록](../../code/docs/audit/2026-08-07-hard-errors.md)에 있다.

상호 배제(Mutual Exclusion)를 통해 한 번에 하나의 스레드만 데이터에 접근하도록 제어하는 것이다.

| 기준 | 동기화 블록 | CAS (Compare-And-Swap) |
| --- | --- | --- |
| 방식 | 락으로 접근 제어 | 값 비교 후 교체 |
| 성능 | 컨텍스트 스위칭 비용 발생 | 논블로킹으로 성능 높음 |
| 특징 | 구현이 쉽고 안전함 | 스핀락으로 CPU 점유율 상승 |

동기화 블록이나 `synchronized` 키워드를 사용하면 임계 영역을 최소화하여 동시성 제어를 가능하게 한다. 하지만 락을 획득하지 못한 스레드는 대기 상태로 전환되어 컨텍스트 스위칭 비용이 발생한다.

CAS 연산은 락을 사용하지 않은 논블로킹 알고리즘으로 구현된다. 현재 값과 예상 값이 일치할 때만 새로운 값으로 교체하는 원자적(Atomic) 원리를 이용한다.

최근에는 읽기-쓰기 락(Read-Write Lock)을 것처럼 읽기 작업이 많을 때 읽기 권한을 대해로 분리하여 효율을 높이는 방식을 주로 사용한다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/b702a8a3-b7b2-460d-9dd1-df95688272d5)

---

## 해시 충돌을 해결하는 두 방식의 차이는 무엇인가?

체이닝은 연결 리스트로 충돌 데이터를 쌓고, 개방 주소법은 빈 공간을 찾아 저장한다.

| 기준 | 체이닝 | 개방 주소법 |
| --- | --- | --- |
| 저장 위치 | 버킷 외부 | 버킷 내부 |
| 메모리 활용 | 추가 할당 필요 | 정해진 크기 내 활용 |
| 성능 저하 | 리스트 탐색 시간 증가 | 클러스터링 발생 |

체이닝은 데이터가 계속 늘어나도 저장 가능하며 구현이 간단하다. 하지만 데이터가 한 버킷에 쏠리면 탐색 시간이 선형적으로 증가한다.

개방 주소법은 메모리 효율이 좋으나 데이터가 찰수록 빈 칸을 찾는 비용이 크다. 특히 특정 영역에 데이터가 뭉치는 클러스터링 현상이 발생하면 성능이 급격히 떨어진다.

따라서 데이터의 양을 예측할 수 없다면 체이닝을, 메모리 공간이 한정적이라면 개방 주소법을 선택한다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/466eaf7e-6cae-4f48-a32f-5d1f58832ec2)

---

## 해시 충돌 발생 시 해결적으로 어떤 방법을 사용하는가?

체이닝과 오픈 어드레싱으로 해결한다. 충돌이 발생한 데이터를 연결 리스트나 빈 공간으로 밀어내는 방식이다.

| 기준 | 체이닝 | 오픈 어드레싱 |
| --- | --- | --- |
| 저장 방식 | 리스트로 연결 | 빈 슬롯으로 이동 |
| 공간 효율 | 추가 메모리 필요 | 기존 배열로 해결 |
| 삭제 연산 | 리스트에서 제거 | 삭제 마커 필요 |

체이닝은 버킷으로 리스트를 구현하여 데이터가 계속 늘어나도 수용 가능하다. 다만 리스트가 길어지면 탐색 시간이 O(1)에서 O(n)으로 늘어난다.

오픈 어드레싱은 빈 칸을 찾을 때까지 계속 탐색한다. 데이터가 꽉 찰수록 충돌이 잦아져 성능이 급격히 떨어진다. 이를 막기 위해 로드 팩터를 관리하고 리사이징을 수행한다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/206e4412-af51-4d71-8d71-9210387ee437)

---

## 해시 충돌이 발생했을 때의 해결책은 무엇인가?

> ⚠️ 이 해설은 교차 대조에서 **사실 지적이 나왔다**(사람 검토 전). 지적 내용은 [하드 오류 목록](../../code/docs/audit/2026-08-07-hard-errors.md)에 있다.

해시 충돌 해결책은 크게 체이닝과 오픈 어드레싱으로 나뉩니다. 충돌한 데이터를 저장할 공간으로 연결 리스트를 사용하거나, 빈 공간을 찾아 저장하는 방식입니다.

| 기준 | 체이닝 | 오픈 어드레싱 |
| --- | --- | --- |
| 저장 방식 | 연결 리스트 사용 | 배열 내 빈 공간 탐색 |
| 성능 저하 | 메모리 오버헤드 | 클러스터링 현상 |
| 삭제 처리 | 리스트에서 제거 | 삭제 마커로 관리 |

체이닝은 버킷에 여러 데이터를 연결해 저장한다. 데이터가 많아지면 리스트가 길어져 탐색 시간은 $O(N)$으로 늘어난다. 최근에는 리스트를 레드-블랙 트리로 바꿔 탐색 시간을 $O(\log N)$으로 줄이는 방식을 쓴다.

오픈 어드레싱은 빈 칸을 찾을 때까지 다른 칸을 탐색한다. 선형 탐색을 쓰면 데이터가 뭉치는 클러스터링이 발생해 성능이 급격히 떨어진다. 이를 막기 위해 2차 해시 함수를 쓰거나 무작위로 탐색하는 방식을 사용한다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/53a5b06e-fae6-43c0-b488-00dfce172a75)

---

## 최단 경로 알고리즘의 선택 기준은 무엇인가?

> ⚠️ 이 해설은 교차 대조에서 **사실 지적이 나왔다**(사람 검토 전). 지적 내용은 [하드 오류 목록](../../code/docs/audit/2026-08-07-hard-errors.md)에 있다.

가중치 유무와 음수 가중치 존재 여부에 따라 결정한다. 가중치가 없다면 BFS, 양수 가중치만 있다면 다익스트라, 음수 가중치가 있다면 벨만-포드 알고리즘을 쓴다.

| 기준 | BFS | 다익스트라 | 벨만-포드 |
| --- | --- | --- | --- |
| 가중치 | 없음 | 양수 | 음수 포함 |

BFS는 모든 노드를 동일한 거리로 계산하므로 가중치가 없을 때 가장 빠르다. 다익스트라는 우선순위 큐를 이용해 최단 거리를 확정하며 탐색 범위를 좁힌다.

벨만-포드는 모든 간선을 반복적으로 확인하여 음수 사이클이 있는지 판단한다. 다익스트라와 달리 음수 가중치가 있어도 정확한 거리값을 찾을 수 있다.

알고리즘마다 시간 복잡도와 정확성의 트레이드오프가 발생한다. 상황에 맞는 알고리즘을 선택해 불필요한 계산량을 줄이는 것이 핵심이다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/8296435a-7e7f-4cf3-8f42-343e167d7983)

---

## 문자열 검색 최적화 시 어떤 기준으로 알고리즘을 고르는가?

검색 대상의 크기, 패턴의 반복성, 그리고 검색 빈도에 따라 고른다. 정적인 사전에서 빠른 조회가 필요하면 트라이를, 긴 텍스트 내 단일 패턴 검색은 KMP나 라빈-카프를 쓴다.

| 기준 | 트라이 | KMP / 라빈-카프 |
| --- | --- | --- |
| 주 용도 | 사전식 탐색 | 패턴 매칭 |
| 시간 복잡도 | 문자열 길이 $O(m)$ | 텍스트 길이 $O(n)$ |
| 메모리 사용 | 매우 높음 | 낮음 |

트라이는 문자열을 트리 형태로 저장해 접두사 공유를 극대화한다. 한 번 구축하면 검색 속도가 매우 빠르지만, 각 노드에 포인터 배열을 두어 메모리 낭비가 심하다.

KMP는 패턴 내부의 반복 구조를 미리 계산해 불필요한 비교를 건너뛴다. 텍스트를 되돌아가지 않고 한 번만 훑으므로 효율적이다.

라빈-카프는 해시 값을 이용해 문자열을 비교한다. 해시가 일치할 때만 실제 문자열을 대조하므로, 여러 패턴을 동시에 찾거나 대용량 데이터를 처리할 때 유리하다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/588dc1c9-8c1b-417f-bbeb-c3932c61de1b)

---

## 스택과 큐는 어떤 상황에서 구분해 쓰는가?

데이터의 처리 순서가 최신순인지 선입선출순인지에 따라 결정한다. 가장 나중에 들어온 데이터를 먼저 처리해야 하면 스택을, 먼저 들어온 순서대로 처리해야 하면 큐를 쓴다.

| 기준 | 스택 (Stack) | 큐 (Queue) |
| --- | --- | --- |
| 처리 순서 | LIFO (후입선출) | FIFO (선입선출) |
| 주요 연산 | push / pop | enqueue / dequeue |
| 활용 사례 | 되돌리기, 재귀 호출 | 대기열, 프로세스 스케줄링 |

스택은 데이터의 '맥락'을 유지하는 데 유리하다. 함수 호출 스택처럼 현재 작업을 멈추고 새로운 작업을 수행한 뒤, 다시 이전 상태로 돌아와야 할 때 적합하다.

큐는 데이터의 '흐름'을 제어하는 데 유리하다. 요청이 몰릴 때 이를 순차적으로 처리하여 시스템의 부하를 조절하는 버퍼 역할로 주로 쓰인다.

두 구조 모두 삽입과 삭제가 $O(1)$의 시간 복잡도를 가진다. 하지만 큐를 배열로 구현할 때 삭제 연산 시 데이터 시프팅이 발생하므로, 원형 큐나 연결 리스트로 구현하는 것이 효율적이다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/123ded34-49d4-439c-b50f-6b3267bca2d3)

---

## 이진 탐색 트리를 사용하는 이유는 무엇인가?

> ⚠️ 이 해설은 교차 대조에서 **사실 지적이 나왔다**(사람 검토 전). 지적 내용은 [하드 오류 목록](../../code/docs/audit/2026-08-07-hard-errors.md)에 있다.

데이터를 정렬된 상태로 유지하며 탐색, 삽입, 삭제를 빠르게 수행하기 위해서다.

| 기준 | 배열/리스트 | 이진 탐색 트리 |
| --- | --- | --- |
| 탐색 시간 | O(n) | O(log n) |
| 삽입/삭제 | O(n) | O(log n) |

표에서 보듯 배열보다 탐색 속도가 빠르다. 값의 크기를 비교해 왼쪽 또는 오른쪽 자식으로 이동하며 탐색 범위를 절반씩 줄이기 때문이다.

하지만 데이터가 정렬된 순서로 들어오면 트리가 한쪽으로 치우치는 편향 트리(Skewed Tree)가 된다. 이 경우 시간 복잡도는 O(n)으로 늘어난다.

이를 해결하기 위해 스스로 높이를 조절하는 AVL 트리나 레드-블랙 트리 같은 자가 균형 이진 탐색 트리를 사용한다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/86f59092-177d-47db-90c0-c02bcdd29b86)

---

## 재귀 함수 사용 시 스택 오버플로가 발생하는 이유는 무엇인가?

함수 호출 시마다 스택 프레임이 생성되어 메모리의 스택 영역을 가득 채우기 때문이다.

위가 위층이다.

| 층 | 설명 |
| --- | --- |
| 스택 프레임 | 지역 변수, 매개변수, 복귀 주소 |
| 스택 메모리 | 프레임이 쌓이는 한정된 공간 |

재귀는 자기 자신을 다시 호출하며 멈추지 않고 쌓인다. 각 호출은 이전 상태를 기억해야 하므로 메모리 공간을 계속 점유한다.

탈출 조건이 없거나 깊이가 너무 깊으면 할당된 스택 크기를 초과한다. 이때 런타임 에러인 스택 오버플로가 발생한다.

반복문으로 대체하면 추가적인 스택 프레임 생성이 없어 메모리 효율이 높다. 하지만 트리나 그래프 같은 계층 구조 탐색 시에는 재귀가 가독성이 좋다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/b7d2bf6d-44dc-4f87-b4b6-0a5badabbb01)

---

## 우선순위 큐를 힙으로 구현하는 이유는 무엇인가?

> ⚠️ 이 해설은 교차 대조에서 **사실 지적이 나왔다**(사람 검토 전). 지적 내용은 [하드 오류 목록](../../code/docs/audit/2026-08-07-hard-errors.md)에 있다.

배열로 구현한 우선순위 큐는 삽입이나 삭제 시 데이터를 밀어내야 해서 시간이 많이 걸리기 때문이다.

위가 위층이다.

| 층 | 설명 |
| --- | --- |
| 최대 힙 | 부모가 자식보다 항상 크거나 같다 |
| 최소 힙 | 부모가 자식보다 항상 작거나 같다 |

힙은 완전 이진 트리 구조를 사용한다. 이를 통해 삽입과 삭제 모두 O(log N)의 시간 복잡도로 처리할 수 있다.

반면 정렬된 배열은 삭제는 빠르지만 삽입 시 데이터 이동이 O(N)만큼 필요하다. 정렬되지 않은 배열은 삽입은 빠르지만 최댓값/최솟값을 찾는데 O(N)이 걸린다.

실무에서는 다익스트라 알고리즘이나 OS의 스케줄러처럼 우선순위가 계속 바뀌는 환경에서 힙을 선택한다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/9ac88a5c-11c3-4a52-b642-09a97936fdfa)

---

## B-tree는 왜 디스크에 유리한가?

한 번 읽을 때 많이 가져오기 때문이다. 디스크는 블록 단위로 읽으므로 노드 하나를 블록만큼 크게 잡으면 읽기 횟수가 준다.

- **[30 60]** — 루트 하나에 열쇠가 여럿이다
  - **[10 20]** — 30보다 작은 것
  - **[40 50]** — 30과 60 사이
  - **[70 80]** — 60보다 큰 것

균형 잡힌 이진 트리도 백만 건이면 깊이가 스물이다. 갈래를 백으로 늘리면 셋이면 닿는다. 캐시가 비어 있다면 깊이만큼 디스크를 읽는다.

모든 잎이 같은 깊이에 있다. 넣고 지울 때 노드를 쪼개거나 합쳐서 균형을 지키므로 최악도 `O(log n)`을 지킨다.

위쪽 노드는 대개 버퍼 캐시에 남아 있어 실제 읽기는 깊이보다 적다. 메모리만 쓰는 색인에서는 이 이점이 줄어든다.

[이 질문 파고들기 →](https://cs-pathfinder.vercel.app/q/212ffacb-9cca-4c8f-9cb5-906568c356b3)
