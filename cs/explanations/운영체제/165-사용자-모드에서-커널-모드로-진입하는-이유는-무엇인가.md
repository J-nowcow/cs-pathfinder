# 사용자 모드에서 커널 모드로 진입하는 이유는 무엇인가?

`#165` · 운영체제

사용자 프로그램이 하드웨어에 직접 접근하여 시스템을 파괴하는 것을 막기 위해서다.

위가 위층이다.

| 층 | 설명 |
| --- | --- |
| 사용자 모드 | 프로세스. 제한된 권한으로 실행 |
| 커널 모드 | 운영체제. 모든 하드웨어 권한 보유 |

위 구조처럼 권한을 분리하여 하드웨어 제어권을 독점한다. 프로그램이 메모리나 디스크에 직접 쓰기를 시도하면 CPU가 이를 감지해 커널 모드로 전환한다.

시스템 콜은 프로그램이 스스로 커널에 일을 맡기는 통로다. 다만 유일한 통로는 아니다. 하드웨어 인터럽트와 예외도 커널 모드로 넘긴다. 운영체제가 미리 정해놓은 인터페이스를 통해 요청을 보내고, 커널이 요청의 정당성을 검사한 후 대신 수행한다.

이 과정에서 컨텍스트 스위칭 비용이 발생한다. 빈번한 시스템 콜 호출은 전체 시스템 성능에 영향을 주므로 효율적으로 설계해야 한다.

---

**[꼬리를 물고 더 파고들기 →](https://cs-pathfinder.vercel.app/q/165)** · [운영체제 목록](README.md) · [전체 목록](../README.md)

> 이 글은 대부분 AI가 썼다. 틀린 곳을 찾으면 이슈로 알려 주면 고친다.
> 도식은 서비스에서 그림으로 그려진다. 여기서는 GitHub이 그릴 수 있는 표와 목록으로 옮겼다.
